#[drop(free_it)]
own struct Page { addr: i64 }
fn free_it(p: Page) { p.addr; }
fn main() -> i64 {
    p := Page{ addr: 1 };
    c := 1;
    if c > 0 {
        free_it(p);
    } else {
        c;
    }
    0
}
