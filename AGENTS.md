# NyxOS — Agent Context

## Goal
Evolve NyxOS into a functional x86_64 kernel with filesystem, networking, shell, process multitasking, GUI, multimedia, and audio.

## Commit rule
Every commit must keep these in sync: GitHub Wiki (`nyx-os.wiki.git`), website (`docs/`), README.md, and any version/status references in AGENTS.md. Always check what changed and update accordingly before committing.

## Build & Test
- Cross-compiler: host GCC with `-m64` (or `x86_64-elf-gcc` for cross)
- Build (PowerShell): `.\build.ps1` → `kernel/nyx-kernel.bin`
- Build (make): `make -C kernel` → `kernel/nyx-kernel.bin`
- Run (GUI): `.\run.ps1`
- Run (serial debug): `.\run.ps1 -Mode serial`
- Run (with net): `.\run.ps1 -Mode net`
- Run (with disk): append `-hda ext2-test.img` to QEMU args
- ISO: `tools/build.sh` → `NyxOS.iso`
- Repo: `https://github.com/kazah-png/nyx-os`
- GitHub Wiki: live at `https://github.com/kazah-png/nyx-os/wiki` — 16 pages pushed; `git clone https://github.com/kazah-png/nyx-os.wiki.git`

## Releases
| Version | Description |
|---------|-------------|
| v1.0.0 | Base kernel |
| v1.1.0 | Ramdisk VFS + shell commands |
| v1.2.0 | Real networking (RTL8139, ARP, IP, UDP, ICMP/ping) |
| v2.0.0 | Clean slate: removed all hacking/offensive code |
| v2.0.1 | DOOM game integration (VGA mode 13h, doomgeneric, shell command) |
| v2.1.0 | Preemptive multitasking, interrupt-driven I/O, pipe support, diff command |
| v2.1.1 | Full TCP stack, RTL8139 fixes, GUI/window compositor, PC speaker, bitmap font |
| v2.2.0 | GUI auto-boot: NyxOS Desktop launches at startup instead of text shell |
| v2.3.0 | Real EXT2 read support: VFS mount layer, auto-mount at /mnt, ls/cd/cat on ext2 |
| v2.4.0 | Sound Blaster 16 DMA/IRQ audio driver (sb16play command, DMA buffer fix, auto-init) |
| v3.0.0 | ELF userspace loader, initramfs, per-process paging, ring 3 execution, int 0x80 syscalls |
| v3.1.0 | RTC driver, more syscalls (open/read/close/getpid/sbrk/fsize/exec), libc (printf/malloc/snprintf), initramfs auto-boot, desktop polish (right-click, settings, wallpaper) |
| v4.0.0 | Full x86_64 native (long mode, 4-level paging, syscall/sysret, ELF64, ring 3), clean 32-bit dead code removal, docs corrected |
| v4.1.0 | Higher-half kernel mapping (PML4[256] mirror), user/kernel page-table isolation (user has no identity mapping), CR3 switching in ISR/IRQ/syscall entries, scheduler uses next_cr3 |
| v4.2.0 | NX bit (No-Execute) + SMEP (Supervisor Mode Execution Prevention), PAGE_NX added to user stack/heap/data, Local APIC + I/O APIC init, CPUID detection, IST (Interrupt Stack Table) for double fault (#8) and NMI (#2), repo cleanup (README x86_64, .gitignore, AGENTS.md) |
| v5.0.0 | Full GUI application suite: Text Editor (file open/save, cursor nav), Image Viewer (test pattern, zoom/pan), Sound Test (PC speaker + SB16 sine/square/sweep). Brighter wallpaper gradient (sky-blue + grass). 8 desktop icons. **All placeholders replaced with real apps.** No crashes, zero warnings in build. |
| v5.1.0 | Stability and bugfix release: TSS struct alignment fix (IST pointers shifted 4 bytes early → triple fault on double fault/NMI), GDT limit correction, APIC/PIC IRQ masking fix (PIC left fully masked when APIC active), switch_to_user_process inline asm operand reversal fix, VGA 8x16 font data corruption fix (marker bytes inserted in each glyph → garbage text in GUI). QEMU display changed to sdl for Windows compatibility. |
| v5.3.0 | Login system: boot animation (~5s, NyxOS-themed, 23-step progress bar), credential storage on EXT2 (/etc/passwd), framebuffer login screen with keyboard input, default user nyx/nyx, fallback when no EXT2 disk, login failure → reboot, success → launch desktop. |
| v5.4.0 | Audit/hardening pass: fixed clean-build break (`sha256.h` pulled host `<stdint.h>`, conflicting with kernel int types); build is warning-free again (0 warnings from ~160 — pointer-truncation casts routed through `uintptr_t`, keyboard keymap over-initializers trimmed, dead code removed). **Security:** ring-3 syscall boundary hardened — user pointers validated against the canonical user half, and userspace now gets small integer fds via a translation table instead of raw (forgeable) kernel VFS handles (closed an info-leak + arbitrary-kernel-r/w reachable via `exec`). Panic handler now prints faulting RIP/CS/ring/error. |
| v5.5.0 | **Ring-3 userspace now actually runs.** `exec <elf>` loads an ELF64, enters ring 3, services syscalls, and returns to the shell on exit. Fixed the full chain: (1) EFER.SCE was never enabled → `syscall` #UD'd; (2) LSTAR pointed at the low link address, unmapped under the user CR3 → set it to the PML4[511] higher-half alias; (3) `syscall_entry` clobbered RAX/RBX (and truncated user RSP) *before* SAVE_REGS — rewrote it to save all user GPRs first using RIP-relative ops (no scratch reg), with `kernel_rsp` stored as a higher-half alias; (4) added `copy_from_user`/`copy_to_user` that walk the user page tables to physical (masking bits 51:12 — `~0xFFF` alone kept the NX bit and produced non-canonical → #GP); (5) return via `iretq` (NASM's bare `sysret` is the 32-bit form and truncates RSP); (6) `switch_to_user_process` uses a small setjmp/longjmp (`ku_setjmp`/`ku_longjmp`) so exit() unwinds to the caller instead of halting. Verified: init.elf prints, getpid/malloc(sbrk)/snprintf/write/exit all work, control returns to boot. |
| v5.6.0 | **Userspace cleanup + heap bug.** (1) `exec` now reaps the process on exit: `free_page_directory` (paging.c) walks the user half (PML4[0..510], skips the 511 kernel mirror) freeing every leaf frame + table; `reap_user_process` (process.c) also frees the kernel/context stack (`kernel_stack-4096` is the kmalloc base — `proc->stack` is a *middle* pointer, so the old `destroy_process` `kfree(proc->stack)` corrupted the heap; fixed). Verified clean over repeated execs. (2) **Heap bug:** `kmalloc` sent every request ≤ `SLAB_MAX_OBJ` (1024) to the slab, but the slab caches only went up to 512, so `slab_alloc` returned NULL for 505..1016 B and kmalloc returned NULL *instead of falling back to the heap* — every mid-size allocation failed (e.g. VFS file writes: `/home/user/welcome.txt` was silently empty). Fixed: kmalloc falls back to heap on slab miss; two header magics (`_SLAB`/`_HEAP`) so kfree routes correctly regardless of size; added the missing 1024 slab class. (3) `SYS_WRITE` now routes file fds (≥3) through the ufd table to `vfs_write`, so userspace can create/write files (not just stdout/stderr). init.elf exercises the full file-I/O path (open/create/write/read/close) as a regression test. |
| v5.7.0 | **Per-fd offsets / streaming file I/O.** Added `vfs_pread`/`vfs_pwrite` (offset-aware; pwrite grows + zero-fills without discarding existing content). The userspace fd table (`ufd_*` in syscall.c) now tracks a byte offset per open fd, advanced by each `SYS_READ`/`SYS_WRITE`, so ring-3 reads stream across calls and writes append instead of overwriting. Kernel-internal `vfs_read`/`vfs_write` (offset 0) are unchanged — no blast radius on the shell's one-shot open→read→close callers. Verified: init.elf writes "Hello, " + "userspace file I/O!" in two calls and streams it back in two reads. |
| v5.7.1 | **Fd-based I/O routes to EXT2 mounts (persistent files).** Previously only the path-based helpers (`vfs_write_file`/`vfs_cat_file`) reached a mounted FS; the fd-based path (`vfs_open`/`read`/`write`/`close` — used by `echo >`, head/tail/grep/sort/wc/diff, the editor, and userspace) hit the ramdisk and never persisted, and `/mnt` isn't even a ramdisk node. Now `vfs_open` detects a mount path (`vfs_find_mount`) and returns a transient **mount-backed node** preloaded from the FS (`read_file`); `vfs_write`/`vfs_pwrite` flush the whole node back via `write_file`; `vfs_close` frees it. Added a node free-list so these per-open nodes don't exhaust the 128-node pool. Verified across reboot: a file written to `/mnt` via `vfs_open`+`vfs_write` is read back on the next boot with the same disk image. (Ext2 write engine + persistence were already solid; this wires the fd layer to it.) |
| v5.7.2 | **Fd-based `readdir` on EXT2 mounts (file-manager can browse disk).** `vfs_open` on a mount path now probes `readdir` (ext2_readdir returns −1 for non-dirs) — if it's a directory it loads all entries once into the mount-backed node; `vfs_readdir` serves them by index and `vfs_close` frees them. Completes the mount FS surface: fd-based file read/write **and** directory listing on `/mnt` (path-based `ls`/`mkdir`/`rm` already worked and persist). Verified: `vfs_open("/mnt")` + `vfs_readdir` loop lists `lost+found/`, files, and created subdirs with correct dir/file typing. |
| v5.7.3 | **Networking works — DHCP completes** (`dhcp` command gets a lease from QEMU slirp). The stack was 100% broken; fixed 7 bugs, chief among them a byte-order bug that killed *every* packet: (1) **IP header checksum stored little-endian** — `ip->checksum = ip_checksum()` wrote the network-order value LE-reversed, so slirp silently dropped all our IP frames (found via `-object filter-dump` pcap: checksum verified to 0x649b not 0xffff). (2) **`ip_send` ARP-resolved broadcast** (255.255.255.255) → hung; now uses the broadcast MAC directly. (3) **RTL8139 RX ring** re-derived the read offset from `CAPR+16` but wrote it back without `−16` (asymmetric, misaligned after packet 1) and never checked BUFE — rewrote to track the offset itself, check the empty bit, and init CAPR=0xFFF0. (4) **RCR** missing MXDMA/RXFTH fields. (5) **DHCP magic cookie** stored byte-reversed and (6) at BOOTP offset 240 instead of 236. (7) **DHCP poll loop** had no inter-poll delay (finished before replies arrived; timer is dead so it busy-waits on port 0x80). Verified end-to-end: DISCOVER→OFFER→REQUEST→ACK, lease 10.0.2.15. **Still TODO** (same byte-order class of bug, now unmasked): DNS (`hdr->flags`/return IP host-order), TCP/HTTP (seq/ack/ports/checksum), and the `ifconfig` IP *display* order (shows 10.0.2.15 reversed). |
| v5.7.4 | **Full network stack: DNS + TCP + HTTP work.** Building on v5.7.3's DHCP, fixed the rest of the host↔network byte-order bugs (all masked until the IP checksum was fixed): **DNS** — `qdcount`/`flags` stored LE (query had 256 questions → dropped), returned/parsed IPs and the timeout loop (used dead `tick_count` → infinite hang); **ARP** — sender/target IPs written high-byte-first (asked "who has 3.2.0.10"), `sleep()` hang, host-order static gateway entry with the wrong MAC (removed); **TCP** — `offset_flags`/`window`/checksum stored LE, checksum pseudo-header + RX `offset_flags` in the wrong order, and `payload_len` counted Ethernet padding (60-byte min frame added 2 phantom bytes → wrong ack → RST). **Routing** — `ip_send` now sends off-subnet traffic via the gateway (was ARP-ing internet IPs directly) and skips loopback when auto-selecting (was using lo's IP for the TCP pseudo-header). **http_get** waits for the handshake before sending. `parse_ip` + all IP prints normalized to network order (`ifconfig`/DHCP show 10.0.2.15, not reversed). Verified end-to-end via `-object filter-dump` pcap: DHCP→lease, DNS resolves, and `http_get(1.1.1.1:80)` returns a parsed **HTTP 301** with body. (A real HTTP server works; a captive-portal/proxy in some envs returns a non-HTTP page.) |
| v5.7.5 | **GUI stability fixed — the `KERNEL_BASE+3` crash is dead** (and with it the timer/scheduler blocker). Root cause, found with QEMU `-d int` (which, unlike serial `printf`, doesn't perturb the Heisenbug): `irq_common` did the CR3 switch using **rax/rbx as scratch BEFORE `SAVE_REGS`**, so on return the interrupted code resumed with a corrupted RBX (= `KERNEL_BASE`); if it then computed a call/jump from rbx it landed at `KERNEL_BASE+3` → `#UD`. Intermittent because it only bit when the IRQ interrupted an instruction about to use rax/rbx — so it showed up under IRQ load (typing in the GUI terminal, or the timer firing). Fix: move `SAVE_REGS` to the very top of `irq_common` (and `isr_common`), same as the earlier `syscall_entry` fix. Also: boot stack 16 KB→128 KB and moved *above* the page tables (a deep compositor call chain could have overflowed into them); removed the `mpos=`/`.` serial debug spam. **Verified visually** (QEMU SDL + monitor `sendkey`/`screendump`): login → desktop, 12+ terminal commands incl. a full DHCP lease, zero crashes. The timer/scheduler can now be revisited (they crashed via this exact bug). |
| v5.7.6 | **Preemptive scheduler + timer live.** (1) **Timer enabled:** the PIT (ISA IRQ0) is delivered on I/O APIC **pin 2** (QEMU ACPI interrupt-source override), not pin 0 — so the old `irq_unmask(0)` was a no-op and `tick_count` stayed 0. Now `ioapic_redirect_irq(2,32,apic_id)`+`ioapic_unmask_irq(2)` deliver ticks; `sleep()`/uptime work (nyxfetch shows real uptime). (2) **Preemptive round-robin** over kernel threads in `irq_scheduler_tick` (behind `sched_enable()`; dormant by default → keeps the old cooperative behaviour). The compositor runs as the `compositor` process; `init` is a placeholder. Fixed two latent bugs that only fire once you actually `iretq` into a built frame: **(a)** `init_task_stack` set **SS=0x08** (kernel *code* selector) — long-mode `iretq` always pops SS, and loading a code seg #GP's; must be `KERNEL_DS` (0x10). **(b)** the round-robin would switch into the `init` placeholder whose `stack==NULL` (never given a frame) → `iretq` off a null RSP → #GP→#PF→#DF→triple fault; now skips NULL-stack threads. Both found with `qemu -d int,cpu_reset`. (3) **`mtdemo`** shell command: spins up two demo kernel threads that heartbeat ~1 Hz for 3 s then self-retire (`state=0`) so the desktop returns to full speed — the scheduler self-test. Verified visually: `mtdemo` runs both threads concurrently with a stable GUI (counters ~470–515M in 400 ms), then they retire; `ps` lists them. (4) **printf width/flags:** `vprintf` now parses `-`/`0` flags + width (`%-4d`, `%5u`, `%-16s`) — previously the `default` case swallowed `%-4d` **without consuming the arg**, shifting every later arg and garbling `ps`. Backward-compatible (width 0 ⇒ identical output). |
| v5.7.7 | **Preemptive ring-3 processes.** `usertest` spawns two copies of a new `spin.elf` as *scheduled* user processes (`spawn_user_path` → `sched_managed`) that run in ring 3, time-sliced with the GUI, then exit and are reaped — the desktop stays fully responsive (a spin loop would otherwise freeze it). Machinery: (1) the scheduler now switches **user CR3 + TSS RSP0** for `sched_managed` procs and tracks `current_idx` so syscalls resolve per-process; it still skips `init.elf`/the blocking-exec target. (2) **`preempt_disable/enable`** guards `kmalloc`/`kfree` (the freelists aren't reentrant). (3) `SYS_EXIT` from a scheduled proc → `PROC_ZOMBIE` + yield; `reap_zombies()` (a compositor background task) frees the address space/stacks from a safe context — you can't free a process from its own stack. Two latent bugs fixed, both found with `qemu -d int,cpu_reset`: **(a)** user-proc kernel stacks must be the **higher-half** alias — `init_user_task_stack` stored the low kmalloc address, so once the scheduler switched to the user CR3 the low stack was unmapped → #PF→triple fault; store `+KERNEL_BASE` and drop the now-redundant conversion in `switch_to_user_trampoline`. **(b)** a blocking `exec` running while the timer/scheduler is live faulted (#PF, ring 3, kernel CR3): it isn't tracked by `current_idx`, so the "stay on current" path resumed it with the *kernel* CR3 and the user code was unmapped — now `g_user_proc` is resumed in its own CR3 (this had been latent since the timer went live in v5.7.6; fast programs just never took an IRQ mid-run). Verified: `usertest` (2 procs, 8 heartbeats, reaped) **and** `exec /init.elf` (full run) both work with the scheduler active, GUI stable, 0 panics. |
| v5.7.8 | **`wait()` / job control — foreground & background jobs.** `exec <file>` is now built on `spawn_user_path` + a real blocking **`kwait(pid)`**: it spawns the ELF as a scheduled ring-3 process and blocks the shell until the child exits, then prints its exit code (`[exec] PID N exited (code C)`). `spawn <file>` runs the same thing as a **background** job (non-blocking) — it keeps running while the desktop stays interactive; `ps` lists it (with PPID = the shell), and it's reaped when it exits. Mechanism: new **`PROC_BLOCKED`** state + `waiting_for`/`exit_code` fields; `kwait` parks the caller with the classic `cli`-check-then-`sti;hlt` pattern (atomic vs. the child exiting — no lost wakeup on a single core), the scheduler skips blocked threads so the child gets the full CPU, and the child's `SYS_EXIT` calls `wake_waiters()` to set the parent back to `PROC_RUN`. `spawn_user_path` stamps `ppid` so the wakeup finds the right parent. This retires the setjmp/longjmp blocking path — `exec` no longer calls `switch_to_user_process` (that primitive + `g_user_proc` + the trampoline are now unused; removal is a pending cleanup). Verified: `exec /init.elf` runs the full program and returns code 0 with the GUI recovering; `spawn /spin.elf` backgrounds a job that `ps` shows as PID 6 (PPID 4) while the shell stays responsive, then reaps; 0 panics. |
| v5.7.9 | **Legacy blocking-exec path removed.** Pure cleanup, no behaviour change: now that `exec` runs on `spawn`+`kwait` (v5.7.8), the old setjmp/longjmp launcher is dead code, so it's gone — `switch_to_user_process`, `g_user_proc`, `return_from_user_process`, `user_return_ctx`, the `switch_to_user_trampoline` (isr_stubs.asm) and `ku_setjmp`/`ku_longjmp` (switch.asm). `get_cur_proc` now resolves the running process purely via `current_idx`/`get_current_process` (syscalls run with interrupts masked, so it can't change mid-call); `SYS_EXIT` is uniform (every user proc is scheduler-managed → zombie + `wake_waiters` + yield); the scheduler's `g_user_proc` special-case and `reap_user_process`'s `g_user_proc` clear are dropped. One launch path for ring-3 code remains (scheduler `iretq`). Kernel is ~1 KB smaller, 0 warnings. Verified: `exec /init.elf` still runs the full program and returns code 0 (getpid resolves via current_idx → `PID: 5`), `spawn /spin.elf` backgrounds fine, GUI stable, 0 panics. |
| v5.7.10 | **Blocked sleep() + safe kill (timer wait queue).** (1) **`sleep()` no longer busy-waits.** With the scheduler running it now blocks: the caller is marked `PROC_BLOCKED` with a `wake_tick` and yields (`cli`-check-then-`sti;hlt`), so the CPU goes to other threads; `irq_scheduler_tick` runs a wait-queue pass each tick that wakes any sleeper whose `wake_tick` has arrived (distinguished from `kwait` blockers, which use `wake_tick==0`). Before the scheduler exists (early boot) it falls back to an idle `hlt`-loop on `tick_count`. `mtdemo`'s `sleep(400)` now hands the demo threads the full CPU while the shell sleeps. (2) **`kill` is safe on a running process.** `destroy_process` no longer frees a live `sched_managed` proc out from under the scheduler (its ring-3 context may be mid-flight) — it marks it `PROC_ZOMBIE` (+ `wake_waiters`), the scheduler drops it, and `reap_zombies()` frees it. `cmd_kill` refuses kernel threads (`page_directory==NULL`) so you can't nuke init/idle/compositor. Verified: `spawn /spin.elf` then `kill 5` stops it after 3/10 heartbeats (no `SYS_EXIT`), `ps` shows it reaped; `mtdemo`'s blocking sleep returns cleanly (a broken wake would hang the GUI); 0 panics. |
| v5.7.11 | **Shell job control — `jobs` + `wait`.** Completes the job-control set (`exec` foreground, `spawn` background, `kill`): **`jobs`** lists the shell's background jobs (scheduler-managed procs) with pid/ppid/state; **`wait [pid]`** blocks the shell until a specific child (`kwait`) or *all* children (`kwait_all`) exit. `kwait_all` reuses the block/wake machinery — parks the caller `PROC_BLOCKED` with `waiting_for==0` (wait-any) and is woken by any child's `SYS_EXIT` (wake_waiters already matched `waiting_for==0`); zombies count as done and are freed by reap_zombies() once `wait` returns. No struct/scheduler changes — pure additions. Verified: `spawn`×2 → `jobs` lists both running → `wait` blocks until both `exit(0)` (20 heartbeats) then "all background jobs done" → `jobs` shows none (reaped); GUI recovers, 0 panics. |
| v5.7.12 | **Priority scheduling (weighted round-robin).** The scheduler is no longer strict 1-tick round-robin: each proc has a `sched_weight` (ticks per turn; 0⇒1) and a `sched_quantum` countdown. `irq_scheduler_tick` keeps the current thread while its quantum lasts, then picks the next runnable and stamps it a fresh quantum = its weight — so a high-weight proc runs several ticks in a row and gets a proportionally bigger CPU share. The **compositor** is given `SCHED_WEIGHT_GUI` (4), so the desktop stays responsive while background jobs (weight 1) run (~4/(4+N) of the CPU with N jobs). Verified directly: `mtdemo` now gives mtdemoB **weight 3** vs mtdemoA weight 1, and over a 400 ms sample B counted **99.1M vs A's 34.3M — ratio 2.89 ≈ 3.0**, exactly tracking the weights. No regression: `exec /init.elf` runs and exits code 0, GUI stable, 0 panics. (Struct change → clean rebuild.) |
| v5.7.13 | **VFS re-entrancy audit + guard.** Audit of shared FS state under preemption found the dominant hazard: EXT2 uses **one global scratch buffer** (`block_buf` in ext2.c) across a whole operation, and the VFS **node pool / free-list** (`alloc_node`/`free_node`) aren't reentrant. User FS syscalls already run with interrupts masked (atomic), but a *kernel-context* FS caller — a shell command / file-manager action on the compositor thread — runs with interrupts on and can be preempted mid-operation; a scheduled process's FS syscall would then trample the same buffer/pool. (Latent today: `exec` blocks the compositor while its child does I/O, and `spawn`ed demos do no FS — but it'd bite the moment a background job touches the FS.) Fix: a coarse single-core "FS lock" via `preempt_disable` — `alloc_node`/`free_node` guard the pool, and seven thin wrappers (`fs_read_file`/`fs_write_file`/`fs_readdir`/…) on the mount function-pointers run each EXT2 op with preemption off, so ext2.c is untouched and every VFS→EXT2 path is serialized. Noted (not fixed, not crash-risk): the `ufd` table is shared across procs but safe under atomic syscalls (fds aren't per-process isolated); `current_dir` is a single global CWD; `auth.c` + the boot self-test call ext2 directly but only pre-scheduler (boot/login). Verified: boot mount/write-test/auth, `ls /mnt` (ext2 readdir wrapper), and `exec /init.elf` file I/O all work, 0 panics. |
| v5.7.14 | **Per-process file descriptors.** The `ufd` table (opaque fd → internal VFS handle + offset) moved from three **global** arrays in syscall.c into `process_t` — one table per process. `ufd_alloc`/`lookup`/`release`/`offset_of` now index the current process's table (`get_cur_proc()`; syscalls run with interrupts masked so `current_idx` is stable). fds are now **isolated** (a process can't name another's fd) and **freed with the process**: `close_proc_fds()` runs on reap (`reap_zombies`/`reap_user_process`), closing anything left open so a careless/crashed process can't leak VFS handles or exhaust the table. Verified with a new `fdleak.elf` that opens `/init.elf` and exits *without* closing: the kernel logs `[reap] force-closed leaked fd(s)`, and running it repeatedly never exhausts fds (each run gets its own fresh table, PID 5 then 6). init.elf's normal open/read/write/close I/O still works. Deferred (documented): per-process **CWD** — `current_dir` stays global because relative paths are shell-only (user procs use absolute paths), there's one shell, and it feeds the core `resolve_path`; low value, not worth the risk yet. |
| v5.7.15 | **Idle desktop yields CPU + `nice`/`renice`.** (1) The compositor no longer busy-polls: at the top of its loop, when nothing is happening — no key queued (`kbd_head==kbd_tail`), no mouse button, the pointer unmoved, no drag/resize/menu active — it `sleep(5)`s instead of spinning. So an idle desktop hands the CPU to background jobs (or lets it idle) rather than burning its weight-4 slice; any activity skips the sleep, so interactivity is unchanged (≤5 ms wakeup). Measured: a background `spin.elf` (10 heartbeats) ran at ~full CPU with the desktop idle, vs the ~⅕ it'd get from the weight-4 compositor busy-polling. (2) **`nice <weight> <file>`** spawns a background job at a chosen scheduler weight; **`renice <pid> <weight>`** changes a running process's weight (1..64) — exposing the v5.7.12 weighted round-robin from the shell. Verified: `nice 8 /spin.elf` → PID 6 weight 8, `renice 6 2` → weight 2, GUI responsive throughout, 0 panics. |
| v5.7.16 | **File manager: real drag-and-drop into folders.** The FM already had copy/paste (ctx-menu + Ctrl-C/X/V), a context menu, search, rename, and new file/folder — but its **drag-and-drop was a no-op**: on drop it always targeted the *current* directory (`dst = cwd/basename`), so dragging a file within a folder computed `dst == src` and did nothing. Fixed: a new `fileman_row_at()` maps the drop point to the row under the cursor, so dropping a file **onto a folder row moves/copies it into that folder** (`dst = targetfolder/basename`). Plain drag = **move** (`vfs_rename`), Ctrl-drag = **copy** (`vfs_cp`) — the drag ghost shows MOVE/COPY live. Also: pasting a copied file into the same directory now makes a unique `<name>_copy` instead of failing on the name collision. Verified: FM opens and lists the live FS (incl. a shell-created `/box`); the drop backend (`vfs_cp`/`vfs_rename` into a subfolder) confirmed via the shell's `cp /f1 /box/…` + `mv /f1 /box/…`; 0 panics. |
| v5.7.17 | **Networking: loopback + ICMP echo replies + a real `ping`.** Three gaps closed so NyxOS behaves like a host on the network. (1) **Loopback delivery** — packets addressed to one of our own IPs (`127.0.0.0/8` or a NIC's own address) no longer try to ARP or hit the wire; `ip_send` copies the finished IP packet into a small ring that `ip_loopback_poll()` (drained by `kernel_poll_net`) re-injects into `ip_handle_packet`, so `ping 127.0.0.1` works entirely in-guest. A handler may enqueue a reply mid-drain (echo request→reply); the same pass picks it up, so a loopback ping resolves in one poll (no deep recursion). (2) **ICMP echo responder** — `icmp_handle_packet` now answers echo *requests* (type 8) with a reply (type 0), reusing the payload/id/seq — NyxOS is pingable. (3) **Real `ping` client** — `icmp_ping` was a busy-wait with a fixed id and no output; it now uses a random ident, per-packet seq, matches replies by id+seq, times RTT off `get_ticks()`, and prints iputils-style lines + a summary (`N transmitted, M received, P% loss`; `rtt min/avg/max`). Also fixed a **byte-order bug** in the numeric-IP parser (and the `lo` literal): IPs are stored network-order (first octet = low byte) everywhere else, but `ping`'s parser built them high-byte-first — so it printed `127.0.0.1` as `1.0.0.127` and a numeric `ping <ip>` aimed at a byte-reversed address. Verified in QEMU: `ping 127.0.0.1` → 4/4 replies, `rtt min/avg/max = 0/0/0 ms`, 0% loss (exercises loopback + responder + reply-match + RTT + stats); `ifconfig` now shows `lo … IP 127.0.0.1`; DHCP (10.0.2.15) + ARP still resolve; `ping 10.0.2.2`/`8.8.8.8` send correctly but time out cleanly (100% loss, graceful, no hang) — QEMU slirp's gateway doesn't answer ICMP echo, a known emulator limitation, not a stack bug. 0 panics. |
| v5.7.18 | **TCP retransmission (RTO) + robust multi-segment receive.** TCP was fire-and-forget: `send_segment` transmitted once, a lost segment was never resent, and an incoming ACK of our data fell through the state machine unhandled. Now each connection buffers its one outstanding sequence-consuming segment (SYN/FIN/data) verbatim; **`tcp_tick()`** — run from `kernel_poll_net` (every network poll; never an IRQ, since it does ip_send/kmalloc) — resends it once the RTO elapses with **exponential backoff** (300 ms → ×2 → 2400 ms cap) and, after `TCP_MAX_RETRIES` (5), resets the connection. Incoming ACKs are now processed: a cumulative ack past the buffered segment clears it (stops the timer). A testing hook — `tcpdrop <n>` / `tcp_debug_drop()` — silently drops the next N outbound segments (kept armed for retransmit) so the RTO path is exercisable in-guest. Separately, **multi-segment receive** was made robust: `http_get`'s receive/handshake loops were rewritten from a fixed iteration count (a normal inter-segment gap tripped the old counter after one partial segment → truncated/empty bodies) to `get_ticks()`-based timeouts with Content-Length / FIN completion, so large streamed responses arrive whole; `cmd_tcptest` now waits for the handshake and sends the correct `Host:` header (and dumps raw bytes for diagnosis). Verified over real slirp TCP: a genuinely slow SYN to neverssl.com (34.223.124.45) was **recovered by two retransmits** (retry 1 @600 ms, retry 2 @1200 ms — backoff visible) → handshake established → full `HTTP/1.1 403` response (all headers + 239-byte body) received and parsed; `tcpdrop 1` + connect shows the dropped SYN retransmitted and the connection still completing; loopback `ping` unaffected; 0 panics. (Found en route: the host's ISP returns a non-HTTP "Contenido bloqueado" page for some domains — an environment artifact, not a stack bug; DNS + handshake + multi-segment receive all work.) |
| v5.7.19 | **TCP passive open (`listen`/`accept`) + in-guest loopback TCP.** TCP was client-only — an inbound SYN got an RST. Now `tcp_listen(port)` reserves a LISTEN socket; a pure inbound SYN to that port spawns a **child** connection (SYN_RCVD → SYN-ACK), the client's ACK promotes it to ESTABLISHED, and `tcp_accept(listen_id)` hands out the established child while the listener keeps listening. `tcp_connect` is now loopback-aware — a `127.0.0.0/8` destination sources from `lo` and needs no NIC. Combined with the v5.7.17 loopback ring, the whole TCP path — 3-way handshake, bidirectional data, cumulative ACKs, and v5.7.18 retransmission — is now testable **entirely in-guest** with zero external dependency. New `tcploop [drop]` self-test does exactly that: `tcp_listen` + `tcp_connect(127.0.0.1)`, drives the handshake, then sends a message each way and checks the bytes. Also hardened the no-connection path to never answer an RST with an RST (over loopback that would ping-pong forever). Verified: `tcploop` → handshake (client conn 1 ↔ server child 2), server & client each receive the exact payload → **PASS**; `tcploop 1` drops the client's 37-byte data segment (`TX dropped seq=1001 len=37`), the RTO retransmits it (`retransmit conn 1 retry 1`), the server still receives it and the echo completes → **PASS** — all over 127.0.0.1, no NIC, 0 panics. (Note: NIC-side inbound `listen` isn't fully wired yet — a child's local IP defaults to lo, so loopback is the supported server path today; wiring the received dst IP through would enable NIC servers.) |
| v5.7.20 | **NIC-side TCP listen — NyxOS serves HTTP to real clients.** v5.7.19's passive open only worked over loopback: a server child took its local IP from `local_ip`, which `tcp_handle_packet` computed as the *first* interface (always `lo`), so a child accepted on the NIC built its SYN-ACK with a 127.0.0.1 pseudo-header → wrong TCP checksum → slirp/the peer dropped it. Fix: thread the received packet's **destination IP** (the address the peer actually targeted) through `ip_handle_packet` → `tcp_handle_packet`, and source the child from it — a NIC child now checksums against the NIC IP, a loopback child against 127.0.0.1. A second bug, pinned down from a `filter-dump` pcap: a client that FINs right after its request (curl half-closes) moves the child to **CLOSE_WAIT** before the server replies, but `tcp_send`/`tcp_close` only acted in ESTABLISHED — so the response was silently dropped (`tcp_send` returned −1, no data segment on the wire). Now both accept CLOSE_WAIT (send the reply, then FIN → LAST_ACK) — the standard server half-close. New `tcpserve [port]` accepts one connection, prints the request, and returns a small HTTP/1.1 200. Verified end-to-end with QEMU `hostfwd=tcp::8080-:80`: `curl http://127.0.0.1:8080/` **from the host** → NyxOS accepts the inbound connection, reads the 78-byte `curl/8.19.0` GET, and the host receives `HTTP/1.1 200 OK` / `Content-Length: 21` / `Hello from NyxOS TCP!`. The pcap shows a clean SYN → SYN-ACK(seq=1000) → ACK → request(78) → ACK → response → FIN exchange with correct seq/ack. Loopback `tcploop` still PASS; 0 panics. |
| v5.7.21 | **Per-shell CWD + full-path `pwd`/prompt.** The current directory was a single global `current_dir` in vfs.c, shared by every Terminal window — `cd` in one moved them all. Now each Terminal owns its directory: `terminal_win_t` gained a `cwd` node (opaque `void*`), initialised to `/` on creation, swapped into the VFS (`vfs_setcwd_node`) right before `execute_command` and saved back (`vfs_getcwd_node`) after — so `cd` in one shell never disturbs another, and the mechanism is single-threaded-safe (the compositor runs one command at a time). `vfs_getcwd()` was also rewritten to build the **full absolute path** by walking each node's `parent` pointer to the root, instead of returning just the leaf name — so `pwd` and the prompt now show `/home/user`, not `user`. The Terminal prompt is rebuilt from its own CWD after every command. Verified in QEMU: a single shell walks `nyx:/$` → `cd home` → `nyx:/home$` (`pwd` → `/home`) → `ls` → `user/` → `cd user` → `nyx:/home/user$` (`pwd` → `/home/user`); relative `cd`/`ls` resolve against the shell's CWD; 0 panics. (Per-terminal independence is structural — every `terminal_create_ctx` starts at `/` and swaps its own `cwd` — but wasn't shown live because opening a 2nd GUI terminal needs the monitor mouse, which QEMU drives unreliably. User-*process* CWD stays deferred: no `chdir` syscall, ring-3 code uses absolute paths.) |
| v5.7.22 | **SMP: multi-core bringup verified + exposed (`cpus`).** The AP-bringup scaffolding — INIT-SIPI-SIPI → a real-mode trampoline that climbs through protected to long mode and calls `ap_main` — already brought secondary cores up, but silently. This makes it visible and adds per-core self-verification. Fixed a latent triple-fault first: `ap_main` read the core's APIC id from the **Local APIC MMIO** (`0xFEE00000`), but an AP runs on the plain kernel PML4 which doesn't map that device page → `#PF` with no AP IDT → triple fault (QEMU exited under `-no-reboot`, boot froze at "3 AP(s) found"); now the AP reads its id via **CPUID(1).EBX[31:24]** — an instruction, no memory access. Each AP records its self-read APIC id; the BSP is always recorded as CPU 0 so a single-core machine reports correctly too. New `cpus` command prints the topology. Verified: `qemu -smp 4` → `[SMP] 4 CPU(s) online`, and `cpus` shows CPU 0 (BSP) + APs 1–3 with **assigned APIC id == self-read APIC id** (0/0, 1/1, 2/2, 3/3) — proof each is a distinct core executing kernel C; a default single-core boot shows just the BSP; 0 panics. (APs park in `cli; hlt`; actually running threads on them — per-CPU scheduling, reschedule IPIs, TLB shootdown — is the next, much larger step.) |
| v5.7.23 | **Demand paging + copy-on-write (serviced from the #PF handler).** The page-fault handler was panic-only; now `isr_handler` calls `vm_handle_fault(cr2, err)` first. Two OS-available PTE marker bits drive it: **`PTE_DEMAND`** (bit 9) — a not-present PTE that, on first touch (`err` bit0==0), allocates a zeroed page and maps it present+writable; **`PTE_COW`** (bit 10) — a present, read-only PTE that, on a write fault (`err` bits0&1), gets a private writable copy (the shared page is left untouched). A new `pte_ptr(pml4, virt, create)` walks/creates the leaf PTE; `vm_map_demand`/`vm_map_cow`/`vm_unmap` set it up. **Enabled `CR0.WP`** — without it, ring-0 writes to read-only pages don't fault, so the COW write silently hit the shared page (the first run showed "cow faults taken: 0, original page BAD shared!"); with WP the supervisor write traps. The ELF loader is unaffected: it copies segments through the writable identity mapping (the physical address), not the read-only user VA. New `cowtest` self-test. Verified in QEMU: demand → first read returns 0 (zeroed), then writes stick (1 demand fault); COW → read sees the shared value, a write makes `0xBBBB…` while the original stays `0xAAAA…` (1 cow fault) → **PASS**; `usertest` still spawns ring-3 procs (ELF loader OK under WP); 0 panics. (Kernel-space today via the self-test; wiring it into lazy `sbrk`/`fork` is future work.) |
| v5.7.24 | **GitHub Wiki + clean up.** Live wiki at `https://github.com/kazah-png/nyx-os/wiki` with 16 comprehensive markdown pages covering all kernel subsystems: Home, Architecture, Building, Boot Process, Memory Management, Process Management, Networking Stack, GUI Subsystem, Filesystem, Syscalls, SMP, Drivers, Shell, Userspace, Security, Version History. Pages use wiki-style `[[Page Name]]` links with sidebar navigation. Removed local `wiki/` directory from repo (redundant with live GitHub Wiki). README badge updated to point to GitHub Wiki. |
| v5.7.25 | **Real `fork()` — copy-on-write process clone (the VM capstone).** A ring-3 process can now `fork()` (`SYS_FORK`, 10): the child gets a COW copy of the parent's address space and resumes at the same instruction with `fork()==0`, while the parent gets the child's pid — both then round-robin preemptively. Machinery: **(1) physical page refcounting** (`memory.c`): `alloc_page` sets refcount 1, `page_incref` shares a frame, `free_page` now decrements and only frees the frame at the last reference — so COW-shared pages survive one owner exiting. **(2) `clone_page_directory_cow`** walks the parent's user half (PML4[0..510]); each writable leaf is downgraded to **read-only + `PTE_COW`** in *both* parent and child and its refcount bumped, read-only pages (code) are shared as-is — the first writer on either side takes a private copy via the #PF handler. **(3) `vm_handle_fault` reworked**: a user fault now resolves the address space from the *running process's* `page_directory` (the scheduler tracks it by `current_idx`, not the lagging global `current_pml4`), and the COW copy is refcount-aware (sole owner → un-COW in place; else copy + drop the shared reference). **(4) `isr_common` restores the faulting CR3** (`mov r15,cr3` / `mov cr3,r15` around the handler): a recoverable ring-3 fault must `iretq` back on the process's own CR3, not the kernel CR3 the handler switches to. **(5) `do_fork`** builds the child's ring-3 resume frame from the parent's saved syscall register frame (exposed via a new `syscall_frame_ptr` in isr_stubs.asm) with `rax=0`, clones the process_t, and marks both parent and child `sched_managed`. Two bugs fixed in-flight: the page-table walker `pte_ptr` created intermediate tables **without `PAGE_USER`**, so a forked child's user pages sat behind supervisor-only intermediates and ring 3 couldn't even fetch its own code (`#PF` present+user+instr-fetch at its entry) — now set USER on intermediates (the leaf still gates access), matching `map_pml4`; and `vm_map_cow` now `page_incref`s the mapped frame so a single-alloc COW page isn't mistaken for a sole-owner and written in place. Verified in QEMU (`exec /init.elf`): **child** prints `fork()=0, getpid=6, cow_test=200 (own copy)` and **parent** prints `fork()=6 (child pid), getpid=5, cow_test=100 (unchanged)` — distinct pids and a proven COW split (same virtual address, divergent values after the write); the `cowtest` self-test still **PASS**; 0 panics. |
| v5.8.0 | **Nyx C language runtime + first `.nyx` program on NyxOS.** Nyx C is a Go/Zig-inspired typed subset of C that transpiles to C via `nyxc` and links against the `nyxrt` runtime. Adds: `user/nyxrt.h` — runtime types (`nyx_str`, `nyx_slice`, `nyx_result`), `__nyx_syscall6` ABI, and string-interpolation helpers (`__nyx_fmt_begin`/`_str`/`_i64`); `user/nyxrt.c` — runtime implementation; `user/hello.nyx` — first Nyx C program with syscall declarations and string interpolation; `user/hello_nyx.c` — transpiled C output. Build: `NYXCFLAGS` rule in `kernel/makefile`, `hello_nyx.elf` linked with crt0+nyxrt, embedded in initramfs. Verified: `exec /hello_nyx.elf` prints `"hola desde nyx c! pid=5"` in terminal. |
| v5.8.1 | **`waitpid()` — a parent reaps its children from ring 3.** Completes the fork story (v5.7.25): `SYS_WAITPID` (11) lets a ring-3 parent block until a child exits and collect its exit code (`long waitpid(int pid, int* status)`; `pid<=0` waits for any child). `do_waitpid` (process.c) finds the caller's child, and when it's a zombie copies out its exit code, frees its address space + kernel stack + `process_t`, and swap-removes it from the table (keeping `current_idx` valid). The kernel side is deliberately **non-blocking** — it returns `-2` (EAGAIN) while the child still runs and the userspace `waitpid()` wrapper spins on that, because a true sleeping wait would `sti;hlt` mid-syscall and the single shared syscall stack (`kernel_rsp`) would be clobbered by the child's own syscalls (per-process syscall stacks are the future fix; today syscalls are atomic under `SF_MASK`, so the poll is safe). To stop the compositor's `reap_zombies()` from collecting a child before its parent does, that reaper now **skips zombies whose parent is a live user process** — only orphans and kernel-thread-parented background jobs (`spawn`) are auto-reaped; a user parent's children wait for its `waitpid`. Verified in QEMU (`exec /init.elf`): after the COW fork, the child `exit(123)`s and the parent prints `waitpid(6) -> reaped pid 6, child exit code = 123`; `ps` afterwards shows no leaked zombie; `cowtest` still **PASS**; 0 panics. |
| v5.8.2 | **Per-process syscall stacks → `waitpid()` truly blocks.** The syscall path used a single shared kernel stack (`kernel_rsp`, set once at boot) plus `user_cr3`/`user_rsp` globals — fine while syscalls ran atomically (IF masked by `SF_MASK`), but a syscall that *blocks* (`sti;hlt`) can be preempted, and another process's syscall would then clobber the shared stack. Now `sched_target` points `kernel_rsp` at each process's **own** kernel stack (the same higher-half alias as its TSS RSP0), so a process parked mid-syscall keeps its frame on its own stack. With that, `waitpid()` (v5.8.1) becomes a TRUE block: `do_waitpid` parks the caller `PROC_BLOCKED` + `sti;hlt` (woken by the child's `SYS_EXIT` via `wake_waiters`) instead of the old userspace poll. Two subtleties handled: **(1)** a process blocked in ring 0 must resume on the **kernel CR3**, not its user CR3 — `-mcmodel=large` kernel code runs at low link addresses only mapped in the kernel PML4, so resuming on the user CR3 `#PF`'d on `do_waitpid`'s own code (RIP=`0x108843`, ring 0, not-present instruction fetch); a new `process_t.blocked_in_kernel` flag makes the scheduler choose the kernel CR3 for such procs (the process switches back to its user CR3 itself when the syscall returns). **(2)** the child's syscalls overwrite the shared `user_cr3`/`user_rsp` globals while we sleep, so `do_waitpid` saves them on entry and restores them before returning, so the asm syscall-return path iretq's back into the right address space. Verified (`exec /init.elf`): the parent blocks in `waitpid` while the child runs **three** `printf` syscalls (`working 1/3..3/3`) + `exit(123)` on its own stack, then wakes and collects `child exit code = 123` — the old shared stack would have corrupted the parent's parked frame. Two back-to-back `exec` runs (PIDs 5 & 7 both exit 0) + `cowtest` **PASS**; 0 panics. (Foundation for future blocking syscalls — pipe/keyboard reads.) |
| v5.8.3 | **Anonymous pipes + blocking `read()` — real cross-process IPC.** `pipe(int fds[2])` (`SYS_PIPE`, 12) returns a read end (`fds[0]`) and a write end (`fds[1]`) onto a kernel ring buffer (`pipe.c`, 16 pipes × 4 KB). A `read()` on an empty pipe **blocks** until a writer writes, or returns `0` (EOF) once all writers have closed; `write()` copies into the ring and wakes a parked reader; each end is **reference-counted** so `fork()` can share it and EOF fires only when the last writer closes. The read block reuses the v5.8.2 machinery — `blocked_in_kernel` (resume on the kernel CR3) + `user_cr3`/`user_rsp` save/restore around the `sti;hlt` — and the writer wakes the reader by marking it `PROC_RUN`. Integration is via the existing per-process fd table: a pipe fd is stored in `ufd_handle` with a high `UFD_PIPE_FLAG` bit (low bits = `pipe_id<<1 | end`), so `SYS_READ`/`WRITE`/`CLOSE` route to the pipe layer instead of the VFS; `do_fork` now inherits the parent's **pipe** fds (with a refcount bump — VFS fds still aren't, as their handles aren't refcounted), and `close_proc_fds` drops pipe refs on exit. Verified (`exec /init.elf`): after `pipe()`+`fork()`, the parent `write`s 27 bytes and the child `read`s them back — `read 27 bytes from pipe: "hello from parent via pipe!"` — then `waitpid` reaps the child; two runs, VFS file I/O + `cowtest` regressions all clean, 0 panics. (Enables shell pipelines `a | b`; today one reader per pipe, non-blocking writes.) |
| v5.8.4 | **`execve()` — replace a process image (fork→exec→wait complete).** Unlike `SYS_EXEC` (9, which *spawns* a new process), `execve(path, argv, envp)` (`SYS_EXECVE`, 13) replaces the **calling** process's image in place: same pid, same open fds, new address space. `elf_load` was refactored to split out `elf_load_image` (load an ELF into a fresh page directory — segments with W^X flags + a user stack — returning pd/entry/stack-top/brk; also plugs a pre-existing leak on the segment-alloc failure path). `do_execve` (process.c) then loads the new image, swaps `page_directory` on the current `process_t` and frees the old one (COW-refcount aware — pages still shared with a forked relative survive), and **rewrites this syscall's saved user frame** (via `syscall_frame_ptr`, the same hook `do_fork` uses): zero the GPRs, set RIP = new entry + RFLAGS = 0x202, and point `user_rsp`/`user_cr3` at the new stack and address space — so the `syscall` return path iretq's straight into the new program. crt0 hardcodes `argc=0`, so no argv frame is built yet (argv/envp are accepted but not threaded onto the new stack). On failure the caller is left intact and gets -1. Verified (`exec /init.elf`): the child `fork`s then `execve("/hello.elf", …)` — its own code is discarded, hello.elf runs in its place and prints its two-line banner + `exit(42)`, and the parent's `waitpid` collects `42`. With this, one `init.elf` run exercises the whole model — file I/O, `fork`+COW, blocking `waitpid`, `pipe`, and `execve` — plus `cowtest` **PASS**; 0 panics. (fork+execve+wait+pipe = the pieces for a real userspace shell.) |
| v5.8.5 | **`dup2()` — fd redirection (the shell-pipeline primitive).** `dup2(oldfd, newfd)` (`SYS_DUP2`, 14) makes `newfd` refer to the same pipe end as `oldfd`, closing whatever `newfd` held — so `dup2(pipefd[1], 1)` sends a process's stdout into a pipe, which is exactly how `a \| b` wires two processes together. Structural change that enables it: the per-process fd table was **re-indexed by the fd itself** (slots 0..PROC_MAX_FDS-1, previously index = fd−3, so fds 0-2 *couldn't exist* in the table); `open()`/`pipe()` still allocate from `UFD_BASE` (3) up, leaving 0-2 free unless dup2'd onto. `SYS_WRITE` now checks the fd table **first** and only falls back to the console for an *empty* fd 1/2 slot — a redirected stdout routes to the pipe, a normal one still prints; `SYS_READ` picks up a dup2'd fd 0 automatically (lookup-first already). dup2 is **pipe-fds only** for now: pipe ends are reference-counted (each duplicate fd takes a ref via `pipe_incref`; exit's `close_proc_fds` drops each), while VFS handles aren't refcounted — two fds on one handle would double-`vfs_close` on reap → -1. Verified (`exec /init.elf`): child `dup2(pipe_w, 1)` + `write(1, "stdout was redirected!")` → the bytes land in the **pipe**, parent reads them back (22 bytes); all prior demos pass in the same run despite the re-index — file I/O still gets `fd=3`, fork+`waitpid` (123), pipe (27 bytes), `execve` (42) — two exec runs + `cowtest` **PASS**, 0 panics. (With fork+execve+waitpid+pipe+dup2 complete, a real userspace shell with pipelines is now buildable.) |
| v5.8.6 | **`execve()` argv passing — programs receive real command-line arguments.** `execve(path, argv, envp)` now delivers `argv` to the new program via the standard **SysV entry-stack layout**: the kernel copies each string onto the top of the new program's stack, builds `[argc][argv[0..n-1]][NULL][envp NULL]` below them (entry RSP 16-byte aligned), and `crt0` reads `argc = [rsp]`, `argv = rsp+8` before calling `main(argc, argv)`. Because crt0 now reads the stack on EVERY launch, `elf_load_image` (and `create_user_process`'s fallback stack path) write an **empty frame** (`argc=0`, NULL argv/envp, 32 bytes) at the stack top for plain spawns — so `exec`/`spawn`/boot-init all keep working, just with `main(0, …)` as before. Plumbing: the `SYS_EXECVE` case copies the caller's argv (a NULL-terminated user array of user string pointers, max 8 × 63 chars) into kernel buffers **before** `do_execve` destroys the old address space; `do_execve(data, size, kargv, argc)` then points `user_cr3` at the new pd and builds the frame through `copy_to_user` (now exported from syscall.c — it translates via `user_cr3`, so the writes land in the fresh stack page through the identity map); frame `rdi`/`rsi` = argc/argv as a courtesy for register-based entry code. New test target `user/args.elf` prints its argv and exits with argc. Verified (`exec /init.elf`): child `execve("/args.elf", {"args","uno","dos","tres"})` → prints `argc=4` + all four strings, parent's `waitpid` gets status **4** (independent confirmation); the crt0 change is regression-tested across every program type — init.elf's full suite (file I/O, fork+`waitpid` 123, pipe 27B, execve 42, dup2 redirect) + `hello_nyx.elf` (Nyx C, same crt0: `hola desde nyx c!`) + `cowtest` **PASS**, 0 panics. (envp still ignored; a shell can now run `prog arg1 arg2`.) |
| v5.8.7 | **A userspace shell with pipelines — the process-model capstone, zero kernel changes.** New `user/sh.c` → `/sh.elf`: a ring-3 shell built purely on the syscall surface grown in v5.7.25→v5.8.6 — `fork()` + `execve(argv)` + `waitpid()` + `pipe()` + `dup2()`. It parses a command line into up to 4 stages joined by `\|`, fork+execve's each stage with its stdin/stdout wired to the neighbouring pipes via `dup2` (closing the raw fds promptly so EOF fires when the last writer exits), resolves bare names (`echo` → `/echo.elf`), and `waitpid`s every stage, reporting non-zero exit statuses. Two coreutils to pipe between: `user/echo.c` (argv → stdout) and `user/upper.c` (stdin → uppercase → stdout until EOF) — and since libc's `putchar`/`puts` write via `write(1,…)`, *any* C program's printf is pipeline-capturable. Modes: `sh -c "line"`, or a built-in demo script when run bare (interactive stdin needs kernel keyboard→fd0 routing — future work). Verified (`exec /sh.elf`): `echo hello…` prints; **`echo pipelines are working on nyxos \| upper` → `PIPELINES ARE WORKING ON NYXOS`** — a real two-process pipeline through a kernel pipe, wired entirely from ring 3; `args one two three` prints its argv and sh reports `exited with status 4`. Full `init.elf` regression (file I/O, fork+waitpid 123, pipe 27B, execve 42, dup2 redirect, argv 4) + two sh runs + `cowtest` **PASS**, 0 panics. The kernel didn't change — proof the process API is complete. |
| v5.8.8 | **Interactive stdin — a live shell you type into.** `read(0, …)` on an EMPTY fd-0 slot now reads the **keyboard** with a canonical line discipline (`stdin_read_line`, syscall.c): it drains the IRQ-fed ASCII ring via `getchar_poll()`, **echoes** each key (`putchar` mirrors to the terminal capture hook + serial), handles backspace, and returns the line when Enter arrives. Blocking while the ring is empty reuses the v5.8.2 mid-syscall discipline (`sti;hlt` a timeslice at a time — the timer keeps re-scheduling us, so no keyboard-IRQ wakeup hook is needed yet — with `blocked_in_kernel` so the scheduler resumes on the kernel CR3, and the shared `user_cr3`/`user_rsp` globals saved/restored around the loop). No contention: the compositor, the ring's usual consumer, is parked in `kwait()` while a foreground process runs. `SYS_READ` was restructured lookup-first so a `dup2`'d fd 0 (a pipeline's read end) still routes to the pipe, while a bare fd 0 falls through to stdin. `sh` (`user/sh.c`) becomes a real **REPL** (v0.2): a `sh$ ` prompt, `read(0)` a line, builtins `exit`/`demo`, else run the pipeline — `sh -c` still works. Verified live in QEMU (`exec /sh.elf`, keystrokes via the monitor): typed `demo` runs the canned script, `echo hola nyx` → `hola nyx`, `echo … | upper` still pipelines, `exit` → `sh: bye`; the full `init.elf` regression (fork+waitpid 123, pipe, execve 42, dup2, argv 4) + `cowtest` **PASS**, 0 panics. (Line editing is echo+backspace; job control / arrow-key history are future work.) |
| v5.8.9 | **Lazy `sbrk` — the demand-paged heap (last VM roadmap item).** `SYS_SBRK` no longer eagerly allocates + maps pages as the break grows — it just moves `program_break` (and can shrink it, clamped at `heap_start`). Pages materialise on **first touch**: a USER not-present fault (`err` bit2 set, bit0 clear) whose CR2 lies in the process's heap window **[`heap_start`, `program_break`)** is serviced by `vm_handle_fault` (alloc a zeroed page, `map_page_dir` P\|W\|U\|NX) — checked BEFORE the normal `pte_ptr` walk, since a brand-new heap page has no page-table entry or intermediate tables yet. New `process_t.heap_start` = the initial break, set at ELF load and `execve`, inherited across `fork`. So a big `malloc` that's only partly written costs only the pages actually used. It slots cleanly onto the v5.7.25 VM work: user faults already resolve to `get_current_process()->page_directory`, and `isr_common` already restores the faulting CR3 so the process resumes on its own address space with the new page mapped. Verified (`exec /init.elf`): `malloc(8000)` grows the break `0x16000 → 0x19000` (+12288 = 3 pages) **lazily**, and the pattern written across all 8000 bytes reads back **intact** — plus the full process suite (fork+waitpid 123, pipe, execve 42, dup2, argv 4) and `cowtest` still **PASS**, 0 panics. (Lost twice to the early working-tree resets; now landed. Foundation for a future `mmap`.) |
| v5.8.10 | **Coreutils (`cat`/`wc`/`ls`) + shell background jobs (`&`).** Three new ring-3 programs on the existing syscall surface — `user/cat.c` (files or stdin → stdout), `user/wc.c` (lines/words/bytes of stdin or files), `user/ls.c` (directory listing) — plus job control in `sh`. `ls` needed a new enumeration syscall: **`SYS_GETDENTS` (15)** runs the whole `vfs_open`/`vfs_readdir`/`vfs_close` cycle in the kernel and copies up to N fixed 68-byte `{char name[64]; u32 type}` records into a user buffer (ring 3 never holds a raw VFS handle). A lazy-`sbrk` interaction surfaced and is handled: `copy_to_user` walks the *user* page tables and can't fault a not-yet-touched heap page in, so `ls` `memset`s its `malloc`'d buffer first (every page resident) and `getdents` returns the count-so-far rather than -1 if a copy ever fails. **Background jobs:** a trailing `&` makes `sh` fork the pipeline and *not* wait — it parks the pids and prints `[bg] pid N`, then sweeps them non-blocking at each prompt via the new **`waitpid(…, WNOHANG)`** (`do_waitpid` gained an `options` arg; the `SYS_WAITPID` case threads `a3`; WNOHANG returns 0 instead of blocking when a child exists but hasn't exited). The shell reaps its own bg zombies — `reap_zombies()` only frees procs whose parent has gone — so `reap_bg()` prints `[done] pid N (status S)` as each finishes. Verified (`exec /init.elf` + `exec /sh.elf`): `ls /` lists the dirs (trailing `/`) and all 12 `.elf`s incl. `cat`/`ls`/`wc`; `cat welcome.txt | wc` → `1 4 26`; the WNOHANG probe polls 61× while a child runs then reaps status 7; in the shell `echo … | upper &` → `[bg] pid 25`, runs in the background, and both stages are reaped `[done] pid 24/25` at the next prompt. Full process suite (fork+waitpid 123, pipe, execve 42, dup2, argv 4) + `cowtest` **PASS**, 0 panics. (`welcome.txt` is 26 B — boot-time `vfs.c` writes it with two offset-less `vfs_write`s, a pre-existing quirk; `wc`'s `1 4 26` is correct for the real file.) |
| v5.8.11 | **Signals — `kill`/`signal`/handlers + Ctrl-C.** A POSIX-style signal subset in a new `kernel/signal.c`: **`SYS_KILL` (16)**, **`SYS_SIGNAL` (17)**, **`SYS_SIGRETURN` (18)**. Model: a signal is *posted* (set a bit in the target's `sig_pending`) and *delivered* at return-to-ring-3 by `signal_dispatch`, which the syscall entry stub (isr_stubs.asm) calls with the saved user frame after finalizing it. Delivery: **SIG_IGN** drops it; **SIG_DFL** terminates (exit code `128+signo`, shell convention — or ignores for SIGCHLD/SIGCONT); a **user handler** is entered by saving the interrupted context into `process_t.sig_saved`, pushing a return trampoline (`__sigreturn` in crt0.asm) onto the user stack, and rewriting the frame (RIP=handler, RDI=signo, user_rsp=adjusted) — the handler returns through the trampoline into `SYS_SIGRETURN`, which restores the saved context so the process resumes where it was interrupted. `process_t` gains `sig_pending`/`sig_mask`/`sig_active`/`sig_handlers[NSIG]`/`sig_trampoline`/`sig_saved[18]`; `fork` inherits dispositions, `execve` resets them (the old handler addresses die with the image). **Ctrl-C**: the keyboard IRQ (`ctrl_pressed` + `c`) posts SIGINT to the foreground process (`g_foreground_pid`, set by `cmd_exec`); `stdin_read_line` is now signal-aware (returns `-EINTR` when a signal is pending) and `do_kill` wakes a blocked target, so a signal actually interrupts a blocking `read(0)`. Verified (`exec /init.elf`): a SIGUSR1 handler runs (`caught signal 10`) and control **returns to main** via sigreturn; `SIG_IGN` drops SIGUSR2; `kill(child, SIGTERM)` → `waitpid` status **143** (128+15). Verified (`exec /sh.elf`, monitor `sendkey ctrl-c`): typing `hello` then Ctrl-C prints `^C` and drops to a fresh prompt — the shell installs a SIGINT handler and **survives** (then `echo ok` → `ok`). Full process suite + `cowtest` **PASS**, 0 panics. Limitations (future work): delivery only on the syscall-return path, so a pure compute loop with no syscalls won't see a signal until it makes one; `do_waitpid`/`pipe_read` aren't EINTR-aware yet; one handler at a time (single saved slot), no `sigaction`/`sigprocmask`/IRQ-path delivery. |
| v5.8.12 | **`mmap`/`munmap` — anonymous demand-zero mappings.** A new `kernel/mmap.c` + **`SYS_MMAP` (19)** / **`SYS_MUNMAP` (20)** for anonymous `MAP_PRIVATE` memory, built straight on the v5.8.9 demand-paging fault handler. `do_mmap` only records a **VMA** (`{start, end, prot}` in a new `process_t.mmap_vmas[16]`) and bumps a per-process pointer — *no pages are allocated*; the region lives in **[MMAP_BASE=4 GiB, MMAP_MAX=112 TiB)**, above the 4 GiB heap cap and far below the 128 TiB stack, so it never collides. Pages materialise on first touch: `vm_handle_fault` (paging.c) gains an mmap block (right after the lazy-heap block, before the `pte_ptr` walk — a fresh mmap page has no PTE yet) that, for a USER not-present fault inside a VMA (`vma_find`), allocs a zeroed page and **sets the PTE directly** (not via `map_page_dir`, which forces WRITABLE) so prot is honored: writable only if `PROT_WRITE`, `NX` unless `PROT_EXEC`. `munmap` frees the present pages via a new `vm_free_range` (refcount-aware `free_page`, so a COW-shared page just drops a ref) and clears any fully-covered VMA. `fork` inherits the VMAs (already-faulted pages come across COW via `clone_page_directory_cow`, and the metadata lets the child demand-fault the rest); `execve` drops them (freed with the old address space). Verified (`exec /init.elf`): `mmap(12288)` → `0x100000000`, the 3 pages read back **demand-zero**, a pattern written across all of them (each faulted in on touch) reads back **intact**, then `munmap` releases the region; full process suite + signals + `cowtest` **PASS**, 0 panics. Limitations (future): anonymous `MAP_PRIVATE` only (no file-backed mmap), no `addr` hint / `MAP_FIXED`, no partial-unmap VMA splitting, no `mprotect`. |
| v5.8.13 | **Shell polish — I/O redirection (`>` `>>` `<`) + `grep`/`head`/`tail`.** The userspace shell (`user/sh.c`, now v0.4) gains file redirection: a new `parse_redir` pulls `<` / `>` / `>>` (and their attached forms `<f`/`>f`/`>>f`) out of each pipeline stage's argv, and the child `open()`s the target and `dup2()`s it onto fd 0/1 before `execve` — file redirections override the pipe wiring for the fd they touch. This needed **`dup2` to work on VFS file fds** (it was pipe-only): `SYS_DUP2` now branches — pipe ends still *duplicate* (reference-counted, `oldfd` stays valid), but a VFS handle **moves** to `newfd` and clears `oldfd`, so the shell's `dup2(fd,1); close(fd)` leaves exactly one owner and never double-closes an unrefcounted handle. Two `open()` flags back the modes: **`O_TRUNC`** (`vfs_open` empties an existing ramdisk file — frees its data, size=0) for `>`, and **`O_APPEND`** (`SYS_OPEN` seeds the ufd byte-offset to the file size) for `>>`. Three new coreutils: **`grep`** (print stdin/file lines containing a literal PATTERN), **`head`** (`-n N` first lines, default 10), **`tail`** (`-n N` last lines via a circular line buffer). No new syscalls — pure composition on `open`/`read`/`write`/`dup2`/`getdents`. Verified (`exec /init.elf`): a child redirects `echo` into `/tmp/rd.txt` with `O_CREAT|O_TRUNC`, a second appends with `O_APPEND`, and the parent reads back **23 bytes / "first line" + "second line"** (truncate + move-dup2 + append all correct). Verified (`exec /sh.elf`, `demo`): `echo … > /tmp/sh.txt` then `>> ` → `cat` shows both lines, `cat … | wc` → `2 6 37`, **`ls / | grep elf`** filters to the 15 `.elf`s (dirs dropped), **`cat welcome.txt | head -n 1`** prints the one line; background jobs + argv still work; 0 panics. (Deferred to a follow-up: `cd`/`pwd`/`export` — they need a per-process CWD + `chdir`/`getcwd` and an env store.) |
| v5.8.14 | **Shell polish part 2 — `cd`/`pwd`/`export`/`$VAR` on a per-process CWD.** Each process now carries a **working directory** (`process_t.cwd`, default `/`): new **`SYS_CHDIR` (21)** / **`SYS_GETCWD` (22)**, and a `path_resolve` canonicalizer (syscall.c) that joins a relative path onto the caller's cwd and collapses `.`, `..`, and redundant `/`. It's applied in `SYS_OPEN` and `SYS_GETDENTS`, so a program that `open()`s `"welcome.txt"` from `/home/user` hits `/home/user/welcome.txt`. `chdir` validates the target with a new `vfs_isdir` (rejects a file); the cwd is **inherited across `fork`** and kept across `execve`. On top of that the shell (`sh.c`, v0.5) gains three in-process **builtins** — `cd [dir]` (→ `chdir`, default `/home/user`), `pwd` (→ `getcwd`), `export NAME=value` — plus **`$VAR` / `$?` expansion** from a shell-local env (execve still carries no envp, so `$VAR` lives in the shell): `expand_vars` runs on every line before parsing, and `try_builtin` routes cd/pwd/export from both the REPL and the `demo`. Verified (`exec /init.elf`): `getcwd` → `/`, `chdir("/home/user")` → `/home/user`, a **relative** `open("welcome.txt")` reads 26 bytes, `chdir("..")` → `/home` (normalization). Verified (`exec /sh.elf`, `demo`): `pwd`→`/`, `cd /home/user`+`pwd`→`/home/user`, **`cat welcome.txt`** (relative — the forked child inherits the shell's cwd) prints the file, `export NAME=NyxOS` + **`echo hello $NAME`** → `hello NyxOS`; redirection/grep/pipes/bg from v5.8.13 still work; 0 panics. (The interactive shell is now genuinely usable: pipelines, redirection, job control, cwd, and variables.) |
| v5.8.15 | **Userland file tools — `mkdir`/`rm`/`touch`/`sort`/`find` (+ a real VFS bug fix).** Two new syscalls — **`SYS_MKDIR` (23)** / **`SYS_UNLINK` (24)**, both cwd-relative via `copy_path_from_user` — expose `vfs_mkdir`/`vfs_unlink` to ring 3, and five new coreutils land: `mkdir.elf`/`rm.elf`/`touch.elf` (thin wrappers; touch = `open(O_CREAT)`+close), `sort.elf` (read-all → line split → insertion sort; static buffers, since `read()`'s `copy_to_user` can't fault untouched lazy-heap pages in), and `find.elf` (recursive `getdents` walk with per-level static entry pools, optional name-substring filter). Wiring them exposed **two pre-existing VFS bugs**: (1) `resolve_parent` (vfs.c) never advanced its `result` in the walk loop, so a path ending in an existing directory at depth ≥ 2 returned the walk's *start* node instead of the true parent — `vfs_unlink("/tmp/mkd")` (and rename) on nested dirs always failed, incl. from the kernel shell; fixed by tracking `result = dir` before each descent. (2) When an intermediate component is missing, `resolve_parent` returns the deepest existing dir, so `vfs_mkdir("/no/such/parent/dir")` silently created `/no`; guarded at the syscall (`SYS_MKDIR` requires the parent to already be a directory via `vfs_isdir`), leaving kernel-shell semantics untouched. Verified (`exec /init.elf`): `mkdir=0, create+unlink=0, reopen=-1, rmdir=0, bad-mkdir=-1` — all expected. Verified (`exec /sh.elf`, `demo`): `mkdir /tmp/proj` → `touch b.txt a.txt` → **`find /tmp`** recursively lists `/tmp/proj/` + both files → **`ls /tmp/proj \| sort`** reorders `b,a → a.txt b.txt` through a pipe → `rm` both; full regression + 0 panics. (20 user ELFs now ship in the initramfs.) |
| v5.8.16 | **Raw tty + shell line editor — arrow-key history and cursor editing (+ a real keyboard-driver bug fix).** New **`SYS_TTYMODE` (25)**: a per-process stdin discipline (`process_t.tty_raw`, reset by `execve`, not inherited by `fork`). Canonical stays the v5.8.8 kernel line read; **raw** (`stdin_read_raw`, syscall.c) returns bytes as they arrive with NO echo, blocking/EINTR like the canonical path, and delivers extended keys as ANSI escapes (**ESC [ A/B/C/D** up/down/right/left, H/F home/end — emitted atomically, sourced from `getkey_poll()`'s keycode ring). The shell (v0.6) grows a real **`readline()`**: insert/delete at a cursor, ←/→/Home/End movement, and a 16-entry ↑/↓ **history** (in-progress line stashed while browsing; dedup against the last entry) — echo is fully client-side, redrawn per keystroke as `'\r'` + prompt + line + trailing-erase + backspaces to park the cursor. To render that correctly in the GUI, `terminal_capture_putchar` (terminal_win.c) now handles **`'\b'`** (destructive on the pending line) and **`'\r'`** (rebuild) — fixing kernel-echo backspace in the GUI terminal too, cosmetically broken since v5.8.8. **Bug fix found live:** arrows never arrived — `scancode_to_ascii` (keyboard.c) masked `sc &= 0x7F` *before* its `sc == 0xE0` prefix check (`0xE0 & 0x7F = 0x60`), so the E0 prefix could never match and **extended keys never reached the keycode ring from the IRQ path at all** (pre-existing; GUI arrow handling was dead too). Fixed by checking the raw byte first — arrows now work system-wide (verified: the kernel terminal's own line editing responded to ←/backspace as well). Verified (`exec /sh.elf`): `echo first`, `echo second`, then **↑↑ + Enter recalls and reruns `echo first`** → `first`; typing `echo abcd` then **←← + backspace deletes the 'b' mid-line** → Enter runs `echo acd` → `acd`; full `init.elf` regression + 0 panics. |
| v5.8.17 | **Shell tab completion — commands and paths, zero kernel changes.** The shell's `readline()` (v0.7) gains a Tab handler (`do_complete`, `user/sh.c`) built entirely on the existing `getdents` syscall. At the **command position** (line start, or right after a `|` — pipeline-aware) it completes shell builtins (`cd`/`pwd`/`export`/`exit`/`demo`) + the bare names of `/`'s `*.elf` programs; elsewhere it completes a **filesystem path** by `getdents`-ing the token's directory (`.` → cwd) and matching the base. A unique match is inserted with a trailing `' '` (or `'/'` for a directory); several candidates extend to their **longest common prefix**, or — when already ambiguous — are printed on a fresh line and the prompt is redrawn (the client-side echo model from v5.8.16 makes the reprint trivial). Candidate/dirent buffers are `.bss` statics (getdents' `copy_to_user` can't fault the one-page user stack's lazy neighbors, and 64 dirents would overflow it anyway). Verified live (`exec /sh.elf`, monitor `sendkey tab`): `ec`+Tab → `echo ` → `echo hello` prints `hello`; `t`+Tab lists `tail  touch`; `ls /ho`+Tab → `ls /home/` lists `user/`. No kernel changes, full regression + 0 panics. |
| v5.8.18 | **File-backed `mmap` + `mprotect`.** Extends the v5.8.12 anonymous VM. **File-backed mmap**: `mmap(…, fd, …)` without `MAP_ANONYMOUS` snapshots the file into a private kernel buffer at map time (`do_mmap` `kmalloc`+`vfs_pread`; the fd → VFS handle is resolved in the `SYS_MMAP` case, which has the ufd table), stored on the VMA (`vma_t.file_buf`/`file_size`). A demand-faulted page in that VMA copies its slice from the buffer (`vm_handle_fault`, after the zero-fill) and zero-fills any tail past EOF — so reading the mapping returns file contents, still one page at a time. The buffer is VMA-owned: freed on `munmap`/`execve`, **deep-copied on `fork`** (so neither side double-frees), and released at reap via a new `mmap_free_bufs` wired into all three address-space teardown sites. **`SYS_MPROTECT` (26)**: `mprotect(addr,len,prot)` rewrites the flag bits of present pages (writable iff `PROT_WRITE`, NX unless `PROT_EXEC`) via a new `vm_protect_range` (paging.c) and updates the overlapping VMAs' `prot` so pages faulted in later use it. (v1 limits: file mappings start at file offset 0 — the syscall ABI marshals only 5 args, so mmap's `offset`/a6 isn't available; a partial mprotect updates the whole overlapping VMA's prot; ramdisk files.) Verified (`exec /init.elf`): `mmap(welcome.txt, 26)` → reading the mapping yields `Type 'help' for commands.`; an anonymous `PROT_READ` page reads back zero, then `mprotect(RO→RW)` lets a write succeed (`wrote ok=1`) — a write without the mprotect would fault a read-only page and panic, so this proves the flip took. Full process suite + anonymous mmap + `cowtest` still **PASS**, 0 panics. |
| v5.8.19 | **`/dev` special files — `null`/`zero`/`random`/`urandom`, no new syscalls.** Real VFS nodes under `/dev` whose read/write are intercepted by a new `dev_type` field on `vfs_node_t`: **`/dev/null`** EOFs on read and swallows writes, **`/dev/zero`** reads endless zero bytes, **`/dev/random`** (and its `urandom` alias) yields pseudo-random bytes from a lazily-seeded xorshift64 PRNG (`get_ticks()`). `vfs_pread` special-cases `dev_type` before the size/EOF check (device content is generated, not stored in `ino->data`); `vfs_pwrite` returns the byte count and discards. Created in `vfs_init` (each via `vfs_open(O_CREAT)` then setting `dev_type`), so they're ordinary nodes to everything else — `getdents`/`ls /dev` lists them, `open`/`read`/`write` from ring 3 just work, and `>` redirection targets them. Verified (`exec /init.elf`): `/dev/zero` → 16 bytes all-zero; `/dev/null` → read=0, write=10-discarded; `/dev/random` → 8 varied bytes. Verified (`exec /sh.elf`, `demo`): `ls /dev` → `null zero random urandom`, `echo … > /dev/null` swallowed. 0 panics. |
| v5.8.20 | **6-argument syscalls — `mmap` gets a real `offset`.** The syscall entry only marshaled 5 args (the six SysV integer registers held `no` + `a1..a5`), so `mmap`'s 6th argument (`offset`) was unreachable. `syscall_entry` (isr_stubs.asm) now passes **arg6 (user R9, saved at frame+48) as the 7th C argument on the stack** — `push qword [rsp+48]` before `call syscall_handler`, `add rsp, 8` after to restore the frame base for the RAX-slot write (alignment is free to ignore: the kernel is `-mno-sse`, so no `movaps`). `syscall_handler` grew a 7th `a6` param. `SYS_MMAP` threads `a6` to `do_mmap`, which for a file mapping now snapshots the file **from `offset`** (`vfs_pread(handle, buf, size-off, off)`) so a faulted page copies `file_buf[page_offset]` and mapping byte 0 == file[offset]. Verified (`exec /init.elf`): `mmap(welcome.txt, offset 5)` → the mapping starts at `'help' for commands.` (byte 5 of `Type 'help'…`), while offset 0 still reads the whole file — and, crucially, the **entire syscall path is unchanged for the 20+ other calls**: the full process suite (fork+waitpid 123, execve 42, signals 143, dup2, pipe, mprotect, /dev, coreutils) all still **PASS**, 0 panics. This also unblocks any future 6-arg syscall. |
| v5.8.21 | **Userspace `ps` + `kill` — process management from ring 3.** A new **`SYS_GETPROCS` (27)** snapshots the process table into fixed 48-byte records `{ u32 pid, ppid, state, cpu_time; char comm[32] }` — the process analogue of `getdents` (`getprocs(buf,max)`; validated `user_ptr_ok` buffer, skips empty/pid-0 slots, interrupts masked so the table can't reshape mid-walk). Two new coreutils consume it: **`ps.elf`** prints a formatted table (`PID PPID S TIME CMD`; right-justified numeric columns since the user `printf` has no left-justify, `state`→`R`/`S`/`Z`/`P` char, buffer is a `.bss` static so `copy_to_user` never hits an unfaulted lazy-heap page), and **`kill.elf`** is an argv front-end over the existing `SYS_KILL` — `kill [-SIG] pid...` accepting a numeric (`-9`) or named (`-KILL`/`-SIGKILL`) signal, default `SIGTERM`. No kernel signal changes — `kill` just wires argv to `kill()`. Verified (`exec /init.elf`): `getprocs` → **5 processes, self visible=1** (init/idle/elf/compositor with correct states), full process suite still **PASS**, 0 panics. Verified (`exec /sh.elf`): `ps` renders the live table; `spin &` (`[bg] pid 23`) → `kill 23` → `ps` shows pid 23 **Z (zombie)** → shell reaps it `[done] pid 23 (status 143)` — **143 = 128+SIGTERM**, the canonical "killed by signal 15" encoding, proving the whole `kill.elf`→`kill()`→deliver→terminate→reap chain. (22 user ELFs now ship.) |
| v5.8.22 | **`/proc` filesystem — live kernel state as files (no new syscalls) + real process names.** A generated pseudo-filesystem under `/proc`, built on the v5.8.19 `dev_type` pattern: two new `vfs_node_t` tags (`proc_type`/`proc_pid`) mark nodes whose content is **synthesized on read** by `proc_generate` in `vfs_pread` (nothing stored in `ino->data`). Static files created at boot — **`/proc/version`** (kernel banner), **`/proc/meminfo`** (`MemTotal`/`MemUsed`/`MemFree` from `memory_total`/`memory_used`), **`/proc/uptime`** (`tick_count` at 1000 Hz → `s.cc`), **`/proc/cpuinfo`**. The **per-process `/proc/<pid>/{status,cmdline}`** dirs are reconciled with the live process table by `proc_sync()` (add live pids, drop dead ones — preempt-guarded), called at `vfs_open`/`vfs_isdir` so `ls /proc`, `cat /proc/<pid>/status`, and `cd /proc/<pid>` all see current pids — **zero new syscalls**, it's all plain `open`/`read`/`getdents`. Also fixes the v5.8.21 wart where every exec'd process showed `comm="elf"`: a new `proc_set_comm(p, path)` sets `comm` to the path basename minus `.elf` (and records `cmdline`), applied in `do_execve` (now takes the `path`), `spawn_user_path`, and the auto-init loader. Verified (`exec /init.elf`, 0 panics): `/proc/version` → `NyxOS version 5.8.22 (x86_64)`, `/proc/meminfo` → `MemTotal: 261631 kB`, `/proc/uptime` → `2.77`; `getprocs`/`ps` now show real names (`init`/`idle`/`compositor`, not `elf`); `ls /proc` lists the static files **plus** `1/ 2/ 3/ 4/ 5/` per-pid dirs; full process suite still **PASS**. |
| v5.8.23 | **Userspace `top` — a live process monitor (+ timed input + terminal clear).** The first interactive full-screen TUI. A new **`SYS_READKEY` (28)** — `readkey(timeout_ms)` — blocks up to `timeout_ms` for one key and returns it (0 on timeout, `-EINTR` on a signal), implemented via `getkey_poll` + a wrap-safe `tick_count` deadline (`sti;hlt` between polls). This is the first **timed-input** primitive: a TUI can wait a fixed interval for a command and fall through to redraw when nothing was pressed. **`top.elf`** uses it — every ~1.5 s it clears the screen (ANSI `ESC[2J`/`ESC[H`), reads `getprocs()` + the first line of `/proc/meminfo`, and redraws the `PID PPID S TIME CMD` table, staying responsive to `q`. To make the clear work, `terminal_capture_putchar` (terminal_win.c) gained a **minimal CSI escape state machine** — it recognizes `ESC[…J` (clear the scrollback) and `ESC[…H`/`f` (home) and swallows any other CSI sequence, so escapes render as a clean screen instead of literal `[2J` garbage. (A foreground process still freezes the GUI window — the compositor is parked in `kwait` — so the live refresh streams to serial and the window shows the final frame on exit.) Verified (`exec /init.elf`): `readkey(150ms)` → `0` (timeout). Verified (`exec /top.elf`, monitor `sendkey`): **4 refresh frames** at ~1.5 s (`refresh #0…#3`), each showing 5 procs with real `comm` names (`init`/`idle`/`compositor`/`top`) + `MemTotal: 261631 kB`; `sendkey q` → `top: bye`, `exit(0)` — screenshot shows a clean cleared frame. 0 panics. (23 user ELFs now ship.) |
| v5.8.24 | **Foreground jobs no longer freeze the desktop — TUIs run live in the GUI.** Before, `exec <elf>` ran the child then blocked the compositor thread in `kwait()`, so the terminal window froze until the job exited (a running `top` was only visible on serial). Now `cmd_exec` (kernel.c) does a **non-blocking foreground wait**: a loop that recomposites the desktop (`compositor_redraw_now()`, a new public wrapper over the compositor's static `redraw_all()`) and `sleep(60)`s (~16 fps) while the child runs preemptively, breaking when it becomes a `PROC_ZOMBIE` and then reaping it with `kwait()` (immediate — already a zombie). No input contention: the compositor's own event loop is suspended (nested inside this handler), so the child drains the keyboard ring itself via its `read()`/`readkey()` syscalls; and only `reap_zombies()` runs on this thread, which is busy in the loop, so the exit code is never collected out from under us. `redraw_all()` is pure draw (no printf, no input), safe to call re-entrantly from the command handler the key dispatch invoked. Verified (`exec /top.elf`): the GUI **Terminal window now refreshes live** — screenshots 3 s apart show `refresh #8` → `#15` climbing in-window (previously frozen); `q` still quits cleanly (`top: bye`, `exit(0)`) and the prompt returns. Verified (`exec /init.elf`): the full batch suite (fork, execve→42, signals→143, getprocs, readkey→0) still **PASS** through the new wait path, 0 panics. This unblocks every future full-screen TUI (editors, monitors). |
| v5.8.25 | **`edit` — a full-screen text editor (terminal cursor addressing + Ctrl keys).** The first real TUI application. Three pieces come together: **(1) Terminal cursor addressing** — `terminal_capture_putchar` (terminal_win.c) grew a full CSI parser (collects numeric params) that recognizes `ESC[<r>;<c>H` (position), `ESC[2J` (clear), `ESC[K` (clear to EOL). The first such sequence flips the terminal into a **screen mode**: `lines[]` becomes a fixed cell grid (`out_row`/`out_col` cursor) drawn from row 0 with a block cursor, instead of the scrollback+input-line model; it reverts (`terminal_capture_reset`) when the command finishes. **(2) Ctrl keys** — the keyboard IRQ now maps Ctrl+A..Z to control bytes `0x01..0x1A` (Ctrl-C still → SIGINT) so TUIs can bind commands; and `readkey(0)` now **blocks forever** (0 = no timeout) for editors. **(3) `edit.elf`** (`user/edit.c`) — loads a file into a `.bss` line buffer, then a raw-tty `readkey` loop: printable insert, Enter split, Backspace/Del join, arrows/Home/End/PgUp/PgDn, `Ctrl-O` save, `Ctrl-X` exit; each keystroke it repaints the visible slice + a status bar (`file  Ln/n  Col  ^O Save ^X Exit`) and parks the cursor. Verified live (`exec /edit.elf`, monitor `sendkey`): typed `hello nyx`⏎`line two`, the GUI showed the text with the block cursor + status bar updating (`Ln 2/2 Col 9`); `Ctrl-O` → `Saved 2 lines, 19 bytes`; `Ctrl-X` → clean exit; **`cat /noname.txt` read back exactly `hello nyx\nline two`** — the file persisted, and the terminal reverted to scrollback. init.elf regression still **PASS**, 0 panics. (24 user ELFs now ship.) |
| v5.8.26 | **ANSI colors in the terminal (SGR) — `ls --color` + a colored editor status bar.** Extends the v5.8.25 CSI parser with **`ESC[…m` (SGR)**: `terminal_capture_putchar` now tracks a `cur_color` (a VGA attribute byte, fg = low nibble / bg = high nibble) updated by `term_apply_sgr` — `0` reset, `1` bold→bright fg, `7` reverse, `30-37`/`90-97` set fg, `40-47`/`100-107` set bg (via an ANSI→VGA map). Every written cell takes `cur_color`: screen-mode cells directly, and scrollback via a new per-char `out_color[]` buffer committed by `term_add_line_c` — so a colorized line **keeps its colors in history**. The draw now renders the **background** too (`vga_to_rgb` on the high nibble; previously bg was always black), and the block cursor inverts fg/bg. Two demos: **`ls.elf`** emits `ESC[1;34m` for directories and `ESC[1;32m` for `*.elf` (reset after each), and **`edit.elf`**'s status bar is drawn black-on-light-grey (`ESC[30;47m` + `ESC[K` to paint the whole row). Verified (`exec /ls.elf`): every `.elf` renders **bright green** in the GUI (dirs would be blue). Verified (`exec /edit.elf`): the status bar is a solid **black-on-grey** bar (classic nano look) — proving background color. init.elf regression **PASS**, 0 panics. No new syscall (still 29). |
| v5.8.27 | **Visual refresh — unified crescent-moon logo (login + `nyxfetch`) + updated GIFs.** The `nyxfetch` moon ASCII art moved into a shared **`kernel/nyx_logo.h`** (single source of truth); `nyxfetch` now references it instead of a local copy. The **login screen** gained that same moon, drawn in brand **purple** above the login box — each glyph row is rendered over the matching gradient color (`login_grad`) so it blends into the background instead of sitting on a flat panel; the "NyxOS Login" title is tinted purple to match. The README's `media/startup.gif` (login-with-moon → desktop → nyxfetch) and `media/terminal.gif` (nyxfetch → colored `ls` → `ps`) were regenerated from fresh QEMU captures on the v5.8.26 color terminal. Verified in QEMU (0 panics): the login screen shows the purple moon blended into the gradient, and `nyxfetch` renders the identical moon with cyan info labels — the brand mark is now consistent across the boot/login/shell surfaces. Display-only change (no kernel logic touched). |
| v5.8.28 | **Shared ELF libc — one copy of libc, mapped into every process.** Instead of statically bundling ~14 KB of libc into each of 21 programs, libc is now built ONCE as a **prelinked shared object** (`user/libc.so`, `ld -Ttext 0x30000000`) that the kernel loads at boot (`shared_libc_load`, new `kernel/shared_libc.c`) and maps into every user address space (`shared_libc_map`, called from `elf_load_image`). Its read-only `.text`/`.rodata` are mapped **shared** — the same physical frames in every process, RO (a new `map_page_ro` in paging.c, since `map_page_dir` forces writable), `page_incref`'d so process teardown's refcount-aware `free_page` balances and `fork`'s `clone_page_directory_cow` shares them for free; libc's one writable page (`.bss` = the malloc freelist head) is a **private per-process copy**. Programs link with **`ld --just-symbols=libc.so`** (no PLT/GOT, no dynamic linker) so their direct calls to `printf`/`malloc`/... resolve to libc's fixed addresses at 0x30000000 (within rel32 range of the program at 0x10000; the sbrk heap cap moved below it). Result: ELFs shrank ~35% (`init.elf` 26 KB→**18 KB**, `ls.elf` 15 KB→**9.9 KB**), libc ships once (10.6 KB). Verified (`exec /init.elf`, 0 panics): the full suite — `fork` (COW over the shared RO libc + private `.bss`, cow_test isolation intact), `execve`→42 (re-maps libc), signals→143, `mmap`, `getprocs`, `/proc`, `readkey` — all **PASS**; `ls.elf` malloc's + lists correctly. No new syscall (still 29). |
| v5.8.29 | **`dlopen`/`dlsym` — runtime dynamic loading.** Extends the v5.8.28 shared-library machinery from "libc, mapped into everyone at boot" to "any `.so`, loaded on demand by a process." Two syscalls: **`SYS_DLOPEN` (29)** — `dlopen(path)` loads the prelinked `.so` (once, into shared master frames) and maps it into the **calling** process's address space (`do_dlopen`, `kernel/shared_libc.c`), returning a handle; **`SYS_DLSYM` (30)** — `dlsym(handle, name)` resolves a symbol to its address by walking the library's `.symtab`/`.strtab` (new `elf64_shdr_t`/`elf64_sym_t` in elf.h). shared_libc.c was refactored into reusable `libseg_load`/`libseg_map` (shared by libc and the dynlib registry). Because libraries are prelinked at fixed VAs there is **no relocation** — dlsym returns the symbol's link-time `st_value`, the caller casts it to a function pointer and calls it. Demo: **`libdemo.so`** (prelinked at 0x31000000, self-contained `demo_gcd`/`demo_fib`/`demo_name`) + **`dltest.elf`**, which does NOT link libdemo — it loads it at runtime. Verified (`exec /dltest.elf`, 0 panics): `dlopen(/libdemo.so)`→handle 1, `dlsym` resolves all three functions, and calling them gives `demo_gcd(48,36)=12`, `demo_fib(10)=55`, `demo_name()="libdemo v1"`; init.elf regression still **PASS**. mkinitramfs now ships every `.so`. |
| v5.8.30 | **`/proc/<pid>/maps` — a process's mapped memory regions.** A new generated `/proc/<pid>/maps` node (built on the v5.8.22 `/proc` machinery — no new syscall) lists the address ranges present in a process's page tables, each with permissions and a label. A new `vm_collect_regions` (paging.c) walks the user half of the address space and coalesces runs of present pages with the same perms into `vm_region_t{start,end,writable,exec}` (a COW page counts as writable); `proc_generate` formats them `START-END rwx [label]`, labelling by VA — `[program]`, `[libc]` (the shared libc), `[dynlib]` (dlopen'd), `[mmap]`, `[stack]`. So `cat /proc/<pid>/maps` shows exactly what's mapped, including the shared libc and any dlopen'd library. **Also fixes a kernel `snprintf` bug**: `%x`/`%lx` formatted via `itoa((int)val)`, truncating 64-bit values to 32 bits (and ignoring width) — rewritten to be 64-bit-safe with zero-padding, so 48-bit VAs print correctly. Verified (`exec /init.elf`, 0 panics): `/proc/5/maps` shows `000000010000-000000012000 rwx [program]`, the three shared-libc segments at `0x30000000` (`r-x`/`r--`/`rw-`), and the stack at `7fffffffe000-7ffffffff000 rw- [stack]`; init.elf suite still **PASS**. |
| v5.8.31 | **`pmap` + `free` — userspace tools over `/proc`.** Two small coreutils that read the generated `/proc` files (pure userspace, no kernel change). **`pmap [pid]`** (`user/pmap.c`) reads `/proc/<pid>/maps` (default: itself via `getpid`), prints each region, and parses the hex ranges to report a total (`8 regions, 36 KB mapped`). **`free`** (`user/free.c`) reads `/proc/meminfo` and prints the classic `total/used/free` table. Both are ~10 KB — they link against the shared libc (`--just-symbols`), and `pmap` of itself even shows the shared libc mapped at `0x30000000`, tying the last few increments together. Verified (0 panics): `exec /free.elf` → `Mem: (kB) 261631 total / 184 used / 261447 free`; `exec /pmap.elf` → its program/libc/stack regions + the summary. |
| v5.8.32 | **`env` / `date` / `sleep` — coreutils + real environment inheritance.** Three userspace tools plus the plumbing that makes `env` meaningful. **`date`** reads the RTC through a new `SYS_TIME=31` (fills a 6-int `{sec,min,hour,mday,mon,year}` buffer — a stable, padding-free ABI over the kernel's `rtc_time_t`) and prints `YYYY-MM-DD HH:MM:SS`. **`sleep N`** blocks on a new `SYS_SLEEP=32` (`do_sleep_ms`): the caller parks `PROC_BLOCKED` with a `wake_tick` on the timer wait queue and the scheduler runs others until the deadline — the same mid-syscall discipline as `waitpid` (`blocked_in_kernel` → resume on the kernel CR3, `user_cr3`/`user_rsp` saved/restored), and a signal cuts it short with `-EINTR`. **`env`** prints the process environment — which now actually exists: **`execve` carries `envp`**. `SYS_EXECVE` copies the `envp` array in like `argv` (16 entries × 159 chars); `do_execve` lays out the SysV entry stack as `[argc][argv…][NULL][envp…][NULL]`; **crt0** derives `environ = &argv[argc+1]` and publishes it as the libc `environ` global (new `getenv` reads it); and the shell seeds `USER`/`HOME`/`SHELL`/`PATH`/`TERM` and passes `env_build()` to every `execve`, so children inherit the environment and `export` propagates. No relocation surprises — programs are `-mcmodel=small` non-PIE, so the cross-module `environ` reference resolves as an absolute 32-bit reloc via `--just-symbols` (verified in the ELF: `environ` at `0x30002000`). Verified in the GUI (0 panics): `exec /date.elf` → `2026-07-12 17:30:10` (matches the taskbar clock); in `sh`, `env` → `USER=nyx`/`HOME=/home/user`/`SHELL=/sh.elf`/`PATH=/`/`TERM=nyx`, `export FOO=bar` then `env` adds `FOO=bar`, and `sleep 2` blocks ~2 s then `exit(0)` while the desktop stays responsive. 33 syscalls. |
| v5.8.33 | **`$PATH` command resolution — the environment becomes _functional_.** Closes the loop on v5.8.32: the shell now has a real `PATH` in its environment, so `resolve()` (user/sh.c) searches it instead of hardcoding `/`. A bare command name is looked up by walking each `:`-separated directory of `$PATH` in order — trying `<dir>/<name>` then `<dir>/<name>.elf` (NyxOS executables carry the suffix) and taking the first that `open()`s — so **`$PATH` is authoritative**: a command whose directory isn't on `$PATH` is `not found` (exit 127), with no silent fallback to `/` (that fallback survives only for a totally unset `$PATH`, so the shell still works bare). A name containing `/` stays an explicit path (never searched), per POSIX. The default `PATH` is now `/bin:/usr/bin:/` (the executables live at `/`, so the search walks the two empty dirs and resolves at the last entry — exercising multi-directory lookup). Pure userspace, no kernel/syscall change (still 33). Verified in the GUI (0 panics): `echo hola` → `hola` (resolved via the `/bin`→`/usr/bin`→`/echo.elf` walk); `export PATH=/tmp` then `echo hi` → `sh: echo: not found` / exit 127 (proving `$PATH` is consulted, not bypassed); `export PATH=/` then `echo recovered` → `recovered` (resolution is live — changing `$PATH` changes what runs). |
| v5.8.34 | **Terminal scrollback — PgUp/PgDn + mouse wheel + a scrollbar.** The terminal keeps 2000 lines of history but always showed the tail; now you can navigate it. The draw path already honored `scroll_offset` (and `term_add_line` bumps it to hold the view steady when output arrives while scrolled) — this wires the input side. `terminal_win_key` (kernel/terminal_win.c) gains a `term_scroll()` helper (clamped to `[0, line_count - visible_rows]`, no-op in TUI screen mode) and handles **PgUp/PgDn** (one screenful minus a row of overlap; the keyboard already emitted `KEY_PGUP`/`KEY_PGDN`) and synthetic **`KEY_WHEEL_UP`/`KEY_WHEEL_DOWN`** (3 lines); any other key snaps back to the live tail. A **scrollbar** on the right edge shows the thumb sized to `visible/total` and positioned by scroll depth — dim at the tail, purple while scrolled. **Mouse wheel** required real driver work (kernel/mouse.c): the PS/2 mouse was a plain 3-byte device, so `mouse_enable_wheel()` does the IntelliMouse "magic knock" (sample rate 200→100→80, then read device ID; `0x03` = wheel) and the IRQ handler now reads a **4-byte packet** whose signed low-nibble Z accumulates into `mouse_z`; the compositor diffs `mouse_get_z()` each loop and injects one `KEY_WHEEL_*` per notch to the focused window (and its wheel state joins the idle-yield wake condition). Verified in QEMU (0 panics): boot logs `[MOUSE] Initialized (wheel)`; `help` fills the screen, **PgUp** scrolls to `Available commands:` at the top (thumb rises + turns purple), **PgDn** returns to the tail; injecting wheel notches via the monitor's `mouse_move dx dy dz` scrolls the view (thumb tracks) and plain moves still track the cursor correctly (4-byte parse intact). No syscall change (still 33). |
| v5.8.35 | **More coreutils — `cp` / `mv` / `uname` / `whoami` / `which` / `clear` (+ a real build-system fix).** Six small userspace tools (each links the shared libc, so ~10 KB): **`cp SRC DST`** and **`mv SRC DST`** stream a file in 4 KB `.bss`-buffered chunks (`mv` = copy then `unlink`, since ring 3 has no rename syscall); **`uname`** prints `NyxOS` (or the full `/proc/version` banner with `-a`); **`whoami`** reads `$USER` via `getenv`; **`which NAME…`** reimplements the shell's `$PATH` resolver and prints where each name would run (closing the v5.8.33 loop); **`clear`** emits `ESC[3J`. That needed one tiny terminal change (kernel/terminal_win.c): `ESC[3J` now erases the scrollback (`line_count = 0`) **without** entering full-screen mode, so `clear` composes cleanly inside the userspace shell (plain `ESC[2J` still means a TUI screen-clear). **Also fixes a latent build bug** `uname` surfaced: the makefile's `%.o: %.c` rule didn't depend on `kernel.h`, so a bare `KERNEL_VERSION` bump never recompiled `vfs.o`/`kernel.o` — `/proc/version` (and nyxfetch) had been stuck at 5.8.30 across five releases. Adding `kernel.h` as a prerequisite fixes version propagation. Verified in the shell (0 panics): `whoami`→`nyx`, `uname -a`→`NyxOS version 5.8.35 (x86_64)` (now correct), `which whoami`→`/whoami.elf`, `cp /home/user/welcome.txt /w` + `cat /w` round-trips the content, `mv /w /w2` + `cat /w2` confirms the move, `clear` wipes the screen and the shell keeps working. No syscall change (still 33). |
| v5.8.36 | **Job control — Ctrl-Z stop + `jobs`/`fg`/`bg` with transparent restart.** Real Unix job control. **Kernel:** a new `PROC_STOPPED` state (the scheduler already only runs `PROC_RUN`, so it's skipped for free); `SIGTSTP` (20, Ctrl-Z) and `SIGSTOP` **park** the process instead of terminating — `signal_dispatch` (or, for a blocking syscall, the new `signal_check_stop`) sets it `PROC_STOPPED`, wakes the parent via `wake_waiters`, and `sti/hlt`s until `SIGCONT` flips it back to `PROC_RUN` (the do_waitpid mid-syscall discipline: `blocked_in_kernel` + saved `user_cr3`/`user_rsp`). `do_waitpid` gains `WUNTRACED` — it reports a stopped child with the sentinel status `WSTOPPED|sig` (bit 16, unambiguous vs. exit codes). A new `SYS_SETFG` (33) lets the shell point terminal signals at its foreground child; keyboard **Ctrl-Z → SIGTSTP**. **The key subtlety:** a job stopped mid-syscall must *resume* its wait, not abort — so the blocking syscalls (`sleep`, stdin read/readkey) call `signal_check_stop` (park + restart) and consume the `SIGCONT` that woke them, so e.g. `sleep` keeps sleeping after `bg`. `cmd_exec` auto-resumes a Ctrl-Z'd direct-exec job (the kernel terminal has no job control). **Shell (v0.8):** a jobs table (pid/stopped/cmd); `jobs`, `fg [%n]` (SIGCONT + setfg + wait), `bg [%n]` (SIGCONT); the foreground path `setfg`s the child, waits `WUNTRACED`, and parks a Ctrl-Z'd job; it ignores SIGTSTP at the prompt. Verified in the GUI (0 panics): `sleep 30` → **Ctrl-Z** → `[1] Stopped sleep 30`, `jobs` lists it, `bg 1` → `jobs` shows `[1] Running sleep 30` (it *keeps running*, not killed) — proving stop + resume + restart. 34 syscalls. |
| v5.8.37 | **Shell globbing (`*`/`?`) + `~` expansion.** The shell now expands wildcards and home. **`~`** (at a word boundary, followed by `/`, space, or end) expands to `$HOME` in `expand_vars`, so `cd ~` and `echo ~` → `/home/user`. **Globbing** is a new pass in the forked child (user/sh.c): after `split_args`/`parse_redir`, `expand_globs` walks each argv token — an arg with `*`/`?` is replaced by the directory entries it matches (`getdents` on the token's directory, a small recursive `glob_match` where `*` = any run and `?` = one char; leading-dot names are skipped unless the pattern starts with `.`, and a no-match arg stays literal — nullglob off). Because a glob like `*.elf` can expand to many files, the kernel's **execve argument cap was raised 8 → 32** (`kargv_store[32]`, `uargv[33]` in do_execve). Pure userspace + one kernel limit bump; no new syscall (still 34). Verified in the shell (0 panics): `echo *.elf` → `args.elf cat.elf clear.elf cp.elf date.elf dltest.elf echo.elf edit.elf env.elf …` (9+ entries — the raised cap in action), `echo ~` → `/home/user`, `cd ~` + `pwd` → `/home/user`. |
| v5.8.38 | **Wallpaper app — a color-picker for the desktop background (purple by default).** The desktop background was a hardcoded blue/green gradient; now it's a **selectable base color**, defaulting to the NyxOS brand **purple (morado)**. New `kernel/wallpaper_win.c` holds an 11-color palette (`Morado`, `Azul`, `Turquesa`, `Verde`, `Lima`, `Oro`, `Naranja`, `Rojo`, `Rosa`, `Pizarra`, `Carbon`) and a picker window (a 4-column swatch grid, the current color framed white) modeled on the Settings window (`window_create` + `on_click`, content origin `win->x, win->y+TITLE_H`). `draw_background` (compositor.c) was rewritten to build a smooth vertical gradient from `wallpaper_base_color()` — brightness scaled ~45%→115% top-to-bottom. The app is launched from the desktop **right-click menu** (new 5th item "Wallpaper"); clicking a swatch sets the base color and the compositor repaints the background live. Verified in the GUI (0 panics): the desktop boots **purple**; right-click → Wallpaper opens the picker (Morado selected); clicking **Rojo** turns the whole desktop red instantly. No syscall change (still 34). |
| v5.8.39 | **Shell operators — `&&` / `\|\|` / `;` command lists.** The userspace shell (user/sh.c) grew a layer *above* pipelines: a command line is now a list of pipelines joined by `;` (sequence — always run the next), `&&` (run the next only if the last succeeded, `$?==0`) and `\|\|` (run the next only if it failed). A new `split_list` splits the line in place on the top-level operators — carefully **not** mistaking `&&` for a background `&` or `\|\|` for a pipe `\|` (only the doubled forms are list operators; a lone `&`/`\|` is left inside the segment for the existing background/pipeline splitter). A new `run_command_list` drives them: each segment is var-expanded **on its own** (so `$?` sees the *previous* segment's status), then dispatched as a builtin or a pipeline via `run_line`. Short-circuiting works because a skipped segment leaves `last_status` untouched — exactly bash's behaviour for `a && b \|\| c` chains. Two tiny built-ins were added, `true`/`false` (bash has them too), purely to set `$?` for testing. `main` and `sh -c` both route through `run_command_list`, and the `demo` script now exercises the operators. Pure userspace; **no new syscall (still 34)**. Verified in the GUI terminal via `exec /sh.elf` (0 panics, output read off the serial mirror): `true && echo …`→ runs, `false \|\| echo …`→ runs, `false && echo skipped ; echo …`→ prints only the `;` branch (no "skipped"), `false; echo $?`→ `1`; a live-typed `true && echo liveok`→ `liveok` (the raw-mode readline preserves the operator chars end-to-end). |
| v5.8.40 | **Double-buffered compositor — the desktop no longer flickers.** The GUI drew straight to the visible linear framebuffer: `fb_fill_rect`/`fb_put_pixel`/the cursor all wrote to the LFB directly, so every `redraw_all()` painted the full-screen background first, then icons, then each window one at a time — and the user *saw* that sequence, worst of all during clicks/drags (which recomposite every event) → visible flicker. Fixed with **double buffering** (kernel/fb.c): the compositor calls the new `fb_enable_backbuffer()` at startup, which allocates an off-screen buffer the size of the framebuffer and redirects `fb_addr` (the target of every `fb_*` primitive *and* the cursor code, via `fb_get_addr`) to it; each finished frame is then blitted to the hardware LFB in one `memcpy` via the new `fb_present()`. So the screen swaps atomically from one *complete* frame to the next — the half-drawn intermediate states are never visible. The back buffer is **opt-in**: it's off during bootsplash/login (which draw once, straight to the LFB, needing no present) and only turns on when the compositor starts; if the ~3 MB allocation ever fails it cleanly falls back to direct rendering. `redraw_all()` sets a `frame_dirty` flag so *any* caller (incl. menu/drag handlers that call it directly) gets published; the event loop presents once per iteration only when `frame_dirty || pointer-moved` (so an idle desktop doesn't needlessly blit); `compositor_redraw_now` (the foreground-exec repaint path) presents itself. Kernel-only, no syscall change (still 34). Verified in QEMU (0 panics): boot logs `[FB] double buffering on — back buffer 1024x768 (3072 KB)`; the desktop, all windows, taskbar and cursor composite correctly; the pointer moves and the taskbar clock tick present cleanly; opening the Start menu (the heaviest `redraw_all`: background + icons + every window + the menu overlay) paints atomically with no corruption. |
| v5.8.41 | **Graphical kernel panic — a visual stop screen instead of a silent freeze.** Before, `kernel_panic()` only `printf`'d to the VGA text buffer + serial, so in GUI mode a panic just froze the desktop with no on-screen indication. Now it renders a full-screen **NyxOS-purple stop screen**: a big `:(` face, "KERNEL PANIC", the formatted reason, the register/fault context (CR0/CR2/CR3 + the `__builtin_return_address` caller), a restart footer, and a "NyxOS" brand mark. Mechanics: `kernel_panic` now `cli`s, formats the message once into static storage via a new **`vsnprintf`** (factored out of `snprintf`), logs to serial as before (still greppable `[KERNEL PANIC]`), then calls a new static `panic_screen()`. Because the compositor's double-buffer present loop is dead by panic time, `panic_screen` calls the new **`fb_use_lfb_direct()`** (kernel/fb.c) to repoint drawing straight at the hardware LFB, then paints with `fb_fill_rect` + a new scaled font routine **`font_draw_string_scaled()`** (kernel/font.c — the font is a fixed 8x16, so each pixel becomes a scale×scale block for the big face/heading). It's best-effort: it only draws when a framebuffer is up (text-mode boots keep the serial log). The exception handler (isr.c) now passes a descriptive reason — `"<name> (#n) at RIP 0x… (ring r, err 0x…)"` — so real #PF/#GP/#UD faults show the exception name and faulting RIP, with CR2 as the fault address. Kernel-only, no syscall change (still 34). Verified in QEMU (SDL + monitor screendump): `crash` → the desktop is replaced by the purple stop screen showing `:(`, "KERNEL PANIC", "Reason: User requested crash", `CR0=0x80010011 CR2=0x0 CR3=0x1000 from=0x10611d`, and the restart footer; serial logs `[KERNEL PANIC] User requested crash`. |
| v5.8.42 | **Shell command substitution `$(cmd)` (+ two latent kernel bugs it exposed).** `$(command)` now runs `command` in a forked subshell and splices its captured stdout into the line before parsing. **Shell (user/sh.c):** a new `expand_cmdsubst()` pre-pass (run on the whole line inside `run_command_list`, before the `;`/`&&`/`\|\|` split) finds each `$(...)` (closing paren matched by depth), and `capture_command()` runs the inner text via `run_command_list` in a `fork`ed child with fd 1 `dup2`'d to a pipe, draining the pipe in the parent; the captured text is trailing-trimmed and its internal newlines/tabs become spaces (unquoted word-splitting). Nesting works because the subshell runs the inner text back through `run_command_list`. Verified: `echo working dir is $(pwd)` → `working dir is /home/user`, `echo nested: $(echo $(echo deep))` → `nested: deep`, live-typed `echo dir $(pwd)` → `dir /home/user`. **Two latent kernel bugs the feature surfaced, both fixed:** (1) the ring-3 **user stack was a single 4 KB page** — the shell's larger command-list/substitution frames overflowed it and #PF'd on the *first* command; now every user process gets **`USER_STACK_PAGES`=16 (64 KB)**, mapped growing down from `0x7FFFFFFFF000` in `elf_load_image` and the `create_user_process` fallback. (2) a process's **fds were closed only at reap, not at exit**, so `$(cmd)`'s parent blocked forever reading the subshell's pipe (the zombie still held the write end → no EOF); `SYS_EXIT` now calls `close_proc_fds()` (made non-static) so pipe write ends drop at exit and readers get EOF — this also makes pipeline EOF fire sooner (no regression). No new syscall (still 34); shell banner v0.11. Verified in the GUI terminal (0 panics), and the full `demo` — pipelines, operators, background jobs — still runs end-to-end. |
| v5.8.43 | **Shell quoting — `'...'` and `"..."`.** Quoted words can now hold characters that are otherwise special to the parser: spaces (no word-splitting), the operators `; & \| < >`, and the glob/expansion chars `* ? ~ $ ( )`. Userspace-only (user/sh.c), no kernel change (still 34 syscalls). Rather than teach every splitter about quotes, a new `preprocess_quotes()` pre-pass (run first in `run_command_list`, before `$(...)` and the operator split) strips the quote marks and rewrites each *protected* character to a **control-byte placeholder** (0x01–0x11) that none of the existing splitters/expanders look for; the real bytes are restored by `q_unprotect()` only when the final argv / redirect filename / `cd`/`export` arg is built. **Single** quotes protect everything (fully literal — `$`/`(`/`)` too, so nothing expands); **double** quotes leave `$ ( )` live so `$VAR` / `$?` / `$(...)` still expand inside them. A `$(...)` is copied verbatim by the pre-pass (its inner command keeps real spaces; the subshell re-parses it). Verified in the GUI terminal (0 panics): `echo "hello world"` → one arg `hello world`; `echo 'a; b'` → literal `a; b` (the `;` is not an operator); `echo "$HOME"` → `/home/user` (expands); the `demo` also shows `"…a; b \| c"` staying literal, `'…$HOME…'` literal, and `"…$NAME"` expanding. **Known limitation** (no quoting rewrite of the splitters can fix cheaply): the *output* of an unquoted-context `$(...)` is still word-split even inside `"..."`. ⚠️Observed once during testing: a **transient ring-3 #PF in a pipeline stage panics the kernel** (the long-standing "unhandled ring-3 fault panics instead of killing the process" limitation) — not caused by quoting (a no-op for that line) and non-deterministic (re-ran clean); a proper `#PF → SIGSEGV` for ring 3 is the real fix, tracked as future work. |
| v5.8.44 | **Ring-3 fault isolation — a crashing program no longer panics the kernel.** Before, ANY CPU exception taken from ring 3 (segfault, illegal instruction, div-by-zero, …) fell through to `kernel_panic` — one buggy user program took down the whole OS (the flaky pipeline panic seen in v5.8.43 was exactly this). Now `isr_handler` (kernel/isr.c) checks the fault's privilege level: **`(cs & 3) == 3`** (fault from ring 3) → terminate just that process with the default action of the signal it earned (`exception_signal()` maps #DE→SIGFPE, #UD→SIGILL, #MF/#XM→SIGFPE, everything else incl. #PF/#GP→SIGSEGV): set `exit_code = 128 + signo`, `close_proc_fds()`, `state = PROC_ZOMBIE`, `wake_waiters()`, then `sti` + `hlt`-forever so the scheduler takes over and never resumes the faulting instruction — the exact same teardown SYS_EXIT uses (the exception runs on the process's kernel stack via TSS RSP0, so it works identically). A fault from **ring 0** is still a genuine kernel bug → the visual `kernel_panic` (unchanged). The demand-paging fast path (`vm_handle_fault` for COW / lazy-heap / mmap) still runs first, so only *genuine* faults kill. New test program **user/fault.c** (`fault.elf`, NULL-deref) makes it reproducible. No new syscall (still 34). Verified in QEMU (0 kernel panics): running `fault` in the shell logs `[fault] pid 6 (fault): Page Fault (#14) at RIP 0x10017 err 0x6 fault-addr 0x0 -> killed (signal 11)`, the shell reaps it with `exited with status 139` (=128+11=SIGSEGV) and a subsequent `echo alive` → `alive` proves the shell (and the rest of the system) survived the child crash. This retires the "unhandled ring-3 #PF panics" known-latent bug. |
| v5.8.45 | **Pager — `less` / `more`.** A full-screen userspace pager (user/less.c → both `less.elf` and `more.elf`, same program). It reads a file (`less FILE`) or, with no arg, **stdin** (so `cmd \| less` works), buffers it, splits into lines, and shows a screenful at a time on the terminal's cursor-addressed screen mode (`ESC[2J` + `ESC[r;cH`, like edit/top) with a grey status bar (`name  ln a-b/total  pct%  [keys]`). Nav keys come from **`readkey()` — the keyboard, independent of fd 0** — which is the trick that makes paging a pipe work even though stdin is the pipe: Space/`f`/PgDn page down, `b`/PgUp page up, `j`/`k`/arrows line, `g`/`G` top/bottom, `q`/ESC quit. ANSI colors in the content pass through (e.g. `ls --color \| less` shows dirs blue, `.elf` green). No new syscall (still 34); shell banner unchanged. ⚠️**Debugging note that matters:** the first cut buffered 128 KB (~176 KB `.bss`); `ls / \| less` then **reliably** crashed while `ls / \| grep` never did — because a large program image makes `execve()` slow, which **widens the window for the pre-existing IRQ-load Heisenbug** in the forked pipe-stage child (fault appeared as `q_unprotect(-1)` in the sh child at a *consistent* RIP, then a follow-on ring-0 panic). Shrinking `less` to a 32 KB buffer (~48 KB `.bss`) narrowed the window enough that `ls / \| less` is now clean 3/3. Verified in QEMU (0 panics): `less /home/user/welcome.txt` renders the file + `~` fillers + status bar and `q` returns to the shell; `ls / \| less` pages 50 lines (Space → ln 23-44/50, colors intact) and quits cleanly, shell survives. The underlying Heisenbug is still latent (real fix = the register-corruption issue, separate work) — keeping user programs small avoids tripping it. |
| v5.8.46 | **Identity map now covers all RAM (was a fixed 64 MB) — fixes the pipeline kernel-panic root cause.** Root-caused the intermittent `cmd \| less`-class crash with QEMU `-d int`: the kernel identity-maps only the low **64 MB** (`IDENTITY_MAP_MB`), but `alloc_page()` hands out physical pages across **all** RAM (511 MB under `-m 512`), and the kernel touches every page-table / COW target page it allocates through that page's **identity (low) virtual address** (`map_pml4`, `pte_ptr`, `memset` of a fresh table). Under memory pressure — a large program's `.bss` + fork COW + a pipeline — `alloc_page()` eventually returned a page **above 64 MB**, and the kernel `#PF`'d dereferencing its unmapped identity address (the captured fault: `memset` writing to exactly `0x4000000` = 64 MB, and `map_pml4` reading a table above the map). Fix (kernel/paging.c `init_paging`): size the identity map to `memory_total` (rounded to 2 MB, one PD = 512 × 2 MB = 1 GB cap) instead of a constant 64 MB — since `init_paging` runs after `init_memory`, and `MAX_PAGES` already caps allocation at 512 MB, the map now always covers the allocatable range. Verified with `-d int`: boot logs `Identity-mapping 512 MB (RAM 511 MB)`; the worst-case repro (a deliberately 176 KB-`.bss` `less` piped from `ls`) went from **kernel-panicking 2/2 → 0 kernel panics** across heavy hammering, and normal `ls \| less` renders and quits cleanly. ⚠️A rarer **residual** remains under *extreme* back-to-back pipeline churn: a stage occasionally faults on a not-present shared-libc page and is killed (`[fault] … (less): … -> killed (signal 11)`) — but thanks to v5.8.44 that's **non-fatal** (the kernel and shell survive), and normal use is clean. Root-causing that shared-libc/execve-under-churn path is future work. |
| v5.8.48 | **Fix pipeline-corruption root cause #1 — pin the shared-libc master frames un-freeable.** The pipeline Heisenbug's main driver: a per-process shared-libc RO mapping wasn't matched by an `incref`, so teardown (`free_page_directory`) drove the master frame's refcount to 0 and `free_page` released it — then `alloc_page` re-handed the still-mapped frame out (double-allocation), corrupting page tables/data. Caught red-handed with per-page free-tracking + a pin (`free_page` reaching the release path for a shared-libc master at refcount 1). Fix (memory.c `page_pin` + `shared_libc.c libseg_load`): mark the masters un-freeable and floor their refcount at 1 — correct, since they live for the OS lifetime. Plus fail-fast guards: `map_pml4` full `PTE_ADDR_MASK` (was `& ~0xFFF`, leaving NX/reserved bits → non-canonical `#GP`) + reserved-bit panic, and a `slab_alloc` free-list validator. Measured: `ls \| less` fault rate ~1/3 → ~1/6. ⚠️A **separate** `-1` wild-writer remains under extreme fork+execve churn (`pstorm` reproducer; needs record/replay reverse-debug) — root cause #2, still open. (v5.8.47 "16 KB stack canary" was a red-herring — reverted; the canary never tripped.) |
| v5.8.49 | **Catchable CPU-exception signals — `signal(SIGSEGV/SIGFPE/SIGILL, handler)` recovers instead of dying.** A ring-3 fault no longer *always* terminates the process: `isr_common` now passes the exception frame to `isr_handler` (`mov r8, rsp`, 5th arg), which calls new `signal_deliver_fault(frame, signo)` (signal.c) — if the process installed a handler for that signal it diverts ring 3 into it via the same trampoline path as `signal_dispatch` (save the faulting context to `sig_saved`; rewrite RDI=signo / RIP=handler / user-RSP=adjusted-stack-with-trampoline; point `user_cr3` at the faulting pd so `copy_to_user` lands the trampoline), then `isr_handler` returns so `isr_common` `iretq`s into the handler on the process's own CR3. No handler (SIG_DFL) / SIG_IGN / a fault inside a handler / an unwritable user stack → default action (terminate as before). New libc **`setjmp`/`longjmp`** (naked, in the shared libc) let a handler jump out — the saved context is the *faulting instruction*, so returning normally would re-fault. No new syscall (reuses SYS_SIGNAL + SYS_SIGRETURN; still **34**). Demo `user/segv.c` → `segv.elf`: installs a SIGSEGV handler, derefs NULL, `longjmp`s back, **exits 0**. Verified in QEMU: `exec /segv.elf` → "caught signal 11 and recovered … exiting 0" (`code 0`), while `exec /fault.elf` (no handler) still `[fault] … -> killed (signal 11)` (`code 139`), 0 panics. (One-shot: `longjmp` out of a handler leaves the signal blocked — `siglongjmp`/multi-recovery is future work.) |
| v5.8.50 | **Panic-screen polish + kernel `%x` de-doubled.** (1) The kernel `printf` (screen.c) auto-prepended `0x` to `%x`/`%X`, so every format string that *also* wrote a literal `0x` produced **`0x0x…`** (panic registers, `[fault]` lines, driver logs) and it uglified hex dumps / MAC octets (`0x52:0x54`). Made `%x` **standard C** (no auto-prefix — now matches `vsnprintf` + the userspace libc, which were already standard; that's why the visual panic screen was single-`0x` but the *serial* wasn't), added an explicit `0x` to the handful of bare-`%x` address sites (`[MODULES]` / `[INITRAMFS]` / `[PAGING] CR3`), and switched MAC/hex-dump octets to `%02x`. Result: `0x0x` count **0** system-wide; `[KERNEL PANIC] … CR0=0x80010011 CR2=0x0 CR3=0x1000 from=0x1061ca`; `[RTL8139] MAC: 52:54:00:12:34:56`. (2) The graphical panic screen now draws the **shared NyxOS purple moon** (`nyx_logo.h`, tint `fb_rgb(168,138,245)` — same art as login/boot) in the top-right with a "NyxOS" label, replacing the plain corner text. Verified in QEMU (`crash` command): clean single-`0x` registers + the moon on the deep-purple ":(" stop screen. |
| v5.8.51 | **Network byte order consolidated into `htons`/`htonl`/`ntohs`/`ntohl` + an `IP4_OCTETS` display macro (hardening refactor — behavior-preserving).** The stack's per-protocol byte swaps were all *correct* (each a hard-won past fix) but open-coded inline — `((x<<8)&0xFF00)|((x>>8)&0xFF)` and uglier 32-bit `seq`/`ack` OR-chains — duplicated ~40× across ip/tcp/udp/dns/arp/icmp/ethernet/dhcp, the exact pattern that had produced every past wire bug. Added four `static inline` helpers to kernel.h (`__builtin_bswap16/32`, each compiling to a single `bswap`) and swept every protocol onto them: TCP ports/seq/ack/offset_flags/window/checksum (send + receive + RST paths), IP total_len/checksum, UDP ports/length, ICMP id/seq, DNS id/flags/qdcount/ancount (dropped the local `bswap16` macro), ARP htype/ptype/oper, Ethernet ethertype. Also added `IP4_OCTETS(ip)` — expands a network-order IPv4 into four `%d` printf args, first octet first — replacing the ~10 copies of `ip&0xFF, (ip>>8)&0xFF, …` in ifconfig/ping/dns/setip/dhcp/nyxfetch/tcptest. IP↔wire *byte serialization* (arp sender/target IP, the TCP pseudo-header) is deliberately left as explicit byte writes — it's a serialize, not a swap. Zero behavior change by construction (`htons(x)` ≡ the inline swap; the macro ≡ the same four args); builds clean under `-Wall -Wextra` (incl. `-Wformat`, which validates the macro's arg count/types). Verified in QEMU (ISO boot, headless slirp): full DHCP lease `OFFER/ACK IP=10.0.2.15 mask=255.255.255.0 gw=10.0.2.2`, `ifconfig` renders `lo 127.0.0.1` / `eth0 10.0.2.15`, ARP resolves 10.0.2.2 + 10.0.2.3 — desktop stable, no panic. (ping-to-gateway and DNS-upstream go unanswered under headless slirp — an environment limit, not the stack; the DHCP lease proves the same UDP/IP/Ethernet path works both directions.) Kernel-only, no syscall change (still 34). |
| v5.8.52 | **Userspace TCP sockets — `socket()`/`connect()`/`read()`/`write()`/`close()` wired to the real TCP stack, plus a built-in loopback echo service.** The socket layer (net.c) was a set of no-op stubs; it's now a thin layer over tcp.c. Two new syscalls — **SYS_SOCKET (34)** and **SYS_CONNECT (35)** — plus socket fds that live in the *same* per-process fd table as files/pipes, tagged `UFD_SOCK_FLAG (0x20000000)`, so `read`/`write`/`close` route to `tcp_recv`/`tcp_send`/`tcp_close` (BSD `send`/`recv` are libc aliases of `write`/`read`). `connect()` drives the 3-way handshake to ESTABLISHED and a socket `read()` blocks on `tcp_recv` until data or EOF — both busy-poll `kernel_poll_net()` inside the syscall (the dhcp/dns pattern; safe because `cmd_exec`'s foreground wait parks the compositor in `sleep()` and never polls, so the running program is the sole poller — no `kernel_poll_net` re-entrancy). fork doesn't inherit socket fds (like VFS handles — avoids a double-close), and `close_proc_fds` grew a socket branch so an exiting program's sockets are torn down. To give a userspace client something fully self-contained to talk to (loopback, no NIC/host), added a tiny always-on **inetd-style TCP echo service on port 7** (`tcp_echo_init`/`tcp_echo_poll`, driven from the net poll loop). New demo **user/sockdemo.c**: `socket → connect(127.0.0.1:7) → write → read → close`. **36 syscalls.** Verified in QEMU (ISO, headless): `exec /sockdemo.elf` → `connected (fd=3)`, `sent 29 bytes`, `got echo (29 bytes): "hello from userspace sockets!"`, `OK — echo matched`, exit 0, no panic. (Fixed mid-bring-up: `nsock_send` returned tcp_send's on-wire segment length, not the payload count, so `write() != len` tripped a false "write failed" → early exit → RST/retransmit noise; a socket write() now reports payload bytes.) Server-side `bind`/`listen`/`accept` syscalls and UDP (SOCK_DGRAM) sockets are the natural follow-up. |
| v5.8.53 | **Server-side userspace sockets — `bind()`/`listen()`/`accept()` (a userspace program can now run a TCP server).** Three new syscalls — **SYS_BIND (36)**, **SYS_LISTEN (37)**, **SYS_ACCEPT (38)** — over the v5.8.52 socket layer: `nsock_bind` records the local port, `nsock_listen` does a `tcp_listen` passive-open, and `nsock_accept` busy-polls `tcp_accept` until a client's handshake completes, then hands the connection out as a **new fd** (the listener fd stays open). **39 syscalls.** New demo **user/srvdemo.c**: `socket → bind(9000) → listen → (self-connect) → accept → read → write(echo) → client read`. Verified in QEMU (ISO, headless, 3/3 runs): `listening on port 9000 (lfd=3)` / `client connected (cfd=4)` / `accepted the client (afd=5)` / `server got "ping from the client" (20 bytes) → echoing` / `client got reply` / `OK`, exit 0, no panic. **Deliberately single-process** (client + server in one program via sequential loopback calls, no fork): a first cut used `fork()` so a parent server accept()ed while its child connect()ed, but that has **both sides busy-polling `kernel_poll_net()` concurrently**, which reliably tripped the latent IRQ-load register/CR3 Heisenbug (non-deterministic ring-0 instruction-fetch #PF in the net-poll code). Adding a `cli`/`sti` re-entrancy guard to `kernel_poll_net` made it worse — it fired the SAME crash **deterministically** at a fixed RIP right after the `sti` (a strong repro clue: the bug is a timer IRQ interrupting ring-0 kernel code — see [[nyxos-pipeline-corruption]]). So the guard was removed and the demo kept single-process; concurrent multi-process network polling stays unsupported until that Heisenbug is fixed. fork still doesn't inherit sockets; `close_proc_fds` tears a listener down like any socket. |
| v5.8.54 | **Scheduler fix — a process preempted mid-syscall now resumes on the correct CR3 (a latent crash the busy-polling socket syscalls newly exposed).** `irq_scheduler_tick`/`sched_target` (process.c) chose the resume CR3 from `!blocked_in_kernel`, but that flag is only set at *explicit* blocking points (waitpid/sleep/pipe). A userspace program busy-polling `kernel_poll_net()` **inside a syscall** (socket connect/recv/accept, v5.8.52+) never sets it, so a timer preemption while it ran the syscall's low `-mcmodel=large` code resumed it on its **user CR3** — which doesn't map that low kernel `.text` — → a ring-0 instruction-fetch `#PF`. Fix: pick the resume CR3 from the **ring of the interrupted frame** (saved CS at the irq_common frame offset 144 == index 18) instead of the `blocked_in_kernel` proxy — ring 3 → the proc's own CR3, ring 0 (any syscall, busy-poll or not) → the kernel CR3. Root-caused via a *deterministic* repro: a `cli`/`sti` re-entrancy guard in `kernel_poll_net` concentrated the pending timer IRQ to fire right after the `sti`, crashing `sockdemo` **every boot** at a fixed RIP (e.g. 0x111208); with the fix it passes. Verified: process-model self-tests (`init.elf`: fork/pipe/execve/dup2/waitpid/mmap/shell `&&`/`\|\|`/`;`) + `sockdemo`/`srvdemo` all pass, no panic. **Distinct from two still-open Heisenbugs** (see [[nyxos-pipeline-corruption]]): (a) *concurrent* multi-process net polling still garbles TCP state (socket demos stay single-process), and (b) the `pstorm` `-1` wild-writer (pipeline root cause #2, ~1/3 on pstorm 40). No syscall change (39). |
| v5.8.55 | **Userspace UDP sockets — `socket(SOCK_DGRAM)` + `sendto()`/`recvfrom()` over the existing udp.c.** Two new syscalls — **SYS_SENDTO (39)** and **SYS_RECVFROM (40)** — round out the socket API (**41 syscalls**). `nsock_create` now accepts `SOCK_DGRAM`, allocating a small per-socket datagram ring (`UDP_QUEUE` × `dgram_t`, kmalloc'd, freed on close); `bind()` records the local port; `sendto(fd, buf, len, flags, ip, port)` `udp_send`s (auto-binding an ephemeral source port on the first send if unbound) and reports payload bytes; `recvfrom(fd, buf, len, flags, *ip, *port)` busy-polls until a datagram is queued, returning it plus the sender's network-order IP and host-order port. Inbound delivery: `udp_handle_packet` now calls **`nsock_udp_deliver(dst_port, …)`** first — a datagram to a socket-bound port enqueues on that socket's ring; if no socket claims the port it falls back to the kernel listeners (dhcp/dns) unchanged. `connect()` is now TCP-only (rejects `SOCK_DGRAM`). New demo **user/udpdemo.c**: `bind(9001)` a receiver, `sendto` a datagram to 127.0.0.1:9001 from a second socket, `recvfrom` it. Verified in QEMU (ISO, headless): `sent 14 bytes … received "hello over UDP" (14 bytes) from 127.0.0.1:48000`, exit 0, no panic; the TCP demos (`sockdemo`/`srvdemo`) still pass. Single-process/loopback like the TCP demos — concurrent multi-process net polling is still the open issue (see [[nyxos-pipeline-corruption]]). |
| v5.8.56 | **`nc` (netcat) — a userspace tool that bridges stdin/stdout to a TCP or UDP socket, plus a UDP echo service to talk to.** Ties the whole socket API together in one program (`user/nc.c`): `nc <ip> <port>` (TCP client), `nc -u <ip> <port>` (UDP client), `nc -l <port>` (TCP server — bind/listen/accept). Half-duplex (send a stdin chunk, print one response) since NyxOS has blocking sockets and no `select()` — right for request/response protocols (echo, HTTP/1.0). **No new syscalls** (reuses socket/connect/bind/listen/accept/sendto/recvfrom + read/write; still 41). Also extended the built-in loopback echo service to answer **UDP as well as TCP** on port 7 (RFC 862): a `udp_echo_handler` registered as a kernel udp listener bounces each datagram back (it only fires when no userspace socket claims port 7, since `nsock_udp_deliver` runs first). Verified in QEMU via the shell pipe: `exec /sh.elf`, then `echo TCPNCOK \| nc 127.0.0.1 7` prints `TCPNCOK` and `echo UDPNCOK \| nc -u 127.0.0.1 7` prints `UDPNCOK` — nc connects/sends/receives and the echo bounces the payload back, both pipeline stages exit 0, no panic. (Works in a pipeline because only nc polls the net — the peer/echo is in-kernel, so there's no concurrent multi-proc net polling.) |
| v5.8.57 | **`sigsetjmp`/`siglongjmp` — multi-fault recovery (finishes the catchable-signals work from v5.8.49).** A handler that `longjmp`s out (instead of returning through the sigreturn trampoline) never runs `SYS_SIGRETURN`, so `sig_active`/`sig_mask` stayed set — the signal was left **blocked**, and a *second* fault of the same signal hit `if (p->sig_active) return 0` in `signal_deliver_fault` and killed the process (the "one-shot" limitation noted in `segv.c`). Fix: one new syscall **SYS_SIGPROCMASK (41)** — `do_sigprocmask(how, set, oldset)` reads/changes the blocked mask and, on unblock, clears the corresponding `sig_active` bit (we're abandoning that handler's stack frame). libc gains `sigsetjmp(buf, savesigs)` (a macro: stash the mask via sigprocmask, then `setjmp` in the *caller's* frame so the jump lands at the sigsetjmp site) and `siglongjmp(buf, val)` (restore the mask, then `longjmp`). So a siglongjmp out of a handler UNBLOCKS the signal and the same fault is catchable again. **42 syscalls.** New demo **user/sigrec.c** catches the SAME SIGSEGV three times (NULL deref → handler → siglongjmp → refault). Verified in QEMU: `exec /sigrec.elf` → `caught SIGSEGV #1/#2/#3, recovering` → `recovered from 3 SIGSEGVs -- multi-recovery works!`, exit 0; the one-shot `segv.elf` (v5.8.49) still passes, no panic. |
| v5.8.58 | **`alarm()` + SIGALRM — timer-driven signals.** One new syscall **SYS_ALARM (42)** — `alarm(seconds)` sets a per-process tick deadline (`p->alarm_tick = tick_count + seconds*1000`; 0 cancels) and returns the whole seconds left on any previous alarm. `irq_scheduler_tick` (which runs on the live 1000 Hz timer — irq0 does `tick_count++`) posts SIGALRM via `signal_raise` (which wakes a blocked process) once the deadline passes; delivery then runs on the process's next syscall return (`signal_dispatch`). With no handler installed, SIGALRM's default action terminates the process, like Unix. `alarm_tick` is a new zeroed proc field, so `fork()` correctly clears the child's alarm. **43 syscalls.** libc gains `alarm()`. New demo **user/alarmdemo.c**: install a SIGALRM handler, `alarm(5)` then `alarm(1)` (confirming the ~5s remaining return), sleep, catch the alarm. Verified in QEMU: `exec /alarmdemo.elf` → `previous alarm had ~5s left` / `SIGALRM delivered!` / `alarm()/SIGALRM works!`, exit 0, no panic. |
| v5.8.59 | **Persistent filesystem — verified end-to-end + polished** (the writable ext2 stack already existed; this makes it non-destructive, turnkey, and observable). NyxOS already had the full persistent stack — `ata_write_sectors`, ext2 `write_file`/`create_file`/`mkdir`/`unlink`/`alloc_block`/`alloc_inode` + superblock/BGD sync, and VFS mount integration (`mount_backed` nodes flushed to disk) that **auto-mounts a raw ext2 disk (drive 0) at `/mnt` on boot**. Verified across a reboot: `write /mnt/f`, `mkdir /mnt/d`, `rm` all persist — a fresh boot on the same disk still shows the files. Three fixes/additions: **(1)** the boot used to **write `/mnt/os-test.txt` to the user's disk on every boot** (a leftover bring-up self-test) — removed; the mount is now non-destructive and prints free space. **(2)** New **`df`** command — total/used/free 1K-blocks + inodes from the ext2 superblock. **(3)** New **`make disk`** target (mkfs.ext2 → `ext2-test.img`, classic 1024-byte blocks / 128-byte inodes) so persistence is turnkey — boot `qemu … -hda ext2-test.img`; `make clean` never touches it. No syscall change (still 43). Verified in QEMU (`-hda` a fresh disk): mounts `/mnt` writable with no stray test file, `df` shows `16384 1K-blocks, 3% used`, and `write /mnt/persist.txt` survives a reboot (`cat` → `REBOOT_SURVIVOR`). |
| v5.8.60 | **`poll()` — I/O multiplexing over sockets, pipes, and stdin (the full-duplex / multi-fd primitive).** One new syscall **SYS_POLL (43)** — `poll(struct pollfd* fds, int nfds, int timeout_ms)` waits until any listed fd is ready (POLLIN readable / POLLOUT writable), the timeout elapses (0 = non-blocking, <0 = forever), or a signal fires (-EINTR); returns the count of ready fds. Readiness is checked per fd type via new helpers: `nsock_readable` (net.c — TCP `recv_len>0`/closed via `tcp_recv_ready`, UDP datagram queued), `pipe_readable` (pipe.c — buffered data or all-writers-closed EOF), `keyboard_has_input` (keyboard.c — a stdin key/serial byte waiting), and VFS files are always ready. **44 syscalls.** libc gets `poll()` + `struct pollfd` + `POLLIN`/`POLLOUT`. New demo **user/polldemo.c**: poll a pipe + a TCP echo socket together, drain each as it becomes readable, then a poll of the idle socket times out. Verified in QEMU: `pipe readable → "from the pipe"`, `socket readable → "from the socket"`, `poll → 0 (timeout)`, `OK`, exit 0, no panic. (Fixed mid-bring-up: the poll wait must `sti` so the 1000 Hz timer advances `tick_count` — syscalls run with interrupts masked, so the timeout deadline would never pass otherwise; safe now that v5.8.54 handles mid-syscall preemption.) |
| v5.8.61 | **`nc` is now full-duplex (built on v5.8.60's `poll()`).** The TCP client and `-l` server bridge is rewritten from half-duplex (send a stdin chunk, read one response) to a **`poll()` loop over stdin (fd 0) + the socket**, forwarding each direction as data arrives — so nc can send and receive *simultaneously* (interactive chat, or a server pushing data unprompted), not just request/response. Once stdin hits EOF it drains the socket for a 500 ms grace period then exits (the loopback echo never closes; a real peer's FIN ends the socket read). **No new syscalls** (still 44); userspace-only change (`user/nc.c`, reusing `poll`/`read`/`write`). The UDP path is unchanged. Verified in QEMU via the shell: `exec /sh.elf`, then `echo TCPNCOK \| nc 127.0.0.1 7` prints `TCPNCOK` and `echo UDPNCOK \| nc -u 127.0.0.1 7` prints `UDPNCOK` — the round-trip still works through the new poll-driven bridge, both pipeline stages exit 0, no panic. |
| v5.8.62 | **The GUI file manager can now browse (and edit) the persistent `/mnt` ext2 filesystem.** The file manager already used the fd-based VFS (`vfs_open`/`vfs_readdir`/`vfs_read`, which transparently route mount paths to ext2), but three VFS gaps kept `/mnt` out of reach: (1) `/mnt` was **only a mount-table entry, never a ramdisk node**, so a listing of `/` (which walks a directory's children) never showed it — and the file manager has no address bar to type a path; (2) `vfs_rename` and (3) `vfs_cp`'s source read walked **only the ramdisk tree**, so rename / copy silently failed on mount-backed files. Fixes, all in `kernel/vfs.c`: `vfs_mount` now creates a ramdisk **stub directory node** for the mount point (`ensure_mount_stub`) so `/mnt` appears when browsing `/` — `vfs_open("/mnt")` still resolves to ext2 because `vfs_find_mount` takes precedence, so the stub only supplies the parent's entry and is never itself opened; `vfs_rename` is mount-aware (if either endpoint is on a mounted FS it does copy-then-`unlink`, for files — a directory on a mount is left intact, since that needs a recursive copy, and `vfs_cp` fails cleanly on it); `vfs_cp` loads the source and writes the destination **generically**, so copy works in all four ramdisk↔`/mnt` directions (a directory source is refused via a `readdir` probe). **No new syscalls** (still 44); kernel-only, and the file-manager GUI code is unchanged. Verified in QEMU two ways: (a) over the shell, whose `ls`/`cp`/`mv` call the very same `vfs_*` — `ls /` now lists `mnt/`, `ls /mnt` shows the ext2 root, and ram→mnt / mnt→mnt / mnt→ram `cp` plus `mv` (a `/mnt` rename) all succeed and read back; (b) in the GUI, opening the File Manager and pressing End+Enter navigates into `/mnt`, showing `lost+found/` + `hello.txt` + `renamed.txt` — the files the shell run had just written to the same disk image, so they survive across separate boots. |
| v5.8.63 | **The GUI file manager opens files in the Text Editor (cross-app integration).** Double-clicking a file — or selecting it and pressing Enter — in the File Manager now opens it in a **Text Editor** window pre-loaded with its contents, including files on the persistent `/mnt` ext2 mount (building on v5.8.62). Wiring, no new syscalls (still 44), GUI-only: (1) new `compositor_open_editor(const char* path)` in `compositor.c` factors out the editor-window creation that the start-menu / desktop "Text Editor" action used (that action now just calls it with `path = NULL` for a blank document); given a path it calls (2) the new public `editor_load_file(ed, path)` in `editor_win.c` — a thin wrapper over the editor's existing internal loader (`editor_open`), which reads the file through the VFS (so it works for ramdisk **and** mount-backed `/mnt` files). (3) The file manager detects a **double-click** (a second click on the same row within 400 ms, tracked by new `last_click_tick`/`last_click_idx` fields) and calls `compositor_open_editor`; a single click still selects the file and previews its first bytes in the status bar, and Enter on a selected file opens it the same way. The new editor window is created focused, so keyboard input flows to it immediately. Verified in QEMU (screenshot): opened the File Manager, End+Enter into `/mnt`, then **double-clicked `hello.txt`** → a Text Editor window opened titled `/mnt/hello.txt`, showing its content `Type 'help' for commands.` with the status line `Opened: /mnt/hello.txt (26 bytes)`. |
| v5.8.64 | **Persistent user accounts on the ext2 disk — `useradd` survives a reboot; the misleading "EXT2 not available" fallback is fixed.** The persistence machinery already existed — PBKDF2-HMAC-SHA256 password hashes in an XOR-obfuscated `/etc/passwd`, with `auth_add_user`/`auth_verify` reading/writing it via raw ext2 — but it never engaged: `auth_setup`'s guard `if (block_size==0 || !ext2_resolve(AUTH_PATH))` conflated "no disk" with "no passwd file yet", so on a fresh ext2 disk it fell back to 3 hardcoded **in-memory** users and the "create default `/etc/passwd`" code right below it was **unreachable dead code** — nothing was ever persisted (and it wrongly printed "EXT2 not available" when ext2 was in fact mounted). Fixes (`kernel/auth.c`): the guard is split into three cases — no disk → in-memory fallback; disk **with** an existing passwd → use it; disk **without** one → create the default `nyx` account **and write it to disk** (now reachable). `write_passwd` also `ext2_mkdir("/etc")` first, since a freshly `mkfs`'d disk has only `lost+found` and `ext2_create_file` needs the parent dir to exist. `auth_add_user` gained a duplicate-username guard, and a new `auth_list_users` backs a new **`users`** shell command; **`useradd <user> <pass>`** persists an account via `auth_add_user`. **No new syscalls** (still 44). Verified in QEMU with a two-boot persistence test on one ext2 image: boot 1 (fresh disk) logs `Created default user 'nyx' at /etc/passwd (persistent…)`, logs in as nyx, `useradd alice alicepw`, `users` → `nyx`+`alice`; **boot 2 on the same disk** logs `Persistent user file found at /etc/passwd`, and **login as alice/alicepw succeeds** (`[LOGIN] OK.`) with `users` → `nyx`+`alice` — the account survived the reboot. |
| v5.8.65 | **Account security hardening — random per-user salts, a stronger KDF, a password policy, and login lockout.** Four fixes to the auth stack: **(1) Random per-account salt.** The salt used to be *derived deterministically* from the username via `HMAC(SALT_SECRET, username)` with `SALT_SECRET` a compile-time constant — so the same username+password hashed **identically on every NyxOS install** (precomputable / rainbow-table-able, and it revealed duplicate passwords). `gen_salt_hex` is replaced by `gen_random_salt` (`kernel/auth.c`), which mixes RDTSC (CPU cycle counter), the 1000 Hz tick, and the RTC clock through splitmix64 into a random 8-byte salt; the salt is stored per `/etc/passwd` entry (the verifier already reads it back, so the change is transparent and backward-compatible). **(2) Stronger KDF:** `PBKDF2_ITERATIONS` 1000 → 10000 (stored per entry, so old hashes still verify). **(3) Password policy:** `auth_add_user`/`useradd` reject an empty username or a password under `AUTH_MIN_PASS` (4) characters. **(4) Login lockout + throttle:** `login_screen` (`kernel/login.c`) used to `return 0` (→ reboot) on the *first* wrong password; it now **retries in a loop** with a delay that grows after each failure and **locks out (reboots) after 3 attempts** — real brute-force friction with a usable retry UX. **No new syscalls** (still 44). Verified in QEMU: (a) **random salt** — two *fresh* disks each auto-create `nyx`/`nyx`, yet their `/etc/passwd` entries differ (`nyx:7ea70b…:10000:1d4ab9…` vs `nyx:dc91ca…:10000:ebd84c…`) — same credentials, different salt+hash (deterministic salting would have made them identical), both at 10000 iterations; (b) **functional** — a two-boot run: `useradd x ab` is rejected (`password too short (minimum 4 characters)`), `useradd bob bobpass` persists, and after reboot a **wrong login is retried** (`[LOGIN] FAIL.` then a fresh prompt) before `bob/bobpass` logs in `[LOGIN] OK.` — proving the retry loop, persistence, and correct random-salt read-back. |
| v5.8.66 | **Create your own account from the login screen, with a profile picture.** On first use `nyx`/`nyx` is still the built-in *guest*, but the login screen can now **register a new account**: press **TAB** to switch from Login to a "Create your account" panel — enter a username + password (subject to the v5.8.65 policy) and pick **one of four profile pictures** with keys 1–4, then Enter creates the account (persisted to `/etc/passwd`, PBKDF2-salted) and logs straight in. The four avatars are simple procedural pixel-art tiles — **Moon** (purple), **Smile** (blue), **Cool** (green visor), **Cat** (orange, with ears) — drawn by a new shared `draw_avatar(x,y,size,id,selected)` in `kernel/login.c` (any size, so it also renders tiny in the taskbar). The chosen avatar id is stored as a **5th field** on the passwd entry (`user:salt:iter:hash:avatar`; `parse_entry` stops at the hash and ignores it, so old 4-field entries still verify) via `format_entry`/`auth_add_user(user,pass,avatar)`/new `auth_get_avatar(user)` (`kernel/auth.c`); `useradd <user> <pass> [avatar]` gained the optional id too. `login_screen` was reworked into a full login-vs-create panel (`draw_panel`) and now records the account that logged in in globals `g_login_user`/`g_login_avatar`; the desktop **taskbar draws that user's avatar + name** as a "current user" badge beside the clock (`kernel/compositor.c`, window buttons reserve room for it). **No new syscalls** (still 44). Verified in QEMU (screenshots): the create panel shows the 4 avatars with the picked one ringed; creating `maya` (avatar "Cool") logs in and the taskbar shows `maya` + the green badge; after a reboot, logging back in as `maya` shows the **same** avatar — read back from the persisted `/etc/passwd`. |
| v5.8.67 | **Login fixes — a failed login no longer closes the VM, and the create-account panel no longer overflows.** Two bugs reported against v5.8.66: **(1) QEMU closed when creating an account.** Root cause: the v5.8.65 login lockout did `return 0` after 3 failed attempts, and the boot caller reacts to that by **rebooting** — which under QEMU's `-no-reboot` (used by `run.ps1`) just *exits the VM*. A user who typed new credentials while still in **login** mode (TAB not pressed, or pressed twice) would fail 3 times and the window would vanish. Fixed in `kernel/login.c`: the lockout now shows "locked briefly", waits out a **cooldown, resets the counter, and keeps the login screen up** instead of ever returning 0 — brute-force is still throttled, but the VM never closes and the user can retry or press TAB to sign up. **(2) Misaligned create panel.** The login-mode hint line was ~48 chars, wider than the 380px panel, so it ran off the right edge and left a stray `nt.` fragment when toggling to create mode; it's now a shorter, centered string that fits, and every label/message in the panel was checked to stay within bounds. **No new syscalls** (still 44). Verified in QEMU headless (no disk, like `run.ps1`): 3 wrong logins now log `[LOGIN] LOCKED (cooldown).` (not "rebooting"), **QEMU stays alive** through the cooldown, and creating an account afterward still logs in to the desktop; the create panel screenshot is clean with no overflow artifact. |
| v5.8.68 | **Login create-panel crash fix — repaint only the active field per keystroke, not the whole panel.** A user reported QEMU closing *while typing* in the "Create your account" panel (real SDL display + keyboard; not reproducible headless even with rapid/Spanish-layout/mouse input). Root cause: the v5.8.66 login rewrite repainted the **entire panel** — box, all four avatars (`draw_avatar` ×4), every label and both fields — **on every keystroke**, straight to the linear framebuffer with no double-buffering. That ~15× heavier per-keystroke redraw hammered the LFB and destabilised the login on real hardware (a diagnostic build's extra per-key `serial_puts` masked it by adding delay — the tell that it was timing-sensitive). Fix (`kernel/login.c`): a new `draw_active_field()` repaints just the field being typed into (as the pre-rewrite login did); `draw_panel()` full repaints are kept only for the events that actually change the rest of the panel — mode toggle (TAB), field advance (Enter), and avatar selection (1-4). **No new syscalls** (still 44). Verified headless that typing shows the text in the field, the avatars stay drawn, and account creation still logs in to the desktop; the crash itself is environment-specific so final confirmation is on the reporter's machine. |
| v5.8.69 | **Taskbar user menu — click your badge to change your profile picture or log out (back to the login, no reboot).** The logged-in user's avatar+name badge (added v5.8.66) is now clickable: it opens a small popup (`draw_user_menu`/`badge_hit`/`user_menu_*` in `kernel/compositor.c`) headed by the current avatar+name with two items. **Change picture** cycles to the next of the 4 avatars, updates the badge live, and **persists** it via new `auth_set_avatar(user, avatar)` (`kernel/auth.c`) — which rewrites just the 5th (avatar) field of that user's `/etc/passwd` entry, keeping the salt+hash (or updates the in-memory fallback with no disk). **Log out** sets a new exported flag `compositor_logout_requested` + `quit`, so `compositor_run()` returns; the boot flow (`kernel/kernel.c`) is now a **login→desktop loop** instead of a one-shot: on logout it re-shows the login screen without rebooting (`fb_use_lfb_direct()` first so the login draws to the LFB; `compositor_init()` now frees the previous session's windows so the desktop starts clean), and only a real Shutdown leaves the loop to reboot. **No new syscalls** (still 44). Verified in QEMU (no disk, screenshots): logged in as nyx, clicked the badge → menu with the nyx header + "Change picture"/"Log out"; Change picture flipped the badge from the Moon to the Smile avatar; Log out returned to the NyxOS Login screen (QEMU stays alive — not a reboot) and logging back in reached a fresh desktop. |
| v5.8.70 | **Per-user home directories — the Terminal and File Manager start in `/home/<user>`.** On a successful login (or account creation), `login_screen` now calls `setup_user_home()` (`kernel/login.c`): it `vfs_mkdir`s `/home` and `/home/<user>` (both idempotent) and records the path in a new global `g_login_home`. New Terminal windows start there — `terminal_create_ctx` uses the new `login_home_node()` (which resolves `g_login_home` to its VFS node via the new public `vfs_path_node()` in `kernel/vfs.c`, or falls back to root) instead of `vfs_root_node()`, so the prompt reads `nyx:/home/nyx$`. The File Manager opens there too — `fileman_create_ctx` seeds its `cwd` from `g_login_home`. Because `g_login_home` is refreshed on every login, this composes with v5.8.69 logout: log out and back in as a different user and the new windows open in *that* user's home. **No new syscalls** (still 44). Verified in QEMU (no disk, screenshots): logged in as nyx → the desktop Terminal prompt is `nyx:/home/nyx$` and opening the File Manager shows the path bar `/home/nyx`. |
| v5.8.71 | **Starter content for a new home directory.** The `/home/<user>` created at login (v5.8.70) is no longer empty: the first login that creates it (`setup_user_home` now checks `vfs_mkdir`'s return — 0 means freshly created — and calls a new `populate_home()` in `kernel/login.c`) seeds it with a `Documents/` and `Downloads/` folder and a `welcome.txt` note (greeting the user, pointing out double-click-to-edit and the taskbar user badge). It only runs on creation, so a later login (e.g. logout→relogin) never clobbers files the user changed. **No new syscalls** (still 44); `kernel/login.c` only. Verified in QEMU (no disk, screenshot): logging in as nyx and opening the File Manager (which starts at `/home/nyx`) shows "3 entries" — `Documents/`, `Downloads/`, `welcome.txt`. |
| v5.8.72 | **Minesweeper — a GUI game (new app) + a right-click routing fix.** A classic 9×9 / 10-mine Minesweeper as a compositor window app (`kernel/minesweeper_win.{c,h}`), launchable from the Start menu ("Minesweeper", item 12) and a new "Mines" desktop icon (action 12). Left-click uncovers a cell — mines are placed **lazily on the first click**, keeping the opening move *and its 8 neighbours* clear so the first reveal always flood-fills a comfortable opening (iterative stack-based flood, never recursion-deep); right-click plants/removes a flag; left-clicking an already-uncovered number **chords** (uncovers its hidden neighbours once the matching number of flags surround it). The header has a red-LED mines-left counter, a clickable **face button** that starts a new game (also `r`/`n`), and a live seconds **timer** (advances off the compositor's ~1 Hz clock repaint). Classic per-number colours (1 blue, 2 green, 3 red…), drawn flag/mine glyphs, and a smiley/cool/dead face for playing/won/lost. Geometry mirrors the draw path in the click handler using `win->y + TITLE_H` (the *correct* client origin), so hit-testing lines up exactly. **Also fixed a pre-existing compositor bug:** the right-click dispatch iterated the window array in **slot order** and delivered the click to whichever window sat earliest in the array, so an overlapped right-click leaked to a **lower** window — it now sorts **topmost-first by z-order** like the left-click path (this also repairs the File Manager context menu when it overlaps another window). Wiring in `kernel/compositor.c` + `kernel/Makefile`. **No new syscalls** (still 44). Verified in QEMU (no disk, screenshots): opened from the Mines icon → fresh board (010 / smiley / 000); a centre click flood-filled an area showing blue `1` / green `2` numbers and the timer ticked to `001`; right-clicking hidden cells planted red flags and dropped the counter to `008`; the face button reset to a clean board. |
| v5.8.73 | **`wget` — a userspace HTTP client over the TCP sockets, with its own DNS-over-UDP resolver.** New `user/wget.c` (ships in the initramfs as `/wget.elf`): `wget [-O file] [-r resolver] [-p port] [-q] [-S] http://host[:port][/path]` (also a positional `host [port] [path]` form). It opens a `SOCK_STREAM` socket, `connect()`s, sends an HTTP/1.0 `GET` (with `Host:` + `Connection: close`), reads the whole response, splits headers/body at the blank line, parses the status code, and writes the body to stdout (or `-O <file>`); a status line (`wget: HTTP <code>, <n> bytes`) goes to stderr. Hostnames are resolved **entirely in userspace** — a self-contained A-record DNS query built and parsed over the existing `sendto()`/`recvfrom()` UDP calls to a resolver (default 10.0.2.3 = QEMU SLIRP's DNS; overridable with `-r`/`-p`), handling label QNAMEs and 0xC0 compression pointers in the answer; dotted-quad hosts skip DNS. **No new syscalls** (still 44) and **no kernel changes** — a pure userspace program on the existing socket API. Run it from the userspace shell (`exec /sh.elf` → `wget …`), since the kernel shell's `exec` passes only the path (no argv). Wiring: `kernel/Makefile` (USER_ELFS + build rules). Verified in QEMU with SLIRP networking (`-netdev user -device rtl8139`) against a local HTTP server + a local DNS responder reached via the SLIRP host alias 10.0.2.2 (screenshots): after `layout us` + `dhcp`, from `sh.elf` — (1) `wget http://10.0.2.2:8000/hello.txt` printed the file body and `wget: HTTP 200, 35 bytes`; (2) `wget -r 10.0.2.2 -p 5354 http://nyxhost:8000/hello.txt` logged `resolving nyxhost …`, resolved it to 10.0.2.2 via the userspace DNS query, then fetched the body. (De-risked first with the kernel's own `httpget`, which fetched the same file — proving userspace sockets egress the NIC via SLIRP.) |
| v5.8.74 | **`exec` forwards argv — userspace tools run with arguments straight from the kernel shell.** Previously the GUI-terminal shell's `exec <file>` called `spawn_user_path(path)` and passed **only the path**, so anything needing arguments (wget, grep, cat…) had to be run from the userspace shell (`exec /sh.elf` → `cmd args`). Now `exec <file> a b c` (and `spawn`) build the child's SysV argv frame and launch it directly. New `build_argv_stack(pd, stack_top, argv, argc)` (`kernel/process.c`) is the spawn-side counterpart to do_execve's frame builder: it points the global `user_cr3` at the **new** process's page directory and `copy_to_user`s the strings + the `[argc, argv…, NULL, envp NULL]` qword frame onto its fresh stack, returning the adjusted ring-3 RSP — with **interrupts masked across the copy** (unlike do_execve, which already runs inside a masked syscall, this path runs from the shell with IF=1, so a scheduler tick must not swap `user_cr3` mid-loop). New `elf_load_args()` (`kernel/elf.c`) = `elf_load_image` + `build_argv_stack` + `create_user_process(entry, rsp, pd)`; `elf_load` becomes a thin `argc==0` wrapper (byte-identical no-arg behavior). New `spawn_user_path_args(path, argv, argc)` (`kernel/kernel.c`) mirrors `spawn_user_path` (now a wrapper) and records the space-joined argv as the process cmdline; `cmd_exec` / `cmd_spawn` forward `&argv[1], argc-1`. **No new syscalls** (still 44). Verified in QEMU (screenshots): from the GUI Terminal, `exec /echo.elf NyxOS exec argv works` printed `NyxOS exec argv works`, and (after `dhcp`) `exec /wget.elf http://10.0.2.2:8000/hello.txt` fetched the file and printed `HTTP 200, 35 bytes` — both exiting code 0, with no `/sh.elf` step. |
| v5.8.75 | **The kernel shell auto-execs userspace ELFs — run tools by bare name, no `exec` prefix.** When a typed command isn't a builtin, `execute_command` (the GUI-terminal shell) now resolves it to a userspace ELF and runs it with argv instead of printing "Command not found". New `resolve_user_elf(name, out, sz)` (`kernel/kernel.c`): a name containing `/` is used verbatim, otherwise `/<name>.elf` then `/<name>` are probed in the initramfs (first that opens wins). New `run_foreground_elf(path, argv, argc)` factors the spawn + foreground-wait (repaint-while-running) + reap + exit-code logic out of `cmd_exec` (now a one-line wrapper) and is shared with the fallback. Kernel builtins keep priority — they're matched first, so `ls`/`cat`/`grep` keep their built-in behavior and only unknown names hit auto-exec. Builds directly on v5.8.74's argv forwarding. **No new syscalls** (still 44); only `kernel/kernel.c`. Verified in QEMU (screenshot): typed with no `exec`, `whoami` printed `nyx` (auto-exec of `/whoami.elf`), and `wget http://10.0.2.2:8000/hello.txt` fetched the file and printed `wget: HTTP 200, 35 bytes` — both reporting `[exec] PID N exited (code 0)`. |
| v5.8.76 | **Tab-completion now includes userspace ELFs.** The GUI-terminal shell's command-word Tab-completion (`command_complete` / `command_list_matches`) previously knew only builtins; now that a bare `wget`/`whoami` auto-execs (v5.8.75), completion also offers the base names (minus `.elf`) of programs in the initramfs root. New `enum_completions(cb, ctx)` (`kernel/kernel.c`) walks the builtins then `readdir("/")`'s `*.elf` entries, skipping any whose base name shadows a builtin (`is_builtin()`) so each name is offered once; both completion functions were refactored onto it with small ctx callbacks (list = space-joined matches, complete = single match or common prefix). **No new syscalls** (still 44); only `kernel/kernel.c`. Verified in QEMU (screenshots): typing `whoa`+Tab completed to `whoami` and ran it (printed `nyx`) — an ELF-name completion — while `dat`+Tab still completed to the `date` builtin (no regression). (Note: headless `sendkey tab` injection is itself unreliable — some runs drop the keystroke entirely — but completion is correct whenever Tab is actually delivered; a real keypress is reliable.) |
| v5.8.77 | **wget follows redirects + an `open` command = download-and-read.** Two halves of a "fetch a page and view it" loop. (1) **wget 3xx redirects** (`user/wget.c`): the fetch is now a loop over (host, port, path) that, on a 301/302/303/307/308 carrying a `Location:` header, rewrites the target and re-requests — up to 5 hops. `find_location()` scans the header block case-insensitively; the Location may be an absolute `http://host:port/path` (switches host) or root-relative `/path` (same host/port). (2) **`open <file>`** — a new kernel-shell builtin (`cmd_open` in `kernel/kernel.c`) that opens a file in the GUI Text Editor via the existing `compositor_open_editor()`; it resolves the path to absolute against the shell cwd and checks the file exists first. Safe because the kernel shell is dispatched on the compositor thread (which owns the window list) — a userspace program can't create a window, so this belongs in the kernel. **No new syscalls** (still 44). Verified in QEMU (screenshots): `wget http://10.0.2.2:8000/old` logged `redirect -> /hello.txt`, re-fetched, and printed the target body (`HTTP 200, 41 bytes`); then `wget -O /tmp/page.txt http://…/hello.txt` saved it and `open /tmp/page.txt` popped a Text Editor window showing the downloaded text (`Opened: /tmp/page.txt (41 bytes)`). Both ran as bare commands (auto-exec + the new builtin). |
| v5.8.78 | **Concurrent multi-process sockets: hardening + a repro — the "sockets stay single-process" garble no longer reproduces.** New `user/netstorm.c` forks 4 processes that each hammer the loopback echo (25 write/read rounds on their own connection); before this it failed reliably (~1 of 4 processes' `connect()` hung in `SYN_SENT` — its SYN-ACK lost under concurrent loopback handshakes). Added net-stack concurrency hardening, each part addressing a genuine hazard: **(1) re-entrancy serialization** — `kernel_poll_net()` and the `nsock_*` ops (connect/send/recv/accept/listen/close) run under `preempt_disable()`, so while one process drives the *non-reentrant* loopback-ring drain + TCP state machine, `irq_scheduler_tick` won't switch to another process mid-flight (IRQs stay on; blocking calls release preemption during their idle delay so peers still progress); **(2) atomic ephemeral-port pick** — the client source port used a non-atomic `ephemeral++`, so two concurrent connects could grab the SAME port (then the second's SYN matched the first's server conn instead of spawning its own → no SYN-ACK); now it's inside the `preempt_disable` window; **(3) connection-table capacity** — `TCP_MAX_CONNS` 8→32 and `ECHO_MAX` 8→16 (N concurrent loopback clients need 1+2N slots; 8 overflowed at 4). **No new syscalls** (still 44). Verified in QEMU: netstorm passed **38 consecutive fresh-boot runs** where it previously failed ~1/4. **Correction (see v5.8.79):** what looked like an "ultra-fragile timing Heisenbug" was actually a **conn-table capacity limit** (`TCP_MAX_CONNS` 8 < the 1+2N slots N concurrent clients need) **masked by a Makefile stale-build gap** — the `TCP_MAX_CONNS` bump in tcp.h wasn't recompiling `tcp.o`, so it silently didn't take effect, and edits that touched tcp.c only *seemed* to "fix by perturbation." Root-caused deterministically under `-icount` in v5.8.79; the capacity bump (32) is the robust fix. `netstorm` ships as the regression repro. |
| v5.8.79 | **Root-caused the v5.8.78 concurrent-socket "Heisenbug": it was a conn-table capacity limit masked by a Makefile stale-build gap — and fixed the Makefile.** Reproduced netstorm's failure **deterministically under QEMU `-icount`** (virtual time makes the timing race reproducible AND stable under observation, unlike host-time where printf/tracing "perturbed it away"), then dumped the conn table at the stuck `connect()`: all 8 `conns[]` slots were full — 1 listener + 3 fully-established client+server pairs + the 4th client's `SYN_SENT` — so the 4th client's SYN found `find_slot()` empty → **no server child → no SYN-ACK → SYN_SENT hang**. The real fix is **`TCP_MAX_CONNS` 8→32** (v5.8.78 already bumped it): N concurrent loopback clients to one service need 1+2N slots. Why it looked like a fragile Heisenbug: the **Makefile only listed `kernel.h` as a header prereq** (`%.o: %.c kernel.h`), not `tcp.h`, so bumping `TCP_MAX_CONNS` did **not** recompile `tcp.o`/`net.o` — the 8-slot table stayed compiled in until an unrelated `tcp.c` edit forced a rebuild that finally applied the 32 (so edits that touched tcp.c seemed to "fix by perturbation"). **Fix:** GCC auto-dependencies — `-MMD -MP` + `-include $(OBJS_KERNEL:.o=.d)`, so every object depends on every header it `#include`s; `clean` also removes `*.d`. Verified: clean build passes netstorm; editing tcp.h now rebuilds tcp.o/net.o (timestamps + behaviour). The v5.8.78 serialization/atomic-port hardening stay (correct; they shift timing so 8 *mostly* passes — 5/6 — but 32 is the robust fix). **No new syscalls.** |
| v5.8.80 | **Four POSIX file syscalls — `stat`/`fstat`/`lseek`/`getppid` (SYS 44–47) — plus two Makefile build-correctness fixes that had been silently shipping stale kernels.** (1) **Syscalls:** `SYS_STAT`(44)/`SYS_FSTAT`(45) fill a userspace `struct stat` (`st_size`, `st_mode` with S_IFDIR/S_IFREG, `st_ino`) via new `vfs_stat(path,…)`/`vfs_fstat(fd,…)` (dir → mode `0x41ED`, regular → `0x81A4`); `SYS_LSEEK`(46) moves the per-fd offset (`ufd_offset_of`) with SEEK_SET/CUR/END (END uses `vfs_fsize`), rejecting pipes/sockets; `SYS_GETPPID`(47) returns `cur->ppid`. `user/syscall.h` gains `struct stat`, the `S_IS*`/`SEEK_*` macros and four wrappers; new `user/stat.c` → `/stat.elf` (`stat <path>`) exercises all four. Syscall count 44→48. (2) **Build fixes (continuing the v5.8.79 staleness saga):** the *real* reason incremental builds shipped stale kernels was **`-include $(…d)` sitting ABOVE the `all:` target** — the first auto-dep `.d` (kernel.o's) hijacked make's **default goal to `kernel.o`**, so a bare `make` (and `build.ps1`, which runs `make -C kernel` with no target) built only kernel.o and never relinked `nyx-kernel.bin`; only `make clean` (which deletes the `.d`s → `all` becomes first again) had been restoring correct builds. Fixed with an explicit **`.DEFAULT_GOAL := all`**. Second, the generated **`initramfs_data.h` now regenerates via a `FORCE` + content-compare** recipe (always re-runs mkinitramfs into a temp, `mv`s over the header only when the bytes differ) so a freshly-built `user/*.elf` can never be missed, yet unchanged builds don't needlessly relink — this is what had let `/stat.elf` be absent from the initramfs (`Loaded 53` of 54) even though it sat on disk. Verified in QEMU: `stat /sh.elf` → `size=23480 type=file mode=0x81a4`, `fstat` matches, `lseek(mid)` and `lseek(SEEK_END)` correct, `getppid` → the shell's pid; `stat /` → `type=dir`; boot now loads all **54** initramfs files. |
| v5.8.81 | **VFS hardening — closed a latent heap OOB and raised the capacity caps.** `vfs_node_t` holds a fixed `children[MAX_CHILDREN]` array, and **eight creation paths appended with an unchecked `children[child_count++] = node`** — once a directory hit `MAX_CHILDREN` the next insert wrote one element past the array, corrupting the adjacent inode in the static `nodes[]` pool. Not hypothetical: `/` already holds 54 initramfs files + the mount dirs (~58 children) and the old cap was **64**. Fix: **(1)** a single `vfs_append_child(parent, child)` choke point returns -1 at capacity (callers `free_node()` the just-alloc'd node and return their own error) and now backs every insert in `vfs_open`(O_CREAT), `vfs_create_from_mem`, `vfs_mkdir`, `vfs_rename`, `vfs_chdir`, `vfs_touch`, `vfs_write_file`, `vfs_cp` — `vfs_rename` appends to the destination *before* unlinking from the source, so a full target can't orphan the node; the two already-guarded paths (`proc_make`, mount-stub) keep their pre-`alloc` checks. **(2)** Caps raised: `MAX_INODES` 128→256, `MAX_CHILDREN` 64→128 (root now has 2× headroom; `nodes[]` BSS ~91KB→~313KB — fine at 256 MB RAM). **(3)** The initramfs loader (`kernel/initramfs.c`) now **logs a warning** instead of silently dropping a file when the VFS is full, and `file_count` reflects what actually loaded. New `user/vfsfill.c` → `/vfsfill.elf` regression test fills a dir until `open()` refuses, then confirms an unrelated inode still stats + cleans up. **No new syscalls** (still 48). Verified in QEMU: boot loads all **55** initramfs files with **0** VFS-full warnings; `vfsfill` created exactly **128** files then `open()` returned <0 ("refused cleanly, no crash"), `/sh.elf` still stat'd (no corruption), 128 files cleaned up, and `stat /tmp` worked afterward — system fully alive. |
| v5.8.82 | **`dup` + `rename` syscalls, and shell-spawned tools now inherit the terminal's cwd.** Investigating "make cwd per-process" surfaced that it *already is*: the process struct carries `cwd[MAX_PATH]`, `do_fork` copies the parent's, `copy_path_from_user` routes every relative path through `path_resolve(cur->cwd, …)` (which handles `.`/`..`/`//`), and `chdir`/`getcwd` (SYS 21/22) read/write it. The real gap: `create_user_process` `memset`s the struct, so a **spawned** (non-forked) tool started at `/` regardless of the terminal's `cd`. Fixes: **(1)** `create_user_process` now seeds `p->cwd` from the spawning context's cwd (`vfs_getcwd()` — the global `current_dir` the compositor sets per-terminal), so a tool run after `cd /tmp` begins in `/tmp`. **(2)** New **`SYS_DUP`(48)** — `dup(oldfd)` returns the lowest-available ufd aliasing oldfd (pipe ends `pipe_incref`; VFS/socket handles alias — safe because `vfs_close` is a no-op for the ramdisk nodes ring 3 opens), and both fds stay open, unlike `dup2`'s move. **(3)** New **`SYS_RENAME`(49)** — `rename(old,new)` wraps the v5.8.81-hardened `vfs_rename`, refusing a missing source and confirming the destination landed. Userspace `dup`/`rename` wrappers added; new `user/fsx.c`→`/fsx.elf` demo. Syscall count 48→50. Verified in QEMU: a bare `fsx` printed `start cwd = /home/nyx` (inherited the Terminal's home, not `/`); after `cd /tmp` a second `fsx` printed `start cwd = /tmp` (inherited the `cd`); `rename a->b = 0` (old gone, new present); `dup(3)=4` still read the file after `close(3)`; relative `stat("fsx_b")` resolved under `/tmp` and `chdir("..")` walked to `/`. |
| v5.8.83 | **THE pipeline-corruption Heisenbug — root-caused and FIXED after spanning many increments.** For releases the shared-libc pipeline/fork churn (`ls\|less`, `pstorm`) intermittently corrupted memory, surfacing as a `heap_free` #GP, a `pdpte=0xFFFF…FF` page-table MAPCORRUPT, or a userspace SIGSEGV — the same corruption wearing different masks, and famously "un-pin-able" (every `printf`/instrumentation change moved which mask fired first). **Root cause:** `init_memory` reserved only page 0 and `[0x100000, _kernel_end)`, leaving pages **`0x1000–0xFFFFF` marked FREE** — including the legacy firmware hole **`0xA0000–0xFFFFF`** (the VGA framebuffer MMIO window and the video/system **BIOS ROMs**, which QEMU/SeaBIOS shadow write-protected). We never parse the multiboot RAM map, so `alloc_page` (which scans the bitmap low→high) handed those non-RAM frames out **under memory pressure** — writes are dropped, reads return firmware bytes — and the allocator laid live kernel structures (heap/slab metadata, page tables, user pages) onto ROM, then read back BIOS garbage. That is the entire Heisenbug: **stress-dependent** (the low RAM pool must be exhausted to climb into the `0xA0000+` hole, so light use never reaches it) and **manifestation-varying** (which mask fires depends on what the ROM frame was reused for). **How it was finally caught (perturbation-proof):** QEMU **`-icount shift=3`** (deterministic virtual time — makes the race reproducible on run 1 *and* observation-stable) + a native-gdb **hardware watchpoint set from boot** on the corrupt slot (`0xc2fc0`), conditioned on the exact bad value — it stopped with **`cr3=0`, `pc` inside SeaBIOS's high self-relocation**, i.e. the "writer" was the *firmware at POST*: the value was ROM content that had sat there since boot and was never RAM. Trail: `heap_free+22` #GP on a non-canonical `block->next` → the block lived at `0xc2fc0`, which is inside the `0xC0000–0xC7FFF` video-BIOS window → `alloc_page` had handed out a ROM frame. **Fix (one line):** `init_memory` now reserves all of **`[0, _kernel_end)`** (loop from page 0, not `0x100000/PAGE_SIZE`). Safe — the kernel loads at 1 MB and never allocates sub-1 MB RAM; the SMP trampoline at `0x8000` is a direct `memcpy` (now *protected* from `alloc_page`, not clobberable), and VGA/BIOS MMIO is accessed directly, never via the allocator; the ~640 KB of conventional RAM below 1 MB is a negligible forgone amount. **Also fixed a second, independent unreserved region** (a secondary path to the identical corruption, reached only after both the low pool and the low hole are exhausted): with `-mcmodel=large`, GCC emits large objects into **`.lbss`/`.ldata`/`.lrodata`**, which the linker script's `.bss`/`.data`/`.rodata` did **not** capture — so `page_refcount[]`, `page_pinned[]`, `pipes[]`, `nodes[]`, and `libc_segs[]` landed **past `_kernel_end`**, also unreserved. `linker.ld` now folds `.lbss/.ldata/.lrodata` into `.bss/.data/.rodata` (all five arrays confirmed below `_kernel_end` by `nm`). Plus a build fix: **`nyx-kernel.bin` now depends on `linker.ld`** — a linker-script-only edit was silently not relinking on incremental builds (the same stale-build class as the earlier tcp.h/`.DEFAULT_GOAL` bugs), which briefly masked the `.lbss` fix as ineffective. **No new syscalls** (still 50). **Verified:** `pstorm 60` × 4 real-time = **4/4 CLEAN** (0 faults / 0 MAPCORRUPT / 0 PANIC; was **0/4** before), and `pstorm 50` under **`-icount`** = **CLEAN** — the exact deterministic mode that had failed on run 1. |
| v5.8.84 | **Physical allocator now honors the multiboot2 memory map — the principled generalization of the v5.8.83 firmware-hole fix.** v5.8.83 stopped `alloc_page` from handing out the *low* firmware hole by hardcoding a reserve of `[0, _kernel_end)`, but that's blind to reserved regions *above* the kernel (safe only because `pstorm` never allocated high enough to reach them). Now `kernel_main` parses the **multiboot2 type-6 memory-map tag** into an `mb_mmap_entry_t[]` (kernel.h) and `init_memory(mem_size, mmap, count)` does the canonical thing: **mark every page used, then free ONLY the ranges the firmware reports as available RAM (type 1)**, and finally carve the kernel image + the whole low 1 MB back out (page 0 / real-mode IVT-BDA / the SMP trampoline at 0x8000). Every hole is now reserved automatically, whatever the RAM size or layout. Verified against QEMU's actual map for `-m 256M` — 7 regions incl. **`0xffe0000 len=0x20000 type=2`, a 128 KB firmware-reserved block near the top of RAM that v5.8.83 would have freed** (the exact class of high hole that could have reawakened the corruption under heavier load), plus the low EBDA (`0x9fc00`)/BIOS-ROM (`0xf0000`) ranges and the high BIOS-flash (`0xfffc0000`)/PCI-MMIO (`0xfd00000000`) holes. When no type-6 tag is present it falls back to the exact v5.8.83 behavior (reserve `[0,_kernel_end)`, free the rest). Boot now prints `[MEM] mmap 7 regions: 60699 pages free (237 MB usable), RAM top 255 MB`. **No new syscalls** (still 50). Verified: system boots + runs normally and `pstorm 40` is **CLEAN** (0 faults / 0 MAPCORRUPT / 0 PANIC). |
| v5.8.85 | **User stacks are now demand-grown with a guard page (was a fixed, pre-committed 64 KB).** Every user process used to get exactly `USER_STACK_PAGES`=16 pages (64 KB) mapped up front at exec — a hard ceiling (a deep call chain #PF'd → SIGSEGV at 64 KB) that also committed 64 KB of RAM per process even for tiny programs. Now `elf_load_image` / `create_user_process` commit only **`USER_STACK_INIT_PAGES`=4 (16 KB)**, and **`vm_handle_fault` grows the stack on demand**: a USER not-present fault in `[USER_STACK_LOW, USER_STACK_TOP)` below the committed pages materialises a fresh zeroed RW+NX page, down to a floor of **`USER_STACK_MAX_PAGES`=128 (512 KB)**. A fault below the floor is a genuine overflow → falls through to SIGSEGV; the page just under the floor `[USER_STACK_LOW-PAGE_SIZE, USER_STACK_LOW)` is a permanent unmapped **guard page**, and a fault there prints `[stack overflow] pid=… comm=… (cr2=…)` for a clear diagnostic before the kill. So a deep program now gets up to 512 KB (8× the old limit) while a plain one commits ~16 KB, and a runaway recursion is caught cleanly instead of dying at an unlabeled #PF. Placed before the `pte_ptr` walk (a grown page has no PTE yet); fork's COW and execve's fresh image both work unchanged (the constants are fixed VAs, no per-process state). New `kernel.h` constants (`USER_STACK_TOP`/`INIT_PAGES`/`MAX_PAGES`/`LOW`); `elf.c` + `process.c` stack setup loop to `USER_STACK_INIT_PAGES`; `paging.c` grow+guard blocks. New `user/stacktest.c` → `/stacktest.elf` (non-tail recursion, ~4 KB/frame). **No new syscalls** (still 50). Verified in QEMU: `stacktest` recurses ~192 KB and **SURVIVES** (was fatal at 64 KB); `stacktest 300` (~1.2 MB) trips the guard — kernel prints `[stack overflow]` and the process dies with SIGSEGV; `pstorm 40` stays **CLEAN** (0 faults / 0 MAPCORRUPT / 0 PANIC — the fault-handler change is a clean regression). |
| v5.8.86 | **SMEP + SMAP — hardware-enforced ring-0 ↔ user isolation.** Enable the CPU's Supervisor Mode Execution/Access Prevention when the processor advertises them (CPUID.(EAX=7,ECX=0):EBX bit 7 = SMEP, bit 20 = SMAP): **CR4.SMEP** (bit 20) makes ring 0 **executing** a user (U=1) page a #GP; **CR4.SMAP** (bit 21) makes ring 0 **reading/writing** a user page a #GP unless RFLAGS.AC is set (stac/clac). The enabling insight: **NyxOS's kernel never touches user pages directly** — it runs on the kernel CR3 (user pages unmapped) and reaches user data only by walking the user tables to a PHYSICAL address (`user_v2p`) and accessing it through the identity map, a supervisor page. Every user-memory access already funnels through `copy_to_user`/`copy_from_user`/`copy_str_from_user` (and every caller, incl. signal-frame delivery), so **no stac/clac bracketing is needed**, and turning SMEP/SMAP on becomes a **hardware-enforced audit** of that isolation invariant: any future stray user-VA access from ring 0 now traps instead of silently succeeding (it would also stop the classic "corrupt kernel pointer executes user-controlled code" escalation). New `enable_smep_smap()` (kernel.c): CPUID-gated CR4 set + `clac` (AC starts clear); called on the BSP after `init_paging`, and by each AP (smp.c — CR4 is per-CPU). Because it's CPUID-gated it's a safe no-op on CPUs lacking the features — including the default QEMU `qemu64`. Boot prints `[CPU] SMEP=on/unavailable SMAP=on/unavailable`. **No new syscalls** (still 50). Verified two ways: with **`-cpu qemu64,+smep,+smap`** the boot reports `SMEP=on SMAP=on` and `stacktest` + `pstorm 30` run with **0 faults / 0 MAPCORRUPT / 0 PANIC / 0 #GP** — the heavy fork/pipe/execve churn never once accessed a user page from ring 0, proving the invariant under hardware enforcement; with plain **`qemu64`** it reports `unavailable` and boots identically (graceful no-op). |
| v5.8.87 | **Real user threads — `clone(CLONE_VM)` + `futex` (SYS 50/51, count 50→52).** **`do_clone(fn, stack, arg, flags)`** creates a scheduled ring-3 task that **SHARES** the caller's address space (`t->page_directory = self->page_directory`) instead of taking fork's COW copy — that one line is the whole thread property. It gets its own kernel stack from new `init_thread_task_stack()`, which builds the iretq frame with RIP=`fn`, RSP=the caller-supplied stack and **RDI=`arg`** (the 6th push of the high→low `rax,rbx,rcx,rdx,rsi,rdi,rbp,r8..r15` SAVE_REGS block), so the thread enters as `fn(arg)` under the SysV ABI. Without `CLONE_VM` clone() is refused (fork remains the separate-address-space primitive). **Address-space lifetime** is the subtle part: several `process_t` now share one PML4, so a new **`addr_space_shared(pml4, except)`** guards *all four* `free_page_directory` sites (execve's `old_pd`, `reap_user_process`, `reap_zombies`, `do_waitpid`) — only the LAST task out releases the space, in any exit order. **`do_futex(uaddr, op, val)`**: `FUTEX_WAIT` blocks while `*uaddr == val` (returning at once if it already differs — the compare-and-sleep that makes a lock race-free), `FUTEX_WAKE` wakes up to `val` waiters. The queue is keyed by the word's **physical** address (`user_v2p`, de-static'd in syscall.c) so every task mapping the page agrees on the key, and the word is read through the **identity map** — the same supervisor path `copy_to_user` uses, so it stays SMEP/SMAP-clean (v5.8.86). WAIT parks with the `do_waitpid` discipline (`blocked_in_kernel`, and save/restore of the shared `user_cr3`/`user_rsp` across the `sti;hlt`); the compare and the park are atomic under the syscall's interrupt mask, so no wakeup is lost. Userspace gains `clone()`/`futex_wait()`/`futex_wake()` wrappers plus new `user/threads.c` → `/threads.elf`: 4 threads × 2000 increments of a shared counter behind a textbook futex mutex (atomic `xchg` fast path, syscall only on contention). **Known limits (honest):** `program_break`, the VMA table and fds are still per-`process_t` *copies* taken at clone time, so post-clone `sbrk`/`mmap`/fd changes don't propagate between threads (multi-threaded `malloc` isn't coherent yet), and a thread is given no `file_buf` ownership (file-backed mmap in a thread zero-fills) so a sibling's exit can't double-free it. Verified in QEMU: **4 tids started**, shared counter **= 8000 of 8000 → OK** (proving both the shared address space and the mutex — 8000 deliberately non-atomic increments serialized correctly), and `pstorm 30` still **0 faulted / 0 MAPCORRUPT / 0 PANIC** (the teardown guard is a clean regression). |
| v5.8.88 | **Thread coherence — one heap and one mmap table per thread group (multi-threaded `malloc` now works).** v5.8.87 shipped real threads but left `program_break`/`heap_start` and the VMA table as per-`process_t` **copies** taken at clone time: each thread bumped its *own* break, so two threads `malloc`ing were handed **overlapping** regions, and a page `sbrk`'d by one thread wasn't in a sibling's fault-in window (spurious SIGSEGV). Fixed by giving every task a **`tgid`** — the pid of the thread-group **leader** that owns the shared heap/VMA state (0 or own pid ⇒ it *is* the leader; `do_clone` sets `t->tgid = tg_leader(self)->pid`, so a thread cloning a thread still joins the one group). A pid rather than a pointer **by design**: an exited leader can never leave a dangling reference — `tg_leader()` resolves by `find_process()` and falls back to self. Six chokepoints now resolve through it: **`SYS_SBRK`**, the **heap window in `vm_handle_fault`** (a page sbrk'd by one thread must fault in for any sibling), and mmap.c's **`vma_find` / `do_mmap` / `do_mprotect` / `do_munmap`** — so allocation, lookup and teardown all hit one shared table and two threads can never be handed the same region. **`mmap_free_bufs` is deliberately NOT redirected**: those `file_buf` snapshots are per-task property, and redirecting it would let a thread's exit free the leader's buffers. Leader-exits-first is handled too: new **`tg_reassign_leader()`** (called from all three reap paths before `mmap_free_bufs`) hands the shared heap/VMA state — *including `file_buf` ownership*, zeroing it in the dying leader so it isn't freed out from under the heir — to a surviving member and re-points the rest at it, so the group stays coherent in any exit order. `user/threads.c` extended: each thread now `malloc`s and signs a block, and main verifies all blocks are **distinct, non-overlapping and intact** — exactly the check that would have failed before this. **No new syscalls** (still 52). **Remaining limit (honest):** the fd table is still a per-task copy taken at clone, so fds opened *after* the clone aren't shared between threads. Verified in QEMU: 4 tids, counter **8000/8000 → OK**, **heap → OK**, and `pstorm 30` still **0 faulted / 0 MAPCORRUPT / 0 PANIC**. |
| v5.8.89 | **Shared fd table per thread group — the thread model is now complete.** v5.8.88 gave threads one heap and one mmap table but left the descriptor table a per-task copy taken at clone, so an fd opened *after* the clone was invisible to siblings (and each thread's copy held its own pipe refcounts). Now the fd table follows the same `tg_leader()` rule. The redirect landed almost entirely in **four small helpers** — `ufd_alloc` / `ufd_lookup` / `ufd_offset_of` / `ufd_release` — which every fd syscall already funnels through, so a one-line `fd_owner()` in each made open/close/read/write/lseek/fstat/getdents/pipe/socket group-wide at a stroke; only `dup`/`dup2`, which touch `p->ufd_*` directly, needed their own binding redirected. **Deliberately NOT redirected: `get_cur_proc()` in general** — `getpid()` and friends must still answer for the calling THREAD, so only fd paths resolve to the leader. `do_clone` now copies **no** fds at all (and takes no `pipe_incref`): a thread's own table stays empty, which is exactly what makes `close_proc_fds()` at its exit a safe no-op that leaves the group's descriptors open for its siblings — the same ownership discipline `mmap_free_bufs` uses. `tg_reassign_leader()` also hands the fd table to the heir and **clears it in the dying leader**, so a leader exiting first can't close descriptors the survivors are still using. `user/threads.c` extended: thread 0 opens `/thrfd.txt`, writes to it and leaves it open; **main then lseeks and reads back through that same fd** — a descriptor opened by a thread, used by another task, which simply would not exist with per-thread tables. **No new syscalls** (still 52). Verified in QEMU: 4 tids, counter **8000/8000 → OK**, heap **→ OK**, **fds → OK**, and `pstorm 30` still **0 faulted / 0 MAPCORRUPT / 0 PANIC**. |
| v5.8.90 | **SMP stage 1 — the application processors are alive.** Until now the kernel was single-CPU *in practice*: no test harness ever passed `-smp`, so the AP path shipped in v5.7.x had never actually run past `cli; hlt`. This stage gives each core a real existence — it executes kernel code and services **its own** interrupts — without letting it near the scheduler. **Per-CPU block:** `cpu_info_t` gains `ticks`, and `cpu_self()` resolves the calling core from CPUID(1).EBX[31:24] (an instruction, not a memory access, so it is valid inside an ISR and before any mapping is reachable). **AP bring-up** (`ap_main`): load the BSP's GDT and IDT — both are read-only after boot, so one copy serves every core — then enable the LAPIC and start its **periodic LAPIC timer**, then `for(;;) sti; hlt`. **The TSS is deliberately NOT loaded**: an AP never leaves ring 0 yet, and `ltr` on the shared TSS would #GP anyway since the BSP's `ltr` already set its busy bit. **A dedicated interrupt path, not `irq_common`:** the AP timer gets its own vector (0x40) and stub, because `irq_common` drives the scheduler through the machine-wide globals `saved_rsp`/`next_rsp`/`next_cr3` — an AP entering it would overwrite whatever context switch the BSP was mid-way through. `ap_timer_tick()` therefore does exactly two CPU-local things: bump `cpu_self()->ticks` and EOI its own LAPIC. Nothing else is safe while the kernel still has **no spinlocks** (`preempt_disable()` is just a counter, which means nothing to another core). `enable_smep_smap()` was split so APs call a **silent** `cpu_apply_smep_smap()` — `printf` is shared unlocked state. **Latent bug found and fixed by doing this:** the AP trampoline set only `EFER.LME`, never **`EFER.NXE`**, which `init_paging` enables on the BSP alone. With NXE clear, every PTE carrying `PAGE_NX` is a *reserved-bit* violation — so an AP died the instant it touched its own LAPIC (`map_mmio` maps that page NX). Invisible while APs only halted; instantly fatal once they do anything. **Observability:** `cpus` now prints each core's clock source and tick count. `run.ps1` defaults to `-smp 4`. **No new syscalls** (still 52). Verified in QEMU with `-smp 4`: 3 APs started / 4 CPUs online, all three AP counters climbing **independently** across two samples (~500 Hz each, against the BSP's 1000 Hz PIT), `threads` still **8000/8000 OK** and `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**. **Next:** stage 2 = real spinlocks for the allocators/process table plus per-CPU `user_cr3`/`user_rsp`; stage 3 = per-CPU scheduler, user processes on APs, TLB shootdown IPIs. |
| v5.8.91 | **SMP stage 2 — real spinlocks, and the syscall path's scratch state goes per-CPU.** Stage 1 left the APs alive but fenced off, because everything they might touch was unsynchronised. Two halves fix that. **(1) Locks.** `spinlock.h` adds `spin_lock`/`spin_trylock`/`spin_lock_irqsave` — a plain test-and-set with `pause`, not a ticket lock (8 cores, critical sections of a few dozen instructions; fairness isn't worth the state). The physical page allocator (`alloc_page`/`free_page`/`page_incref`) and `kmalloc`/`kfree` now take one, replacing `preempt_disable()`, which stops a context switch on the LOCAL core and means nothing to another one. Always taken with interrupts off: a spinlock guards against other cores, not against this core re-entering through an interrupt, which would just deadlock. One lock covers slab **and** heap because they are one dependency graph (`slab_new_page` → `alloc_page`), which also makes the ordering trivially deadlock-free — `kmalloc_lock` before `page_lock`, never the reverse. **(2) Per-CPU syscall state.** `user_rsp`/`kernel_rsp`/`user_cr3`/`syscall_frame_ptr` were three `dq 0` globals in isr_stubs.asm — one set for the whole machine, which two cores in a syscall would have overwritten for each other (the v5.8.83 wild-writer class, multiplied). They now live in an **ABI-fixed prefix** of `cpu_info_t` that `syscall_entry` reaches through **GS**, the only register-free anchor the hardware offers; a compile-time offset check in smp.h fails the build if anyone reorders them. **Deliberately NO `swapgs`:** the textbook discipline swaps GS on every ring transition and is wrong here, because `irq_common` enters from a process in ring 0 and can `iretq` into a *different* process in ring 3 — the swaps wouldn't pair, and the next `syscall` would compute gs:0 and write to address 0. Instead GS_BASE simply stays pointed at the per-CPU block in both rings, which is safe only because NyxOS gives userspace no GS base of its own (no TLS, no `arch_prctl`, CR4.FSGSBASE off) and ring 3 still faults on the supervisor page. **If userspace ever gets TLS, this must become a real swapgs.** The 87 C references keep their exact shape via accessor macros in kernel.h. **Honest limit:** per-CPU fixes cross-CPU clobbering, not the save/restore dances around every blocking wait — those slots are still shared between processes time-slicing on one core, and ending that needs them saved into the process at context-switch time. **Bug found during the work:** `cpu_install_gs_base` first wrote MSR `0xC0000100`, which is **FS_BASE**, not GS_BASE (`0xC0000101`) — GS stayed 0, so every user process died on its first syscall while the kernel-only paths (login, compositor, `smpstress`) ran fine. **New `smpstress [secs]`** is what proves the locks: every core runs the same alloc/verify/free loop at once, each checking that the memory it was handed still holds what it wrote. **No new syscalls** (still 52). Verified with `-smp 4`: **~900k ops across 4 cores, 0 integrity failures**, free pages exactly balanced on a warm run (a cold run is legitimately one page short — a new slab page is never returned), `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**, per-CPU ticks still climbing independently, and **single-CPU boot unaffected**. |
| v5.8.92 | **SMP stage 3a — the APs run scheduled kernel threads, in parallel with the BSP.** Stages 1 and 2 gave the cores life and safe shared state; this one gives them *work*. `ap_timer_stub` stops being a tick counter and becomes a **real context-switching ISR**: it saves the full register set, parks its RSP in the per-CPU `sc_saved_rsp`, calls this core's own `ap_scheduler_tick()`, and resumes whatever that returns in `sc_next_rsp`. The pushed frame matches `init_task_stack()` exactly (error, int_no, 15 GPRs), which is precisely what makes a thread built by the existing `create_process` resumable on an AP. **Mutual exclusion is by PINNING, not by locking the tasks:** `process_t` gains `sched_cpu` (0 = BSP, the memset default, so every existing task is unaffected; N = that AP exclusively). No core will even look at a task that isn't its own, so two cores can never pick the same one and no "running on" flag is needed — the BSP's delicate `irq_scheduler_tick` needed exactly **one** added line (`if (p->sched_cpu != 0) continue;`). A new `sched_lock` guards only the SHAPE of `process_table` (the entries and `process_count`) against a core scanning while another appends or swap-removes; the contents of a `process_t` need no lock because one CPU owns it. **`saved_rsp`/`next_rsp`/`next_cr3` moved into the per-CPU block** and `irq_common` now reaches them through GS — which also *deleted* the old hand-rolled `mov rax, sym / add KERNEL_BASE` dance, since the GS base is already the higher-half alias. That path runs 1000×/second under every existing test, so the change is heavily exercised rather than merely written. Each CPU's scheduler cursor is `sched_cur`, with NULL meaning "the idle context this core booted into", whose stack pointer is parked in `idle_rsp` so there is always somewhere to return when nothing is runnable. No CR3 switching and no ring crossing on APs: these are kernel threads sharing the kernel address space, which is what keeps stage 3a clear of the ring-3 machinery (per-CPU TSS and friends come with 3b). **New `smpthreads [secs]`** wakes one worker per AP. The proof is deliberately un-fakeable: a worker counts into `cpu_self()->work_ops`, i.e. the core that **actually executed it** — if pinning broke and the BSP picked one up, the work would appear in the BSP's row. **No new syscalls** (still 52). Verified with `-smp 4`: `apworker1/2/3` ran **30.9M / 28.0M / 25.7M** iterations on CPUs 1/2/3 while the **BSP counted 0** (it was sleeping) → `PARALLEL OK`; `smpstress` still ~470k ops / 0 integrity failures / balanced; `threads` **8000/8000 OK**; `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**; and **single-CPU boot unaffected**. **Next (3b):** per-CPU TSS + user processes on the APs + TLB shootdown IPIs. |
| v5.8.93 | **SMP stage 3b — user processes run in ring 3 on the application processors.** The arc closes: cores alive (3a: kernel threads) now take real ring3 -> ring0 -> ring3 transitions of their own. **Per-CPU TSS** was the blocker. A TSS may be busy on only one core — `ltr` sets the busy bit in its descriptor — so cores cannot share one, which is exactly why an AP deliberately skipped `ltr` back in stage 1. The GDT now carries **one 16-byte TSS descriptor per CPU** (`TSS_SEL_FOR(cpu)`, CPU 0 keeping the historical `TSS_SEL`), each core `ltr`s its own, and `tss_set_stack()` writes the CALLING core's RSP0 — which is what makes a ring-3 entry land on the kernel stack of the process *that core* is running. IST stacks are still shared and set on every TSS: two cores double-faulting at the same instant would collide, an accepted limit of an already-fatal path. `ap_timer_stub` gains the CR3 switch it did not need for kernel threads — an AP can now be interrupted on a user CR3, where the kernel's low `-mcmodel=large` code is not mapped at all. **The scheduler side needed no new logic at all:** `ap_scheduler_tick` simply calls **`sched_target()`**, the BSP's existing aiming function, because every slot it writes (`next_rsp`/`next_cr3`/`kernel_rsp` and the TSS RSP0) became per-CPU in stages 2 and 3a. Reusing it rather than writing a second copy is what brings along every ring/CR3 subtlety earlier Heisenbugs taught us. **Placement:** `create_user_process` hands new processes out round-robin when `smp_user_balance` is on — **off by default**, so the default placement stays CPU 0 and enabling SMP costs nothing until asked for. **Thread groups are pinned to one core** (`do_clone` inherits the leader's `sched_cpu`): threads share a PML4, so two on different cores would leave stale TLB entries on an unmap and NyxOS has no shootdown IPI yet. Separate processes own their tables and spread freely. **New `smpuser [n]`** spawns balanced copies of `/spin.elf` and reports where each landed plus what each core is executing. **No new syscalls** (still 52). Verified with `-smp 4`: `USERSPACE ON APs OK` — processes placed 3/4 on APs, an AP's `sched_cur` observed as the ring-3 `spin` process, and its repeated `[spin] ring-3 process alive` output proving syscalls leaving and re-entering ring 3 **on an AP through its own TSS**; a core seen time-slicing between a kernel thread and a user process; `smpthreads` still `PARALLEL OK`; `smpstress` ~470k ops / 0 integrity failures / balanced; `threads` **8000/8000 OK**; `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**; **single-CPU boot unaffected**. **Remaining gap:** TLB shootdown IPIs — until they exist, thread-group pinning is what keeps the machine honest, and the VFS/terminal are still unlocked against an AP that calls into them. |
| v5.8.94 | **TLB shootdown IPIs — the mechanism, proven.** Changing a page table entry only invalidates the TLB of the core that did it; every other core keeps using a translation the tables no longer describe. `tlb_shootdown(vaddr)` invalidates locally, broadcasts vector **0x41** with the LAPIC's "all excluding self" shorthand, and waits for every online core to acknowledge. **Deadlock was the whole design problem, in two shapes.** (1) Two cores shooting down at once: the sender holds a lock and waits for acks, so if the second core spun on that lock with interrupts *off* it could never service the first's IPI and both would hang. The shootdown lock is therefore taken with plain `spin_lock()` — **deliberately not `spin_lock_irqsave`** — and the ack wait runs with interrupts on, so a core waiting its turn stays interruptible. (2) Shooting down while holding a lock others spin on with interrupts off: **THE RULE is never to call this holding `page_lock` or `kmalloc_lock`**, which callers satisfy because the allocators take and release those internally. The wait is also bounded — a wedged core must not hang the machine. The handler stub switches to the kernel tables before calling C (it can land on a core running ring 3, where the low `-mcmodel=large` code is unmapped) and restores the interrupted CR3 on the way out; it saves **RBX** too, which is callee-saved and clobbered building that address. **The test is built to be able to fail, and the first version correctly refused to claim proof.** `tlbtest` remaps a page and asks CPU 1 what it sees *before* any shootdown: if that read is already fresh, the machine never needed the IPI and the result is reported **INCONCLUSIVE** rather than PASS — which is exactly what happened first time. The reason was real and worth recording: **an AP reloads CR3 on every timer tick (a context switch), and a CR3 reload flushes the TLB**, so a stale entry evaporated within a millisecond. The fix was to have CPU 1 hold the measurement window with **interrupts disabled** (no preemption, no CR3 reload), re-enabling them before the shootdown phase since an IPI cannot reach a core that is not listening. With that window the readings are unambiguous: baseline `0xAAAA…`, after remap with no shootdown **`0xAAAA…` (genuinely stale)**, after the IPI `0xBBBB…` → **SHOOTDOWN PROVEN**. **Scope, stated plainly: this ships the mechanism, not its integration.** `tlb_shootdown` is not yet called from `do_munmap`/`do_mprotect`/the COW remap, and thread groups are still pinned to one core — un-pinning is only safe once every page-table mutation site shoots down, which is the next step. **No new syscalls** (still 52). Verified with `-smp 4`: `SHOOTDOWN PROVEN`, `USERSPACE ON APs OK`, `PARALLEL OK`, smpstress **521k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm` **330 iterations / 0 faulted**, 0 MAPCORRUPT / 0 PANIC. One run out of three that included `smpuser` logged a single unexplained `[fault]` on a ring-3 process being SIGKILLed; the baseline suite is clean across runs and it has not reproduced. |
| v5.8.95 | **TLB shootdown wired into unmap and mprotect — and a deadlock shape found while auditing.** v5.8.94 built the mechanism; this makes the kernel actually use it. **The audit changed the plan.** Every candidate call site — `do_munmap`, `do_mprotect`, the COW remap — runs with **interrupts disabled**: syscalls under `SF_MASK` (which clears IF) and the fault handlers behind interrupt gates. That is deadlock shape (1) in disguise, because a sender that cannot take an interrupt cannot acknowledge anyone *else's* shootdown, and two cores unmapping at once would each wait on the other. `tlb_shootdown()` therefore now **enables interrupts for its duration and restores the caller's IF exactly** on the way out; it is never called from an IRQ handler, only from syscall/fault context where being interruptible is already normal. **Which sites got it, and why only those.** Shootdown went into the paths that REMOVE or RESTRICT a mapping — `vm_free_range` (unmap: another core would keep reading a frame already returned to the allocator), `vm_protect_range` (mprotect only ever narrows what a core has cached) and `vm_unmap`. The demand-fault paths that make a not-present page present keep their plain local `invlpg`: a remote core holding the old not-present state simply faults and re-walks, so a broadcast would buy nothing. **Deliberately NOT done, and this is the honest part: thread groups stay pinned to one core.** Un-pinning needs the COW remap covered too, and that one lives in the `#PF` handler — where enabling interrupts is exactly the kind of change that produces hangs which do not reproduce headless. Shipping the pin removal without verifying that would be selling safety that was never tested. **No new syscalls** (still 52). Verified with `-smp 4` with the shootdown live: `pstorm` exercises `vm_free_range` on every process teardown, so the new path runs constantly — smpstress **533k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**. **Next:** cover the COW remap safely, then un-pin thread groups. |
| v5.8.96 | **COW remap shoots down too — and two things that did NOT work, reverted and documented.** The kept change is small and verified: the copy-on-write path in `vm_handle_fault` REPLACES an existing translation (a shared read-only frame becomes a private writable one), so a sibling thread on another core holding the old entry would keep reading the stale frame. It now calls `tlb_shootdown(cr2)` instead of a local `invlpg`, reached with interrupts off, which `tlb_shootdown` handles by enabling them for its duration and restoring the caller's IF. **What failed #1 — un-pinning thread groups.** This was the increment's headline and it does not work: with threads spread across cores, `threads` starts all four tids and the machine **wedges** — no counter, no output, no fault, no panic. Reverted to pinning. The diagnosis worth keeping: TLB shootdown covers the page tables, but what it does not cover is that the blocking primitives a thread group leans on — futex parking, and the per-CPU `user_cr3`/`user_rsp` save/restore around every wait — were written for one scheduler on one core. That, not stale TLBs, is the real prerequisite. **What failed #2 — a correction to v5.8.93.** Testing `pstorm` with `smp_user_balance` ON (which no earlier test had ever done — `smpuser` cleared the flag before pstorm ran) shows it **wedges too**. So the balancing shipped in v5.8.93 is safe for long-lived processes and NOT for fork/exec churn; `smpbalance` now prints that warning rather than implying otherwise. The cause is again not the page tables. **No new syscalls** (still 52). Verified in the DEFAULT configuration with the COW shootdown live: smpstress **465k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**; and with balancing on, `threads` still 8000/8000 with heap and fds OK. **Next, and it is now clear what it is:** make the blocking primitives (futex, waitpid, the per-CPU syscall-slot save/restore) correct across cores. Un-pinning threads and balancing churn both depend on that, not on more shootdown work. |
| v5.8.97 | **`get_current_process()` was BSP-only — the root cause of both v5.8.96 hangs, found and fixed.** The question every syscall asks first is "which process is running right now?", and the answer came from `current_idx`, which is written **solely** by `irq_scheduler_tick` — the BSP's scheduler. `ap_scheduler_tick` (v5.8.92) tracks its own task in `cpu_info[].sched_cur` and never touches `current_idx`. So any task executing on an application processor resolved to **whatever the BSP happened to be running**. A thread calling `futex_wait` on an AP therefore blocked the BSP's task instead of its own, and the machine wedged with no fault and no panic — exactly the symptom that cost two increments. It stayed hidden because work that never asks who it is (`spin.elf` writing to serial, which is what v5.8.93 verified with) looked perfectly healthy. The fix is five lines: on an AP, return that core's `sched_cur` (NULL there means the idle context, i.e. genuinely no current process); on the BSP, the old path. **This was found by reasoning about the code, not by debugging** — the two hangs shared "quiet, no fault, only under cross-core scheduling", which points at identity, not memory. **Result: the wedge is gone.** With threads spread across cores `threads` now completes. **It is still not enough to un-pin, and the pin stays:** the next failure is immediate and much more informative — `pstorm` panics with **#GP (err 0x8) at RIP 0x10049f**, inside the interrupt-stub / ring-transition path, after two iterations. That is the next thing to chase, and unlike a silent wedge it comes with an address. **No new syscalls** (still 52). Verified in the default configuration: 4-CPU smpstress **435k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**; single-CPU **PASS** as well. |
| v5.8.98 | **The #GP pinned down to one instruction — but not solved, and said so.** `objdump` puts the panic RIP `0x10049f` exactly on the **`iretq` that closes `irq_common`**, the BSP's interrupt return. Error code `0x8` says the frame it pops names selector `0x08`, i.e. a bad CS/SS. The hypothesis chased: `irq_scheduler_tick` does `cur->stack = saved_rsp` on `process_table[current_idx]` **without checking affinity**, and `current_idx` really can come to point at an AP-pinned task without this scheduler ever choosing one — the reap paths swap-remove and then patch `current_idx` to the moved slot, which can hold anything. `pstorm` forks and reaps constantly, which fits the panic arriving two iterations in. **The guard is kept** (`if (cur->sched_cpu == 0)`), and it is a **no-op in the default configuration** where every task has `sched_cpu == 0` — so it costs nothing and is a genuine safety net once balancing is on. **But it did not fix it.** With threads spread, the same `iretq` still faults; the error code moved from `0x8` to **`0x0`**, which is not selector-related at all (a non-canonical RIP or bad RFLAGS in the frame). So the frame reaching that instruction is wrong in more than one way and the cause is upstream of both fixes so far. Thread groups stay pinned; reverted for the third time. **What the next session gets that this one did not have:** the exact instruction, both error codes and what each rules in or out, and a reliable reproducer (`smpbalance on`, then `threads`, then `pstorm`). This is now a job for `-icount` + a gdb watchpoint on the outgoing task's `stack` field — the v5.8.83 methodology — not for more reasoning from the source. **No new syscalls** (still 52). Verified in the default configuration: smpstress **480k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**. |
| v5.8.99 | **`preempt_count` is per-CPU (release blocker P0.2) — and a correction to my own priority call.** It was one `static volatile int` shared by every core, which was wrong in both directions once a second core scheduled: the BSP's `preempt_disable()` meant nothing to an AP, and an AP raising it would have stopped the BSP's scheduler outright (`irq_scheduler_tick` returns early while it is non-zero). It now lives in `cpu_info_t`, because "don't preempt me" is inherently a statement about **one** core. `ap_scheduler_tick` honours its own count too — expressed as "keep running the current task" rather than an early return, because `sc_next_rsp` **must** be written before control goes back to the stub or the core resumes the previous tick's stale pointer. **The correction:** I ranked this P0 ("reachable in the default configuration") and an audit says that was too strong. The only real `preempt_disable` callers are `net.c`, `pipe.c` and the reap paths, all of which run on the BSP today; **no AP-reachable path calls it** (memory.c's uses became spinlocks in v5.8.91). So the harmful direction was **latent, not active** — it would have become real the moment any AP work called it, which the P1 locking sweep would have triggered. Worth shipping as a footgun removed, not as a live bug fixed. **What this deliberately does NOT fix, and the comment in process.c says so:** call sites using `preempt_disable` as a cross-core *lock* (net.c, pipe.c, reap_zombies) are still only safe while one core touches that state — true by default, false once user processes run on APs. Those need real spinlocks and remain P1. **No new syscalls** (still 52). Verified: 4-CPU smpstress **489k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**; single-CPU **PASS**. |
| v5.9.0-rc1 | **Release blocker P0.3 closed: the last local-only invalidations on paths that REMOVE or REPLACE a mapping.** Two sites, deliberately treated differently. **`unmap_page()`** (paging.c) removes an entry from the KERNEL tables, which every core shares — the case v5.8.95 missed while wiring up `vm_free_range`/`vm_protect_range`/`vm_unmap`. It now shoots down; its single caller (`vbe.c`, at mode-set) runs on the BSP holding no allocator lock, so the deadlock rule is satisfied. **`map_pml4()`** is the subtle one, because it serves two situations that must not be conflated: REPLACING a live translation (other cores hold the old one — broadcast required; this is exactly what `tlbtest`'s remap needed) versus creating one where there was none (a remote core holding the old not-present state simply faults and re-walks — a broadcast buys nothing). Since the second case **is the demand-fault path**, shooting down unconditionally would have put an IPI on every page fault in the system. So it now branches on whether the old PTE was present. **A consequence found while testing, and fixed:** with `map_pml4` shooting down, `tlbtest` triggers one while CPU 1 deliberately sits with interrupts disabled — a core that cannot answer. The ack wait's bound was 100,000,000 spins, which meant the sender burned seconds waiting for an answer that would never come. Cut to 2,000,000, and the timeout now **prints once** instead of degrading in silence (release blocker P3.2). **No new syscalls** (still 52). Verified at 4 CPUs: smpstress **492k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**, and `tlbtest` still reports **SHOOTDOWN PROVEN** with a genuinely stale read in the middle. **Not verified:** that the new timeout warning actually fires — the test harness filters serial output and would not have shown it. |
| v5.9.0-rc2 | **Ring-3 → ring-0 arbitrary write closed at its root (3 CRITICAL findings, one fix).** A full-codebase audit (12 subsystems, adversarially verified) found three separate privilege escalations that all reduced to ONE primitive. `alloc_page_directory()` mirrors `kernel_pml4[511]` — the identity map of **all** physical RAM — into every user PML4, and `KERNEL_BASE` is exactly `-2^39`, so `(KERNEL_BASE + X) >> 39 & 0x1FF` is 511 and the walk in `user_v2p()` resolved `KERNEL_BASE+X` straight to physical `X`. `user_v2p` had **no address bound and no PAGE_USER check**, so every `copy_to_user` whose destination was not separately validated was an arbitrary write into kernel memory from an ordinary unprivileged program. Reachable through at least: `signal_dispatch`'s trampoline write to the raw user RSP (with an attacker-chosen value, via `signal()`'s third argument → ring-0 code execution), `signal_deliver_fault`'s identical write, and `do_sigprocmask`'s unvalidated `oldset` pointer (a single call with the default empty mask zeroes the adjacent `uid`/`euid` in `process_t`). **Fixed at the choke point, not per call site** — patching each caller would have left the primitive intact for the next unguarded copy. `user_v2p()` now enforces two independent guards: the address must be in the canonical lower half, AND every level of the walk must carry `PAGE_USER`, so a mapping the hardware would refuse to ring 3 is refused here too. The second guard is what keeps this correct if a kernel-only mapping ever appears in the lower half. **New `user/vmtest.c` proves it in both directions**, which is the only way a security test means anything: a legitimate pointer must still be ACCEPTED (a kernel that rejected everything would pass a one-sided test) and four hostile ones — higher-half kernel alias, physical-zero alias, first non-canonical address, null page — must be REFUSED. All five correct: **USER/KERNEL BOUNDARY HOLDS**. Also fixed: `vmtest` surfaced that the userspace `printf` ignores width specifiers (`%-46s` prints literally). **No new syscalls** (still 52). Verified: 4-CPU smpstress **458k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm` **0 faulted / 0 MAPCORRUPT / 0 PANIC**. **Still open from the same audit:** 4 HIGH (socket fd confused with a VFS handle in `dup2`/`mmap`; `mprotect` can make the shared libc writable; `vm_protect_range` clears `PTE_COW` and breaks fork isolation) and 12 MEDIUM. |
| v5.9.0-rc3 | **Two more CRITICALs closed, from the completion audit of the six areas whose verification had died on a quota limit.** **(1) `ext2_readdir` stack smash (ext2.c:597).** ext2 allows names up to 255 bytes; `dirent_t.name` is `MAX_FILENAME` (128). The copy was unclamped, so a long name wrote up to 128 bytes past the field and, for the last slot, past the caller's whole array — a kernel stack smash with attacker-chosen bytes. The verifier found this **understated**: no hostile disk image is needed, because `ext2_create` does not bound the name either, so an unprivileged process can plant a 200-character name itself and then trigger the smash through `getdents`. Fixed with a clamp, plus two adjacent defects the verifier flagged in the same loop: a `rec_len < 8` floor (a zero `rec_len` made the scan spin forever) and a check that the name actually lies inside the block that was read. **(2) `elf_load_image` had no user/kernel bound on `p_vaddr` (elf.c:84).** `alloc_page_directory` copies `kernel_pml4[511]` **by value** into every process PML4, so a PT_LOAD with a higher-half `p_vaddr` walked into the page tables the kernel itself runs on — either grafting a ring-3-accessible page at a fixed kernel VA in every address space (never reclaimed, since `free_page_directory` stops below PML4[511]) or writing a PTE into identity-mapped kernel RAM. A user program only had to write a file and `execve` it. Also fixed alongside it: `offset + filesz > size` **overflowed**, so a crafted `p_offset` near 2^64 passed the extent check and copied from outside the image buffer. **`USER_SPACE_MIN`/`USER_SPACE_END` moved from syscall.c to kernel.h** — the user/kernel boundary is not a syscall concept, and the ELF loader needing the same bound is exactly how it drifts. One definition now. **No new syscalls** (still 52). Verified: 4-CPU smpstress **595k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC** — and pstorm execve's constantly, which is what proves the new ELF bound does not reject legitimate binaries. **Audit status:** 18 areas covered across two passes, 64 confirmed findings total. 22 findings remain UNVERIFIED (the quota limit hit again) — notably several claimed-critical ones in the compositor, editor_win and switch.asm that must be checked before v5.9.0 ships. |
| v5.9.0-rc4 | **The missing `cld` — every interrupt entry path was running with whatever RFLAGS.DF ring 3 left set.** Two findings the completion audit could only claim (its verifiers died on the quota) and which I checked by reading the code; both were real. **(1) No `cld` anywhere except boot.asm.** DF is part of ring 3's state and survives into a handler, so a user program can execute `std` and simply wait for a timer interrupt. Every rep-string in the kernel then runs BACKWARDS — and `memcpy_asm`/`memset_asm` in stubs.asm are literally `rep movsb` / `rep stosb`, so effectively every copy and clear in the kernel would walk down through memory instead of up. The SysV ABI also requires DF clear on entry to any C function, so compiler-emitted string ops assume it too. The `syscall` path was already covered (IA32_FMASK clears IF and DF); interrupts and exceptions were covered by nothing. Fixed by putting `cld` in the **SAVE_REGS macro**, which covers irq_common, isr_common and ap_timer_stub at once so a future stub cannot forget it, plus tlb_ipi_stub which does not use the macro. **(2) `switch_context` (switch.asm) was thoroughly broken.** The audit spotted that it pushed 14 registers and popped 15, so resuming a context it had itself saved consumed the return address as if it were RAX and `ret` jumped to whatever sat above. Fixing it exposed a second, unreported bug: with 14 pushes the saved RDI/RSI sit at +72/+80, but the code read **+96/+104** — i.e. it took `rcx` and `rbx` as its two arguments. Both fixed and the frame documented. It is **dead code** (the preemptive scheduler switches through irq_common's saved_rsp/next_rsp; nothing calls it), which is why neither bug had ever been observed — but a broken context switcher sitting in a kernel is a trap for whoever wires it up next. **No new syscalls** (still 52). Verified: 4-CPU smpstress **607k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**. |
| v5.9.0-rc5 | **The mprotect family: fork isolation restored, and the address range bounded (3 HIGH + 1 MEDIUM).** **(1) `vm_protect_range` destroyed copy-on-write.** It rebuilt each PTE from the frame address alone, which dropped `PTE_COW` and, for `PROT_WRITE`, granted `PAGE_WRITABLE` outright. So after `fork()`, an `mprotect(PROT_WRITE)` on shared memory let one process write the frame the other was still reading — no fault, no copy, **silent cross-process corruption**, which is precisely the isolation `fork()` exists to provide. Now a COW page keeps its marker and is denied the write bit, so the first write still faults into the COW handler, privatises the page, and only then becomes writable. `PTE_DEMAND` is preserved for the same reason. **(2) `do_mprotect` and `do_munmap` accepted ANY address and ANY length.** Aimed at the shared-libc mapping, mprotect made those pages writable for one process — and since that is a single physical copy mapped read-only into every process, it is a **shared-library patch visible system-wide**. Aimed at a higher-half address, both walked the kernel's own page tables. With a huge length, the per-4 KB loop ran for billions of iterations with interrupts masked — an unprivileged hard hang. All three fall to one bound, applied to both functions, using the same `USER_SPACE_MIN`/`USER_SPACE_END` that `user_v2p` and the ELF loader now share from kernel.h. Wrap-around (`end <= addr`) is rejected too. **No new syscalls** (still 52). Verified: 4-CPU smpstress **609k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC** — pstorm forks and COWs continuously, which is what shows the COW-preservation change does not break legitimate sharing. |
| v5.9.0-rc6 | **Three of the unverified findings checked by hand — all three real, all three fixed.** **(1) `tss_entry_t` had twelve reserved bytes where Intel Vol 3 Fig 7-11 specifies eight** (a leftover of the 32-bit SS0/SS1/SS2 fields). Every IST slot therefore sat four bytes too high: `tss_set_ist()` wrote IST1 at offset 40 while the CPU reads it from 36, so the double-fault handler's stack pointer was always garbage and a double fault triple-faulted into a reboot instead of reaching the panic screen. **The IST mechanism had never worked.** Struct corrected to the spec layout (104 bytes, iomap_base at 102). Note RSP0 is at offset 4 and was never affected, which is why ring transitions always worked and this stayed invisible. **(2) `window_create` never zeroed the window_t it kmalloc'd.** Every field the function does not explicitly assign was heap garbage — including the `on_pressed`/`on_key` callback pointers, which the compositor guards with `if (win->on_pressed)` before calling. A non-NULL garbage value passes that guard and is then **called**, so opening a window could jump to an arbitrary address. Now zeroed before the field assignments. **(3) `editor_save()` put a 128 KB buffer on a 4 KB kernel stack.** `char buf[EDITOR_MAX_LINES * EDITOR_LINE_LEN]` is 512 × 256 = 131072 bytes; kernel task stacks are `kmalloc(4096)`. Every single Save in the Text Editor overflowed the kernel stack by a factor of thirty-two. Moved to the heap, with the two `sizeof(buf)` bounds rewritten (they would have become `sizeof(char*)` = 8) and a matching `kfree`. **No new syscalls** (still 52). Verified: 4-CPU smpstress **603k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**. **Caveat on (1):** the suite exercises RSP0 on every syscall, so the ring-transition half of the TSS is well covered, but the IST path itself is only reachable from an actual double fault and is therefore NOT directly tested — the fix rests on the layout matching the manual. |
| v5.9.0-rc7 | **Three more HIGHs: framebuffer clipping and the dynamic linker.** **(1) `fb_blit()` had no bounds check of any kind.** It wrote `h` rows of `w` pixels at `(dx, dy)` without ever comparing against `fb_width`/`fb_height`, so any caller blitting near the right or bottom edge — a window dragged there, Paint's 512x384 canvas — wrote straight past the end of the framebuffer into whatever the allocator had placed after it. Now clipped, with the subtlety that matters: the copy is narrowed but the SOURCE STRIDE is not, since source rows remain the original width apart. Off-screen negative coordinates arrive as huge unsigned values and are caught by the same test. **(2) `do_dlopen` kept a raw pointer into the VFS node's buffer** (`dynlibs[slot].elf = data`, justified by a comment reading "ramdisk data persists"). That holds for initramfs files and for nothing else: `dlopen("/tmp/x.so")` followed by `unlink()`, a `write()`, or an `open(O_TRUNC)` frees or reallocs that buffer, and `do_dlsym` then walks freed memory on behalf of whoever calls it. The registry outlives the file, so it now takes its own kmalloc'd copy. **(3) `do_dlsym` trusted almost every field of a user-writable file.** Only `e_shoff`/`e_shnum` and `sh_link` were validated; `sh_offset`, `sh_size` and `st_name` were taken on trust, so a crafted `.so` made the symbol loop read wherever it liked and `strcmp` walk off the end of the image. All three are now bounded against `elf_size`, and the string table is required to be NUL-terminated at its end — which is precisely what makes an in-range `st_name` safe to hand to `strcmp`. **No new syscalls** (still 52). Verified: `dltest` resolves all three symbols through the hardened path (`dlopen -> handle 1`, `demo_name`, `demo_gcd(48,36)=12`, `demo_fib(10)=55`, **OK**); 4-CPU smpstress **607k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**. |
| v5.9.0-rc8 | **`dup()` of a mount-backed file was a double free.** A VFS fd IS the `vfs_node_t*`, and `dup` aliases it — which `SYS_DUP`'s own comment justified with "vfs_close is a no-op for the ramdisk nodes ring 3 opens, so closing either fd later is safe". That is true for ramdisk nodes and **false for a mount-backed mirror**, which `vfs_close` frees: `->data` was `kfree`d twice and the node returned to the pool twice, after which the pool hands the same node to two unrelated opens. Note `dup2` was already correct — it MOVES a VFS handle precisely to avoid this, and the comment there says so; only `dup`, which must leave both fds open, was exposed. Fixed with an alias count on the mirror (`dup_refs`): `vfs_dup()` on the aliasing path, and `vfs_close` releases only when the last holder closes. Sockets and pipes are untouched — pipes were already refcounted. **No new syscalls** (still 52). Verified: 4-CPU smpstress **623k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK** with heap and shared fds intact, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**. **Caveat:** the regression suite runs diskless, so it exercises the ramdisk path rather than a real `/mnt` mirror — this fix is verified as non-breaking, not as exercised. A `-hda` run doing `dup` on a `/mnt` file would close that gap. |
| v5.9.0-rc10 | **`libseg_load` — the last confirmed CRITICAL — validated nothing at all.** It is the FIRST code to touch a shared-object image, running before both `do_dlopen` and `do_dlsym`, which the earlier passes hardened; `dlopen()` will happily be aimed at a `.so` the calling process wrote itself. The damage was not subtle: the segment copy reads from `data + offset` with **`p_offset` never compared against `size`**, so a crafted program header copies arbitrary kernel memory into a frame that `libseg_map` then maps INTO THE PROCESS — an arbitrary kernel-memory disclosure readable straight from ring 3. Nothing else was checked either: the program-header table could sit outside the image, `e_phentsize` was unvalidated, `p_filesz` could exceed `p_memsz`, and `p_vaddr` was unbounded — and since `libseg_map` installs these frames into the process PML4 whose higher half mirrors the kernel's own tables by value, a higher-half `p_vaddr` writes into kernel page tables, the same class already fixed in `elf_load_image`. Now bounded on all five axes, with every comparison written subtraction-first so the arithmetic cannot wrap. **This completes the audit's critical set: all 6 confirmed criticals across both passes are closed.** **No new syscalls** (still 52). Verified — and the verification matters here because `libseg_load` maps the SHARED LIBC at boot, so bounds that were too strict would stop the machine dead rather than fail quietly: `[LIBC] shared libc mapped at 0x30000000` still succeeds, `dltest` resolves all three symbols through the hardened path (**OK**), 4-CPU smpstress **472k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**. |
| v5.9.0-rc11 | **Ring-3 flags that survived into the kernel, and the rest of the ELF header validation (1 HIGH + 3 more).** **`IA32_FMASK` cleared only IF and DF.** Everything else in RFLAGS is ring-3 state that rode straight into kernel code. Now also cleared: **TF** — a syscall made with single-step set raised a #DB on the very first kernel instruction, a ring-0 debug exception from an unprivileged program at a moment of its choosing; **NT**, which corrupts IRET behaviour; and **AC**, which matters most because AC is the flag that **suspends SMAP** — the kernel enables SMAP (v5.8.86) precisely so a stray ring-0 access to a user page traps, and letting ring 3 hand us AC=1 switched that protection off for the whole syscall. This is the same family as the `cld` fix one level up: the syscall path was covered for DF and nothing else. **ELF header validation, three more holes.** (1) `e_phentsize` was never checked, while the bound test computes from it and the `phdr[i]` indexing strides by `sizeof(elf64_phdr_t)` — `e_phentsize=1` with `e_phnum=1000` passes a 1000-byte check and then reads 56000. Now required to equal the struct size exactly. (2) The bound's multiply had already been widened to 64-bit, but the **addition still wrapped**, so a huge `e_phoff` sailed past; rewritten subtraction-first. (3) `e_entry` was never validated — and it ends up in an iretq frame, so a non-canonical or higher-half entry point does not kill the process, it faults **the return to user in ring 0**: a kernel panic triggered by running a crafted file. (4) `p_memsz` had no practical ceiling; the user-space bound still permits terabytes, and the loop allocates a frame per page with no way out, so one crafted PT_LOAD walked off with every free page in the system. Capped at `ELF_MAX_SEGMENT` (256 MB). **No new syscalls** (still 52). Verified — and these changes fail loudly rather than quietly, since a wrong FMASK bit or a too-tight entry bound stops every binary from running: `[LIBC] shared libc mapped`, `dltest` **OK**, 4-CPU smpstress **466k ops / 0 integrity failures** / balanced, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**, single-CPU **PASS**. |
| v5.9.0-rc12 | **The 22-pixel GUI family: one missing term, six places.** A window's (x, y) is the top-left of its TITLE BAR and its content starts `TITLE_H` = 22 px below. The compositor already had `win_client_y()` for exactly this — but `static inline` inside compositor.c, so no window module could use it. Every hit-test handler re-derived the origin from `win->y` and forgot the title bar, which is why clicks in Paint, the Text Editor and Sound Test all landed exactly 22 px above what was drawn. Fixed at the root: `WIN_CLIENT_X`/`WIN_CLIENT_Y` are now exported from compositor.h, so the draw path and the hit tests share ONE definition of where content begins instead of each module being right or wrong on its own. **The audit reported three sites; the sweep found six.** Paint had two (click AND mousemove, so strokes landed offset as well), and two more were never reported at all: `editor_win.c:276` — the editor's TEXT AREA, not just its toolbar, so clicking to place the cursor was off by the same 22 px — and `calc_win.c:165`, the calculator's entire button grid. **Two modules were already correct** (`fileman_win`, `minesweeper_win` spelled out `win->y + TITLE_H` by hand); they were converted to the macro so the invariant is stated once rather than re-derived. **No new syscalls** (still 52). Verified: 4-CPU smpstress **629k ops / 0 integrity failures**, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC** — the compositor boots and the desktop renders. **Honest limit:** the regression suite is headless and does not click, so this is verified as non-breaking and correct BY CONSTRUCTION (draw and hit-test now expand from the same macro), not by observing a corrected click. `compositor.c:264`, the start-menu hit test, is a DIFFERENT bug (20 px, plus an item-count off-by-one) and is deliberately left for its own fix. |
| v5.9.0-rc13+rc14 | **The editor no longer eats your file, and the file manager stops indexing entries[-1].** **`edit` (user/edit.c) truncated silently in THREE independent places** — an 8192-byte load buffer, `MAXLINES` 200, `MAXCOL` 200 — and then `save()` opened the original with `O_CREAT | O_TRUNC` and wrote back only what had survived. Opening a 15 KB file and pressing ^O destroyed it. The chosen fix is to **refuse the save**, not to raise the limits: the editor cannot know which part of the file mattered, so refusing is the only honest behaviour. Three details that decide whether the fix actually works: the check runs **before** the `open`, because `O_TRUNC` empties the file the instant it succeeds and a later check would already have destroyed what it protects; the warning is a **persistent** `[READ-ONLY]` marker in the status bar rather than a transient message, since the main loop clears `msg` on any keystroke and the user would otherwise only find out when saving; and the two new strings were **cut to fit `msg[48]`** — at 53 and 60 characters they would have been a `strcpy` overflow, which would have been an ironic way to fix a data-loss bug. **Proven end to end:** `cp /libc.so /big.txt` (15024 bytes), open in `edit`, press ^O — the status bar shows `[READ-ONLY]` and `REFUSED: truncated on load, would lose data`, and the file is still **15024 bytes** afterwards. **`fileman_win.c` committed a rename through `entries[fm->sel_index]` with no bounds check.** The selection is validated when the rename STARTS, but it can be cleared while the user is still typing the new name — `fileman_refresh()` drops it when a search filter stops matching, and deleting resets it to -1. Committing then read 64 bytes from before the array and handed them to `vfs_rename` as a path. Every other consumer of `sel_index` already guards it (lines 134, 160, 169); the commit path was the one that did not, and now does, cancelling cleanly instead. **No new syscalls** (still 52). Verified: 4-CPU smpstress **520k ops / 0 integrity failures**, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**. |
| v5.9.0-rc15 | **The ext2 write path, verified by `e2fsck` instead of by itself.** Nine defects in the one subsystem whose damage outlives a reboot, all the same shape — *a field written without asking how much room it had*. **The allocator handed out the wrong block.** ext2 numbers the block bitmap from `first_data_block`, not from zero; on the 1024-byte-block image NyxOS ships that is 1, and `ext2_alloc_block` used the bit index AS the block number. So it returned block K while marking bit K, which owns block K+1: the block it handed out was one the bitmap still called used (the last inode-table block on a fresh image), and the block it marked used was one nobody held. It *looked* fine because the collision lands on the tail of the inode table, which an almost-empty disk never reaches — a silent overwrite of live metadata waiting for the disk to fill up. `bit_to_block`/`block_to_bit` now convert, and alloc and free always move together. **The 13th block of every large file was zeros.** `ext2_write_file` zeroed the caller's staged data to initialise a fresh indirect block, then wrote those zeros out as the file's data. Fixed by *deleting* the memset — `ext2_alloc_block` already zeroes what it returns. **`de->name` is at +8, not +4**, so `memset(de->name, 0, rec_len - 4)` cleared four bytes past the entry — off the end of the block buffer whenever the entry was the last in its block, which is the common case, because the last entry's `rec_len` runs to the end of the block. **`name_len` is a uint8** fed an unbounded `strlen`, so a 300-byte name recorded itself as 44 while the `memcpy` copied 300. **`ext2_create_file` carried a verbatim copy of `add_dirent_to_parent`**, so every bound bug existed twice and had to be found twice — plus a `links_count++` on the parent for a plain file, which is not how ext2 counts links; it now calls the one implementation. **`free_inode_blocks` read the level-2 indirect block into the buffer its level-1 walk was still iterating** — precisely the aliasing this file's five-buffer split exists to prevent, at the one line the split never reached. **A 4096-byte block buffer lived on the kernel stack**, sized by a guess rather than by `block_size`, in a function that also descends into the ATA layer. **`resolve_path` copied path components into a 256-byte stack array with no ceiling** — the most-travelled function in the driver, reachable from any process that can name a file. **rmdir called a directory empty if no entry had a name longer than two characters**, so a real file called `ab` did not count and the directory was deleted with a live child inside. Mount now validates the geometry it reads off the disk (`log_block_size`, `inodes_per_group`, `inode_size`, the `*_per_group` bit counts against one bitmap block) — each of those fed a divide or a buffer size, so a corrupt image was a `#DE` or an overflow rather than a mount failure. Also closed the consistency gaps `e2fsck` reported: `i_blocks` now charges the inode for its indirect block, deleted inodes get a real `dtime` (from a new RTC-backed `ext2_now()`, so files stop being dated 1970 too), and the superblock free counters and the BGD directory count are actually written to disk — `ext2_sync_superblock()` existed but had **no callers**, so the on-disk totals drifted further from the truth with every write. **Verified against the format, not against NyxOS.** A fresh `mkfs.ext2 -b 1024` image, a workload run inside QEMU, then `e2fsck -fn` and `debugfs` from the host: **e2fsck exit code 0, all five passes clean, zero `Fix?` prompts**, and a 23 KB file copied through the indirect-block path comes back **byte-for-byte identical**. That last check is the point — the `first_data_block` bug both wrote and read back consistently, so no test run from inside the OS could ever have seen it. **No new syscalls** (still 52). Regression: 4-CPU smpstress **535k ops / 0 integrity failures**, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**. |
| v5.9.0-rc16 | **A VFS fd IS a node pointer — so the node pool must know who is holding one.** `vfs_open` returns `(int)(uintptr_t)node`; there is no descriptor table. That single design fact is what made three separate reports one bug: the pool had no idea whether anyone still held a node, so it recycled and wiped them freely. **`vfs_unlink` never returned the node to the pool.** It `memset`'d the struct in place and dropped the pointer — no `free_node`. Every single delete permanently burned one of the 256 slots (root alone holds ~58) until file creation simply stopped working, with no error that pointed anywhere near the cause. It also wiped the struct with no regard for open fds, and an fd here points straight at it. **`proc_sync` handed live `/proc` nodes back to the pool.** It runs from `vfs_open`/`vfs_isdir`, so `cat /proc/<pid>/status` on a process that exits mid-read had its node freed and reissued by the very next open — the reader then read, and could write through, whatever file got the slot. **`free_node` had no double-free guard**, so any path reaching it twice put one node in the free list twice and the next two allocations handed it to two different files. Fixed as one mechanism rather than three patches: an `open_refs` count on every node kind (1 after open, +1 per dup, −1 per close), an `orphaned` flag so a node unlinked while open leaves the tree immediately but is freed by its last close (the unlink-while-open semantics callers already assumed), and an `on_free_list` flag in `free_node`. The old `dup_refs` — which counted only mount mirrors, leaving a dup'd `/proc` handle untracked — is subsumed by `open_refs`. `vfs_unlink` also now **refuses a non-empty directory** instead of dropping the whole subtree on the floor: the directory node went away and every node beneath it stayed allocated but unreachable, which was both a leak and silent data loss. **Measured, not argued.** `vfsfill.elf` fills a directory until the VFS refuses, then unlinks everything it made — its own comment claims this "returns the inode pool so the test can be re-run", an intention the code never honoured. Running it three times is the whole proof. Old code: **128, then 12, then 0** — the pool was dead after two passes and the system could not create another file until reboot. Fixed: **128, 128, 128**, with `/sh.elf` still stating correctly each time. The control run was done by temporarily restoring the old `vfs_unlink` body in place, so the two numbers come from the same harness and the same build path. **No new syscalls** (still 52). Regression: 4-CPU smpstress **614k ops / 0 integrity failures**, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**, and the rc15 ext2 check still reports **e2fsck exit 0** with the 23 KB file byte-identical. |
| v5.9.0-rc17 | **The three GUI highs — and a fourth found only by actually clicking.** **`gui.c:103` was a divide by zero in ring 0.** The paint loop interpolated between the previous and current pointer positions with `(x1 - x0) * i / steps`, and `steps` is 0 whenever the pointer has not moved since the last sample — i.e. any time the user holds the button down without dragging, which is the most ordinary thing to do with a paint tool. The loop still ran once, so `0 * 0 / 0` raised `#DE` in the kernel. At zero steps the endpoints are the same point, so there is nothing to interpolate; it plots and moves on. **`compositor.c:264` disagreed with the drawing code on all three of its numbers.** Entries start `START_ITEM_Y` = 28 px below the menu top, under the green header, and the hit test subtracted 8 — twenty pixels of skew, so the lower two thirds of every entry selected the one BELOW it and clicking "Terminal" opened Settings. The menu has thirteen entries and the test rejected `>= 12`, so Minesweeper could not be launched from the menu at all (the `case 12:` action existed and was reachable only from a desktop icon). And because C integer division truncates toward ZERO, a click on the header computed index 0 rather than something negative, the `< 0` guard never fired, and clicking the title bar launched File Manager. Fixed the way the TITLE_H family was: `START_HDR_H`/`START_ITEM_Y`/`START_ITEM_H`/`START_ITEM_N` are now read by both the drawing loop and the hit test, instead of each deriving the layout and being right or wrong on its own. **`fb.c` was worse than reported.** `fb_enable_backbuffer()` returned early when a buffer already existed — without repointing `fb_addr` at it. After a logout that pointer is the HARDWARE framebuffer, because `fb_use_lfb_direct()` sent the login screen there, so the *second* desktop of a session drew every element straight onto the visible screen (the flicker double buffering exists to prevent) while `fb_present` kept blitting the back buffer — still holding the previous user's frozen desktop — over it every frame. It now repoints and clears the buffer, so nothing from a session that logged out can survive into the next, and `fb_present` refuses to publish unless drawing is actually going to the back buffer. **The fourth: menu transitions were level-triggered.** The whole left-button block runs under `if (btns & 1)`, which re-runs every compositor iteration while the button is held — right for dragging, wrong for menus. Pressing "Menu" toggled it open on iteration 1; on iteration 2 the button was still down and the pointer was still on the taskbar at y=750, outside the menu rect, so the click-outside branch closed it again. The Start menu opened and shut inside a single press and whether anything survived came down to how long the click lasted. Opening, closing and selecting are all edge events; only the drag paths are level events. **Verified by driving the real PS/2 mouse** through the QEMU monitor and reading frames back as PNGs — the first suite in this project that clicks rather than types. It confirmed the drawn menu geometry matches the new constants exactly (13 entries at `360 + 28i`, Minesweeper last at y≈708) and that a press now opens the menu deterministically where the old build was parity-dependent. **Then verified end to end, by clicking.** The scripted harness could not deliver a button press — QEMU's HMP `mouse_button` does not itself emit a PS/2 packet — so the checks were driven on the real machine instead, with a hybrid that covers each tool's gap: the QEMU monitor positions the guest cursor exactly, and a genuine host press supplies the button. (`left_click` is unusable here: it moves the host pointer first, and under grab that relative motion drags the guest cursor off target. Press and release with no move in between works.) Results, each read off a screenshot: clicking the visual centre of **"Terminal"** (index 3) opened a **Terminal** — the old 20 px skew would have opened Settings; clicking **"Minesweeper"** (index 12), with no Minesweeper window on screen beforehand, **opened one** — the old `>= 12` guard rejected that index outright; clicking the **green header** opened nothing and left the menu up, with **Calculator** (index 11) clicked immediately afterwards as a positive control proving the input path was live at that moment, so "nothing happened" means ignored and not undelivered. For `gui.c`, the serial log confirms `gui_demo` was actually running (`Launching GUI demo...`) and two four-second press-and-holds **without moving the pointer** — the exact `steps == 0` condition — produced **0 PANIC, 0 Exception**. For `fb.c`, a real logout showed a clean login screen with no trace of the session behind it, and the second desktop came up **completely clean** — no Calculator, no Minesweeper, no second Terminal from the session that had just ended, which is the stale back buffer the fix clears. Harness kept at `scratchpad/guiverify.py`. **No new syscalls** (still 52). Regression: 4-CPU smpstress **0 integrity failures**, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**, and no faults across every GUI run. |
| v5.9.0-LTS | **P0.1 CLOSED — the intermittent ring-3 `[fault]` was a preemptible CR3-switch→iretq window in the syscall-exit path. LTS release.** The one release-blocking bug in the default configuration, open across the whole SMP arc, is root-caused and fixed with a single `cli`. **The mechanism, found by REASONING after in-guest instrumentation was proven to perturb the race away:** the syscall-exit path (`isr_stubs.asm`) does `mov cr3,[user]` and then runs ~20 instructions (CPL 0, already on the USER CR3, committed to ring 3) before the `iretq`. If a timer preempts the task *in that window*, `irq_common` saves a frame whose CS reads 0x08 — genuinely ring 0 — so `irq_scheduler_tick` resumes it on the KERNEL CR3 under its rule "a ring-0 frame switches to user itself before its iretq". But this task has ALREADY switched, so its `push 0x1B; iretq` returns to ring 3 **on the kernel CR3**; the first user instruction fetch faults with CR2==RIP. **Why it matched every observed property:** the window is only preemptible with IF=1, and syscall ENTRY masks IF via SF_MASK — so a *simple* syscall runs cli'd end-to-end and can never be hit. Only BLOCKING syscalls (`futex_wait`, `do_waitpid`, poll, pipe read) `sti` to yield and *return with IF=1* (verified: both break paths of `futex_wait` leave IF set), which is why the fault was **always in `threads`** (the only futex-heavy workload), **load-dependent** (the timer must land in ~20 instructions), and **present in the default single-core config** (it is a preemption-window bug, NOT an SMP race). It also explains why every earlier scheduler-*decision* probe showed a "correct" `saw_cs=0x8`: the decision *is* correct for a ring-0 frame; the frame cannot reveal that the CR3 switch already happened. **The fix:** one `cli` immediately after `signal_dispatch`, before the user-CR3 reload — making the CR3-switch→iretq window non-preemptible; the `iretq` restores the user RFLAGS (IF=1) atomically as it crosses to ring 3. Covers every syscall regardless of what it left IF as; the entry side needs no equivalent (IF=0 from SF_MASK) and the irq/isr return paths run under interrupt gates (IF=0), so no other such window exists. **Verified in the linked binary** (`fa` cli at 0x100343, between `call signal_dispatch` and `mov %gs:0x10,%rax`) and by an **interleaved A/B under build load** alternating the two ISOs every boot: baseline-without-cli reproduced P0.1 (`pid 7 (threads) … cs=0x1b <ON-KERNEL-CR3>`) while the cli build ran **23 valid rounds, 0 faults** under identical contention. The proof is primarily *mechanistic* — the cli closes the only (CPL0 + user-CR3 + IF=1) window in the kernel — with the A/B as the corroborating control. Also this arc: the fault diagnostic was corrected (`isr_common` now passes the faulting CR3 to `isr_handler`; a prior `read_cr3()` there reported the kernel CR3 on *every* ring-3 fault, an artefact that had driven five dead hypotheses), and `sched_forget()` closes a `sched_cur` use-after-free at both `kfree(process)` sites. **No new syscalls** (still 52). Regression on the fixed build: 4-CPU smpstress **0 integrity failures**, `threads` **8000/8000 OK**, `pstorm 30` **0 faulted / 0 MAPCORRUPT / 0 PANIC**; 23+ clean loaded rounds. **Known limits (opt-in, OFF by default, documented):** `smpbalance on` process-spreading and thread-group un-pinning still wedge under fork/exec churn (P2.x) — the default configuration does not touch them, and the VFS/terminal remain unlocked against a user process deliberately placed on an AP. | 
| v5.9.1 | **"Nightfall" desktop — a central GUI palette, and the brand purple everywhere the placeholder green used to be.** First increment of the visual track that follows the LTS. **The refactor that made it possible: `kernel/theme.h`.** Colours were `fb_rgb(r,g,b)` literals scattered across `compositor.c` and every `*_win.c`, so changing the accent meant hunting call sites and the desktop could not be themed at all. Call sites now name a ROLE — `THEME_ACCENT`, `THEME_WINDOW_BG`, `THEME_BORDER`, `THEME_ROW_DIV`, `THEME_TASKBAR_*`, `THEME_TEXT`, `THEME_ON_ACCENT` — and the values live in one header. They are macros rather than a const struct because `fb_rgb()` is a runtime function, which also leaves the door open for a future Settings→accent by swapping them for a table lookup **without touching a single call site**. **The brand fix:** the accent is the brand purple `{130,90,210}` — the wallpaper's own default "Morado" — replacing the leftover placeholder green `fb_rgb(60,120,60)` on the Start-menu header band and its title text, and driving `taskbar_hl` so the **active Menu button and the focused window** now carry the accent. The window controls keep their traffic-light scheme (close red / minimise grey / maximise green): that green is a control affordance, not branding, and recolouring it would have broken a universal convention. **One accent swap was WRONG and the screenshot caught it.** The workspace indicator's active marker was moved to the accent like everything else — but it is drawn on the DESKTOP (`fh - TASKBAR_H - 10`), not the taskbar, and the wallpaper's base colour is user-selectable. Brand purple on the default purple wallpaper rendered the ACTIVE marker nearly invisible while the inactive greys stood out — exactly backwards, and strictly worse than the green it replaced. Zooming that 14x6 px region 8x is what exposed it; at full-screen scale it looked fine. Fixed with a dedicated `THEME_INDICATOR_ON`/`_OFF` pair (near-white vs dark grey) whose comment states the rule: anything painted over the wallpaper must be light-vs-dark, never accent-coloured, because it has to survive all eleven wallpaper choices. The About window's "NyxOS Desktop" heading also moved green → accent (it sits on a dark panel, so the accent reads fine there). **The desktop itself:** `draw_background()` rewritten as a Nightfall sky — a purple vertical gradient, a glowing moon built from concentric halos plus craters, and **110 deterministic LCG-seeded stars** that avoid the moon's area so the composition stays readable at any resolution. Two small helpers back it: `bg_isqrt()` (integer Newton square root — no FPU in the kernel) and `bg_fill_circle()` (horizontal-span fill). Determinism matters here: the star field is seeded, so every boot paints the identical sky rather than flickering to a new one on each repaint. **Branding swept beyond the GUI:** the README badges were still shields-green `00ff9d` while the brand is purple — all seven are now `825AD2`, so the repo, the login screen and the desktop finally agree on one colour. **No new syscalls** (still 52). No kernel-internals change: this increment is compositor + docs only, so the v5.9.0-LTS guarantees (P0.1 closed, 23 clean loaded rounds) carry over untouched. |
| v5.9.7 | **Window-management keyboard shortcuts — and they retire v5.9.6's honest gap.** Four chords, all conventional: **Alt+Tab** cycles focus, **Alt+F4** closes, **Alt+Up** maximises, **Alt+Down** restores (or minimises a normal window). Until now the compositor had exactly one keyboard chord — Ctrl+1..4 for workspaces — and no way to manage a window without the mouse. Three small layers make it work. (1) `keyboard.c` now **tracks left-Alt** (scancode 0x38 was detected and its state *thrown away* — a one-word fix mirroring `ctrl_pressed`), exposed as `is_alt_pressed()`. (2) **Function keys F1-F12 now reach the compositor**: they produce no character, so they were silently dropped, and Alt+F4 needs F4 to arrive — routed as keycodes 0x90-0x9B on the same ring as the arrows, press-edge only, contiguous so `KEY_F1 + n` indexes Fn. (3) The compositor intercepts the chords **before** routing the key to the focused window (each ends in `goto done_click`), so a maximised Terminal never also eats the Tab. **Alt+Tab deliberately does not reuse `focus_next_window()`** — that picks the highest z-order, which is right on destroy but as a cycler would only ever toggle the top two windows, since focusing one bumps it above the other. New `window_cycle_focus()` walks the window array in **slot order** (stable for a window's whole life) so repeated Alt+Tab visits every window in a fixed rotation and wraps — verified Terminal(slot 2) -> Welcome(slot 0). **The payoff beyond the feature: this closes the exact gap v5.9.6 logged as "reasoned, not exercised."** That row admitted the `WSTATE_MAXIMIZED` re-flow branch could not be tested because maximising needed a title-bar click and monitor-only clicking is broken. Alt+Up **is** a headless maximise (the harness sends `sendkey alt-up`), so the test now maximises the Terminal, then `setres 640 480` re-flows a genuinely maximised window, and the screendump confirms it re-derives to fill 640x480 exactly rather than scaling — the branch is now **exercised and correct**, not argued. All four chords proven by screendump: Alt+Up fills the screen, Alt+Down returns to the prior geometry, Alt+Tab moves the accent title-bar + taskbar highlight from Terminal to Welcome, Alt+F4 drops the window count 3->2 and re-focuses by z-order. **Discoverability:** the Welcome window lists the chords (`Alt+Tab switch  Alt+F4 close`, `Alt+Up/Down maximize/min`); it was grown 280->320 px tall so the two added lines still clear the bottom edge at 640x480 — **checked unobscured at 640, all seven lines visible**, not just reasoned from the 0.625 scale. 0 faults, 0 panics across two full sessions (one keyboard, one mode-switch). **No new syscalls** (still 54 — this is all compositor + keyboard driver, no ring-3 surface). |
| v5.9.6 | **Window geometry follows the screen — the follow-up v5.9.5 named as its own honest limitation.** v5.9.5 could only *clamp*, so at 640x480 the Terminal's hardcoded 640x400 was still screen-sized: correct, but "a window" had become "the desktop". The fix names the assumption that was folklore across nineteen `window_create()` call sites — **their literals are authored on a 1024x768 design grid** — and scales that grid onto the live framebuffer, aspect-preserving and **shrink-only**. Terminal now lands at 400x250 and 640x480 gets the same *layout* as 1024x768, just smaller. **Growing is deliberately not done:** on a bigger screen the right answer is more room for windows, not one inflated window with blurrier fixed-pixel content. **The scale runs before the MIN floor and before the clamp**, so the clamp goes back to being the safety net it was meant to be instead of the thing deciding the layout. **The second, more visible half:** both guards so far only ran at *birth*, and nothing had ever revisited an already-open window when the mode changed under it — switching 1024x768 -> 640x480 from the Settings Display tab left **the very window being clicked** 20 px off the right edge and 18 px under the taskbar. New `windows_reflow()` rescales every live window by the OLD->NEW ratio (not from the design grid, so a window the user has since dragged keeps *their* arrangement), carries `normal_*` across so un-maximising cannot strand it, and clamps. **Q16.16, not Q8.8, and that was measured rather than assumed:** at Q8.8 the round trip 1024 -> 640 -> 1024 brought the Terminal back one pixel narrower *every* trip, because 1024/640 = 1.6 is only representable as 409/256 = 1.5977, quietly multiplying by 0.9985 each time. At Q16.16 with round-to-nearest the **size round-trips exactly** (640 -> 400 -> 640). **Position can still shift one pixel, once** — x=300 maps to 187.5 and no precision breaks that tie — but 301 -> 188 -> 301 is a fixed point, **verified identical across three consecutive round trips**, so it is a static ±1 px and not a leak. The mode switch itself is now one function, `display_set_mode()`, shared by the Settings button and a new **`setres` shell command** (whitelisted modes on purpose: `vbe_set_mode()` validates nothing and would happily program a display you can no longer type on). That command exists because the re-flow was otherwise reachable *only* by clicking a button inside the window the re-flow has to rescue — it gives the test a seam through the identical code path, not a parallel copy. **Two traps caught in review, both flagged by comments already in the file:** hoisting the clamp into `window_clamp(win)` broke `normal_x = x` (the locals are the *unclamped* request once the clamp mutates `win->`), and the one `window_create(fb_get_width()/2-150, ...)` call site would have had the scale applied twice and landed left of centre — it is now design-grid `(362, 284)`. **Honest gap:** the `WSTATE_MAXIMIZED` branch of the re-flow is reasoned, not exercised — it is reachable only by a title-bar click, and monitor-only clicking does not work (PS/2, and `mouse_button` emits no packet; `usb-tablet` is not a way out either, since NyxOS has no USB HID driver). What makes that acceptable is that its failure mode is covered: scaling a maximised window instead yields the right width and an over-tall height, and `window_clamp` caps height at exactly `fh - TASKBAR_H - TITLE_H` — the maximised height — and the clamp *is* exercised. **1024x768 is untouched** (scale = 1.0; the default desktop screendumps identically), and 640x480 shows all three boot windows whole, on-screen, clear of the taskbar. 0 faults, 0 panics across six mode switches. **No new syscalls** (still 54 — `setres` is a shell builtin, not a syscall); compositor + one command. |
| v5.9.5 | **No window can be born off-screen.** Same family as v5.9.4's icon fix, one layer up. **Every** `window_create()` call site passes hardcoded geometry — `(100,100,550,380)` File Manager, `(150,120,600,400)` Text Editor, `(80,80,640,400)` Terminal, `(200,120,450,200)` the About demo, ten of them — all sized for 1024x768. `window_create()` clamped only the MINIMUM size (`MIN_WIN_W/H`) and never checked the framebuffer, so at the 640x480 mode Settings offers the About demo spans x=200..650 and hangs off the right edge. That is not merely ugly: **the title bar is the drag handle**, so a window whose right edge (with its close button) is past the screen cannot be closed *or* dragged back — it is simply stuck. Now `window_create()` clamps into the framebuffer: size capped against the usable area (screen minus taskbar minus title bar), then the origin moved back inside, then floored at zero. **Order matters and is commented** — repositioning before capping would just shove an oversized window off the opposite edge. Fixing it at the constructor rather than at ten call sites means no future caller can reintroduce it. **A subtle second bug fixed with it:** `normal_x/y/w/h` — the geometry un-maximising restores to — was copied from the caller's RAW request, so a clamped window would jump straight back off-screen the first time it was maximised and restored; it now records the clamped values. **Verified before and after at 640x480** by temporarily forcing the boot mode: the old build shows the About window's close button sliced by the right edge, the new one shows every window whole and reachable. That temporary `kernel.c` edit was reverted and confirmed byte-identical to HEAD (it showed as modified only through a stale index mtime, and `git diff HEAD` listed no content change). **At 1024x768 nothing clamps** — the largest window is 750x542 inside 1024x710 usable — and a screenshot confirms the default desktop is unchanged. **Honest limitation:** at 640x480 the Terminal (640x400 requested) now legitimately fills almost the whole screen, because its hardcoded size simply *is* screen-sized there. That is correct clamping, not a new defect, but making the app windows' default sizes screen-relative is a real follow-up. **Syscall count is 54** (SYS_GETTIMEOFDAY=52, SYS_NANOSLEEP=53 arrived in `2257403`, between v5.9.1 and v5.9.2). The "still 52" carried in the **v5.9.2–v5.9.4** rows was stale and has been corrected in place; v5.9.1's was accurate for its commit, which is why the count was checked per version rather than swept — and why the 30 older `still 52` claims in the v5.8.x rows, correct when written, were left alone. Compositor-only otherwise; 0 faults, 0 panics. |
| v5.9.4 | **Desktop icons wrap instead of running off the screen.** The icon layout was one fixed row — `desktop_icon_x[i] = 20 + i * 76`, `y = 20`, with no bound and no dependence on the framebuffer size. Nine icons span x=20..692, which is fine at 1024x768 and **broken at the 640x480 mode Settings itself offers**: the ninth icon ("Mines") is drawn from x=628, so only 12 px of it are on screen — and because `desktop_icon_hit()` reads the same coordinates, the part you *can* see is not where the hit test is either. Nothing re-ran on a mode change, so once you shrank the resolution there was no way to get the icon back. Now the layout is a **grid that wraps on the framebuffer width** (`cols = (fw - margin) / ICON_CELL_W`, clamped to at least one column), and the Settings resolution handler calls `init_desktop_icons()` after `fb_init()` so a mode change re-flows it. **A full re-flow, not per-icon clamping:** icons are drag-reorderable, so clamping each stray icon back inside the new bounds would silently stack several on the same spot; re-flowing is both what desktops conventionally do on a mode change and the only option that cannot produce overlaps. **At 1024x768 the computation yields 13 columns, so all nine still sit in one row and the default desktop is pixel-identical** — only the narrow modes change. **Proven before AND after at the failing resolution**, not just reasoned about: the boot mode was temporarily forced to 640x480 and the old layout photographed with the last icon sliced by the right edge, then the same shot with the fix shows it wrapped onto a second row, whole, with its label. That temporary change to `kernel.c` was reverted and verified to leave no diff. 0 faults, 0 panics. **No new syscalls** (still 54); compositor-only. |
| v5.9.3 | **The window frame joins the focus story.** v5.9.2 gave the focused window an accent title bar; its FRAME still did not care. `draw_window_frame()` painted the same fixed 3D bevel (light `180,180,180` top+left, dark `80,80,80` bottom+right) on every window, focused or not, so once a window's 22px title strip was covered by another window there was no focus cue left at all. Now the frame reads focus too: a focused window takes `THEME_ACCENT` on its top+left edge and `THEME_ACCENT_DIM` on bottom+right, so its border and title bar merge into one continuous coloured edge; an unfocused window keeps the neutral bevel, which is what preserves the depth cue between stacked windows. The two bevel greys became `THEME_FRAME_HI` / `THEME_FRAME_LO` rather than staying literals, and the title text plus the three button glyphs now use `THEME_TITLE_TEXT` instead of three separate hardcoded whites. **The traffic-light buttons are deliberately untouched** (close red / minimise grey / maximise green): that is control affordance, not branding, and this is the third increment in which recolouring them was considered and rejected for the same reason. **Both states were verified, not just the good one** — a 2x crop of the focused Terminal shows the purple frame continuous with its title bar, and a 4x crop of the unfocused About window shows the neutral grey bevel intact, which is what proves the conditional actually branches rather than everything simply having turned purple. `scratchpad/shot.sh` + `crop.py`; 0 faults, 0 panics. **No new syscalls** (still 54); compositor-only, so the v5.9.0-LTS kernel guarantees are untouched. |
| v5.9.2 | **The palette actually reaches the apps — and the focused window is finally the brand colour.** v5.9.1 introduced `theme.h` but only the compositor read it: the ten `*_win.c` apps still held **206 `fb_rgb()` literals and zero roles**, so "change the accent in one place and the desktop follows" was true of the chrome and false of everything inside a window. This closes that gap. **Rolled out, deliberately conservative:** 206 → 169 literals, **61 role uses across 8 apps**. Only unambiguously SHARED chrome was converted — window bodies (`45,45,50`), borders (`100,100,100`), row dividers (`55,55,60`), panel header strips (`40,45,55`, 12 copies) and raised button faces (`60,70,80`, 10 copies), the last two newly named `THEME_PANEL_HEADER` / `THEME_BUTTON` because ten apps had each re-invented their own idea of "a button". **App CONTENT colours were left alone on purpose** — a Paint swatch, a Minesweeper cell, the terminal's ANSI palette and the Wallpaper picker's own colour chips are data, not theme; converting them would have inflated the number while making the code wrong. That is why `terminal_win.c` and `wallpaper_win.c` legitimately convert **zero**. **The branding carrier:** the apps' two selection blues (`60,90,130` ×6 and `60,80,120` ×3 — the same concept spelled two ways) both became `THEME_SELECTION`, i.e. the accent, so selecting a file or a row now shows the brand purple. Body text on `220,220,220` was unified onto `THEME_TEXT`, removing a near-duplicate shade. **Window chrome:** `title_active` was an off-brand blue (`60,130,200`); it is now `THEME_TITLE_ACTIVE` = the accent, with `THEME_TITLE_INACTIVE` as its pair. Focus was previously *technically* distinguishable (blue vs grey) but subtle and unbranded — a zoomed screenshot confirms the new pair reads unambiguously: bright purple with white text for the focused window, dark grey for the rest. The accent is safe on a title bar precisely because it is painted on the window frame, a surface the compositor controls — the opposite of the v5.9.1 workspace-marker mistake, and `theme.h` now states that rule next to both roles. **Verified visually** (`scratchpad/shot.sh`, bash + `-monitor pipe`): desktop screenshot plus an 8x crop of the title bars; 0 faults, 0 panics. **No new syscalls** (54 at this point — `gettimeofday`/`nanosleep` landed in `2257403` between v5.9.1 and here); compositor + apps only, so the v5.9.0-LTS kernel guarantees are untouched. |

## Architecture
### Boot flow
1. `boot.asm` (multiboot header, GDT, paging off) → `kernel_main`
2. `init_gdt()` → `init_idt()` → `init_isr()` → `init_irq()`
3. `init_memory()` (bitmap allocator) → `init_paging()` (identity-map 1:1 up to 4MB) → `init_heap()` (16MB heap)
4. `vbe_init()` → `vbe_set_mode(1024, 768, 32)` → `fb_init()` → `init_apic()` → `init_timer(1000)` → `init_keyboard()`
5. `init_process()` → `ensure_idle_process()` → `init_syscalls()` → `init_vfs()` → `init_load_modules()`
6. `init_ext2()` → `init_net()` → `tcp_init()` → `init_background_tasks()`
7. `mouse_init()` → `speaker_init()` → `sb16_init()` → register IRQ handlers → `sti`
8. `initramfs_load()` → `initramfs_boot()` (create initramfs files in VFS)
9. Auto-detect EXT2 on ATA disk → mount at `/mnt`
10. `bootsplash_update(23, …)` → `bootsplash_clear()` → `auth_setup()` → `login_screen()`
11. `compositor_init()` + `compositor_run()` (GUI desktop) OR `launch_shell()` (fallback)

### Critical constraints
- Paging identity-maps 64 MB (16 page tables). Any static data, BSS, or heap beyond 64 MB causes triple-fault.
- Heap is 16 MB (bumped from 1 MB for DOOM + framebuffer). Physical allocator bitmap supports up to 512 MB RAM.
- `_kernel_end` symbol in `linker.ld` marks kernel BSS boundary for memory manager.
- Serial (`init_serial()`) is a stub — only used via direct `outb(0x3F8, ...)`. VGA text mode (0xB8000) is primary console.
- Interrupts are ENABLED (`sti`). Timer (IRQ0), keyboard (IRQ1), mouse (IRQ12), and SB16 (IRQ5) are interrupt-driven with proper PIC unmasking and EOI.
- Cooperative multitasking via `switch_context`/`create_task_stack` (assembly) + background task callbacks + IRQ scheduler tick.
- Per-process paging: `alloc_page_directory` clones kernel identity-mapped page tables with supervisor-only PTEs; user pages mapped via `map_page_dir` with PTE `U/S=1` and PDE `U/S=1` for ring-3 access.
- ELF loader maps code at ELH entry point (e.g. 0x10000), user stack at 0xD0000000 (one page, 4096 bytes).
- Syscalls via `syscall`/`sysret`: rax=number, rdi/rsi/rdx/rcx/r8/r9=args; handlers in `syscall_handler_c` dispatch table (exit=0, write=1, print=2, open=3, read=4, close=5, getpid=6, sbrk=7, fsize=8, exec=9).
- TSS I/O map base set to full TSS size → all I/O ports denied from ring 3.
- build.ps1: WSL cross-compilation; QEMU 11.x with `-audiodev dsound,id=audio0 -device sb16,audiodev=audio0`

## Kernel structure
```
kernel/
  boot.asm        — Multiboot header, GDT, start
  kernel.c        — Main kernel, shell (40+ command handlers), desktop launch
  kernel.h        — All typedefs, extern declarations, constants
  linker.ld       — Linker script with _kernel_end
  memory.c        — Bitmap physical page allocator
  heap.c          — 16 MB kernel heap (kmalloc/kfree)
  paging.c        — Identity-mapped page tables (4 MB)
  process.c       — Process table, background tasks, schedule
  switch.asm      — Context switch (switch_context, create_task_stack)
  vfs.c           — Ramdisk VFS + mount table (EXT2 passthrough)
  ext2.c/h        — EXT2 filesystem driver (read-only, mountable via VFS)
  ata.c/h         — ATA/IDE PIO disk driver
  timer.c         — PIT timer (1000 Hz, interrupt-driven)
  keyboard.c      — PS/2 keyboard (US/ES layouts, AltGr)
  screen.c        — VGA text mode (80x25) + putchar hook for terminal capture
  serial.c        — Serial stub
  syscall.c       — Syscall handler table
  elf.c/h         — ELF64 loader (validate, parse PT_LOAD, map pages, create user process)
  initramfs.c/h   — Initramfs cpio parser (new-style '070701'), creates files in VFS
  initramfs_data.h — Generated C byte array with embedded cpio archive
  net.c           — Network stack init
  ethernet.c      — Ethernet frame handler
  arp.c           — ARP cache + requests
  ip.c            — IPv4 send/receive with checksum
  icmp.c          — ICMP echo (ping)
  udp.c           — UDP sockets
  rtl8139.c       — RTL8139 NIC driver (PCI, I/O, MMIO, TX/RX rings)
  dhcp.c          — DHCP client (DISCOVER → OFFER → REQUEST → ACK)
  tcp.c/tcp.h     — Full TCP connection state machine (8 connections)
  vbe.c           — Bochs VBE framebuffer (up to 1024x768x32)
  fb.c            — Framebuffer abstraction (put_pixel, fill_rect, blit)
  mouse.c         — PS/2 mouse driver (IRQ12, 3-byte packets)
  gui.c           — GUI paint demo (mouse drawing)
  font.c/font.h   — VGA 8x16 bitmap font (256 glyphs)
  compositor.c/h  — Window compositor (32 windows, z-order, drag, resize, close, workspaces)
  terminal_win.c/h — Terminal emulator window (scrollback, Tab completion, command execution)
  fileman_win.c/h — File Manager window (VFS browsing, directory navigation, file preview)
  speaker.c/h     — PC speaker driver (PIT channel 2, beep, melody)
  sb16.c/h        — Sound Blaster 16 DSP driver (DMA, IRQ, mixer, PCM playback)
  editor_win.c/h  — Text Editor window (file open/save, cursor nav, scroll, click)
  imageview_win.c/h — Image Viewer window (test pattern, zoom/pan)
  soundtest_win.c/h — Sound Test window (PC Speaker + SB16 buttons)
  gdt.c/idt.c/isr.c/irq.c — x86 descriptor tables
```

## Shell commands (40+)
| Command       | Description |
|---------------|-------------|
| help          | Show all commands |
| clear         | Clear screen |
| nyxfetch      | System info with ASCII logo |
| echo          | Print text or `echo text > file` |
| ls/cd/pwd     | Filesystem navigation (also on EXT2 via /mnt) |
| cat/touch/mkdir/rm/cp/mv | File operations |
| which/head/tail/grep/sort/wc/find/tree | File utilities |
| write/diff    | Write file / compare two files |
| env/export    | Environment variables |
| history       | Command history |
| ps/kill/mem   | Process management |
| ifconfig/dhcp/ping/setip | Network commands |
| tcptest       | TCP HTTP GET test |
| mount         | Mount EXT2: mount [drive] [part_lba] |
| date/uname/version | System info |
| exec          | Execute ELF binary: exec <file> |
| layout        | Switch keyboard layout (us/es) |
| hexdump       | Dump memory |
| crash         | Trigger kernel panic |
| reboot        | Reboot via 0x64/0xFE |
| doom          | Run DOOM game (requires doom1.wad, legacy) |
| mode          | Set VBE video mode |
| gui           | GUI paint demo with mouse |
| fonttest      | Test bitmap font rendering |
| desktop       | Window compositor desktop |
| beep/play     | PC speaker tones and melodies |
| sb16play      | SB16 DMA audio playback: sb16play [freq] [ms] |

## Shell features
- Tab completion for command names
- Environment variable expansion: `echo $VARNAME`
- Command history (last 10, duplicates filtered)
- `echo > file` redirection support
- Pipe `|` support (`cmd1 | cmd2` with temp file)

## Network stack
- RTL8139: PCI I/O BAR, MMIO, TX/RX rings, link detection, CONFIG1 fix
- ARP: Cache with request/reply, static entries, periodic cleanup
- IP: Send/receive with header checksum, local delivery
- UDP: Raw datagram send, port-based listener registration
- ICMP: Echo request/reply for ping
- **DHCP**: Full client (DISCOVER → OFFER → REQUEST → ACK), auto-config
- **TCP**: Full connection state machine (SYN, ESTABLISHED, FIN, RST handling)
- Single static IP: 10.0.2.15/24 or DHCP-assigned

## GUI subsystem
- Bochs VBE framebuffer (1024x768x32, LFB at 0xE0000000)
- PS/2 mouse (IRQ12, absolute positioning)
- VGA 8x16 bitmap font from Linux kernel (256 glyphs)
- Window compositor: z-ordering, title bars (min/max/close), drag-to-move, resize, 4 workspaces
- Taskbar with running app buttons, Start menu (12 items), clock
- Desktop icons (Files, Terminal, Editor, Viewer, DOOM, Settings, Paint, Sounds)
- Terminal emulator window (scrollback, Tab completion, command execution)
- File Manager window (VFS browsing, directory navigation, file preview)
- Text Editor window (file open/save, cursor navigation, scroll, click-to-position)
- Image Viewer window (color-bar test pattern with zoom/pan, grid overlay)
- Sound Test window (PC Speaker beep/melody/alarm + SB16 sine/square/sweep) 

## v5.1.0 — Stability and bugfix release
- **TSS struct alignment fix**: `kernel/kernel.h` — three reserved `uint32_t` fields (SS0/SS1/SS2 at offsets 28-39) were incorrectly merged into a single `uint64_t`, shifting all IST pointers 4 bytes early. Any exception using an IST (double fault, NMI) read a garbage stack pointer → triple fault.
- **GDT limit correction**: `kernel/gdt.c` — limit was `(6*8+16)-1=63` (too large), corrected to `(5*8+16)-1=55`.
- **APIC/PIC IRQ masking fix**: `kernel/irq.c` — when APIC is active, the legacy PIC stays fully masked. Prevents duplicate interrupt delivery.
- **switch_to_user_process inline asm fix**: `kernel/switch.asm` — AT&T operand order was reversed (source/destination swapped in `mov`), causing exec'd user processes to crash.
- **QEMU display fix**: `run.ps1` — changed from `-display gtk` to `-display sdl` (gtk is broken on Windows).
- **VGA 8x16 font data corruption fix**: `kernel/font.c` — font_data array had marker bytes (the glyph's own index) inserted at position `(index % 16)`, shifting all pixel data. Replaced with correct IBM VGA 8x16 ROM font. GUI text now renders legibly.
- **Boot loop resolved**: kernel boots to desktop in GUI mode and runs stably for extended periods. No double faults, page faults, or crashes observed after running 60+ seconds.
- Build: zero errors, zero warnings. Kernel confirmed stable in both serial and GUI (SDL) mode.

## v5.3.0 — Login system
- **Boot animation**: Full-screen NyxOS-themed splash (~5s) with dark gradient, ASCII bird logo, twinkling stars (60 points), animated spinner (8 rotating dots), progress bar (23 steps with percentage and status text). Fades to black before desktop.
- **Auth backend** (`auth.c/h`): Credential storage in `/etc/passwd` on EXT2 (`username:salt:hex_hash`). First boot creates default user `nyx`/`nyx`. Fallback to hardcoded default when no EXT2 disk. Hash uses djb2 with per-user salt.
- **Login screen** (`login.c/h`): Framebuffer-based login panel (centered dark panel with title, username/password fields). Keyboard input via direct `kbd_buffer` polling with `cli`/`sti`. Password masked with `*`. Enter switches fields or submits. No cursor or mouse to avoid page faults.
- **Integration**: `bootsplash_clear()` → `auth_setup()` → `login_screen()` → compositor in kernel_main. Login failure → reboot via `outb(0x64, 0xFE)`. Success → launch NyxOS Desktop.
- **Keyboard buffer exposed**: `kbd_buffer`, `kbd_head`, `kbd_tail` made non-static in `keyboard.c` for direct polling from login.c.

## Next features to add
- **Userspace polish** (ring-3 works as of v5.5.0; exec reaps on exit + file
  read/write from ring 3, v5.6.0; per-fd offsets / streaming, v5.7.0):
  - Each read/write is still capped at one ≤4 KB `kmalloc` bounce per syscall
    (loop in userspace for more). `vfs_pwrite` reallocs on every grow — fine for
    small files, revisit for large/append-heavy workloads.
  - `copy_from_user`/`copy_to_user` assume user physical pages fall in the 64 MB
    identity map; fine for small programs, revisit if RAM use grows.
  - Jobs: `exec <file>` = foreground (blocks via `kwait`), `spawn <file>` =
    background (v5.7.8); `kill` on a running scheduled proc is safe (v5.7.10); `jobs`
    + `wait [pid]`/wait-all (v5.7.11). The shell job-control set is complete.
- **Preemptive scheduler** — DONE. Kernel threads (v5.7.6, PIT via I/O APIC pin 2
  → vector 32, round-robin behind `sched_enable()`, `mtdemo` self-test). Ring-3
  processes (v5.7.7): the scheduler switches user CR3 + TSS RSP0 for `sched_managed`
  procs and reaps them on exit. `wait()`/job control (v5.7.8): `exec` = foreground
  (`spawn`+`kwait`, `PROC_BLOCKED` + `wake_waiters` on child exit), `spawn` =
  background. Legacy setjmp/longjmp blocking-exec path removed (v5.7.9) — one ring-3
  launch path (scheduler `iretq`) remains. Blocked states + timer wait queue
  (v5.7.10): `sleep()` blocks (`PROC_BLOCKED`+`wake_tick`, scheduler wakes sleepers)
  instead of busy-waiting; `kill` on a running scheduled proc marks it a zombie
  safely. `jobs`/`wait`/wait-all (v5.7.11) round out shell job control. Weighted
  round-robin priority (v5.7.12): `sched_weight`/`sched_quantum`, compositor gets
  weight 4 so the GUI beats background jobs. Heap + VFS re-entrancy guarded
  (`preempt_disable`): kmalloc/kfree (v5.7.6-era) and EXT2 ops + the VFS node pool
  (v5.7.13). Idle desktop yields the CPU (v5.7.15: compositor `sleep(5)`s when nothing
  is happening — the "block instead of poll" win, via a short timeout rather than a
  keyboard wait queue); `nice`/`renice` set job weights from the shell. Remaining
  follow-ups:
  1. **True IO wakeups (optional).** The idle-yield uses a 5 ms timeout; a real
     keyboard/mouse-IRQ wakeup would drop input latency further and let it sleep
     longer, but needs IRQ→scheduler coupling — only worth it if latency matters.
     mtdemo/spin still `hlt`-park after finishing rather than exiting.
  2. **Per-process FS state.** Fds are per-process (v5.7.14: `ufd` table in
     `process_t`, isolated + closed on reap). CWD is now **per-shell** (v5.7.21):
     each Terminal window owns its `current_dir` node and swaps it in around command
     execution, so `cd` in one shell no longer moves another. Still deferred:
     per-*process* CWD for ring-3 code — user procs use absolute paths and there's no
     `chdir` syscall, so it's low value. Also: the FS lock is coarse (whole EXT2 op
     under `preempt_disable`) — fine now, but a buffer-per-op would let FS I/O interleave.
- File Manager: copy/paste + drag-and-drop **done** (v5.7.16 — drop into folders,
  move/Ctrl-copy, paste-`_copy`). Possible follow-ups: multi-select, cut-and-drop
  visual feedback on the target folder, a two-pane view.
- Network: DHCP/DNS/TCP/HTTP all work (v5.7.3–v5.7.4); loopback + ICMP echo replies
  + a real `ping` with RTT/loss stats **done** (v5.7.17); TCP retransmission (RTO +
  exponential backoff, incoming-ACK processing) + robust time-based multi-segment
  receive **done** (v5.7.18); TCP passive open (`listen`/`accept`) + fully in-guest
  loopback TCP with a `tcploop` self-test **done** (v5.7.19); NIC-side `listen` —
  NyxOS serves HTTP to real external clients via `hostfwd`, incl. the server
  half-close path — **done** (v5.7.20). Possible follow-ups: TCP send-side
  segmentation for large bodies, a persistent multi-connection listen loop, and DNS
  reliability under high latency. Note: QEMU slirp's gateway doesn't answer ICMP
  echo and the host ISP may intercept some HTTP — verify against loopback (`tcploop`,
  `ping 127.0.0.1`), an inbound `tcpserve` + host `curl` via `hostfwd`, or a pcap
  dump (`qemu -object filter-dump,id=f0,netdev=n0,file=x.pcap`).
- **Nyx C language runtime DONE** (v5.8.0): Nyx C transpiles to C via `nyxc`, links against `nyxrt` (string/io/syscall runtime), and runs as `hello_nyx.elf` on NyxOS. Next: real `nyxc` compiler tool, more `.nyx` programs, self-hosted nyxc.
- SMP: multi-core bringup (INIT-SIPI-SIPI + trampoline → long mode → `ap_main`)
  works and is exposed via `cpus` **done** (v5.7.22); APs currently park in `cli;hlt`.
  Next (large): per-CPU scheduling — per-core run queues, a real lock (not just
  `preempt_disable`), reschedule IPIs, and TLB shootdown — to actually run threads
  on the APs.
- Page fault advanced features: demand paging + copy-on-write **done** (v5.7.23 —
  `vm_handle_fault` in the #PF path, PTE marker bits, `cowtest`). Still open: swapped
  pages (page-out to disk), and wiring demand/COW into the user-process path (lazy
  `sbrk` heap, COW `fork`) — today it's kernel-space, driven by the `cowtest` self-test.
- EXT2: mount-backed dirs (fd-based `readdir`/`mkdir`/`unlink` on `/mnt`), and
  flush-on-close instead of flush-on-every-write for large files. (Fd-based
  file read/write on `/mnt` now persists — v5.7.1.)

## Security notes (v5.4.0)
- Ring-3 syscall args are validated: `user_ptr_ok`/`user_str_ok` in `syscall.c`
  reject any pointer outside the canonical user half `[0x1000, 0x800000000000)`,
  so a user process can't name a kernel address as a syscall buffer.
- Userspace fds are opaque small integers (`UFD_BASE`+slot) mapped to internal
  VFS handles in `syscall.c`; kernel VFS pointers are never exposed to or
  accepted from ring 3.
- Still open: passwd salt is derived deterministically from the username
  (`gen_salt_hex` = HMAC(secret, username)) rather than random-per-user; the
  on-disk XOR of `/etc/passwd` is obfuscation, not encryption (the stored
  values are already PBKDF2 hashes). Fine for a hobby OS; note it's not a real
  secret-at-rest scheme.
